# 3D配方页面样式统一改造说明

## 改造概述
将3D料架定位配方页面（`rack_location_recipes.html`）的样式从传统表格式设计改造为现代化卡片式设计，与2D泡棉检测配方页面（`recipe_management.html`）保持一致的视觉风格。

## 主要改动

### 1. CSS样式改造
- **标题栏样式**：统一使用左侧色条装饰，字体大小调整为22px
- **卡片布局**：将表格布局改为卡片式布局，每个配方独立一张卡片
- **配方卡片**：
  - 添加hover悬停效果
  - 禁用状态透明度降低并使用灰色背景
  - 层号标签使用主题色圆角徽章
  - 状态标签移至卡片右上角
- **Toast提示框**：统一提示框样式，标题改为"操作成功/操作失败/提示"
- **Modal弹窗**：保持一致的弹窗样式和动画效果
- **按钮样式**：统一按钮大小和配色方案

### 2. HTML结构改造
#### 改造前（表格式）：
```html
<div class="recipe-table-wrapper">
  <table class="recipe-table">
    <thead>...</thead>
    <tbody>
      <tr>...</tr>
    </tbody>
  </table>
</div>
```

#### 改造后（卡片式）：
```html
<div class="recipe-content">
  <div id="recipe-list">
    <div class="recipe-card">
      <h3>
        <span class="layer-badge">第X层</span>
        配方名称
        <span class="status-badge">状态</span>
      </h3>
      <div class="recipe-card-row">标准坐标</div>
      <div class="recipe-card-row">ROI范围</div>
      <div class="recipe-card-actions">按钮组</div>
    </div>
  </div>
</div>
```

### 3. JavaScript代码更新
- 将`row`变量重命名为`card`，适配卡片布局
- 保持所有功能逻辑不变：新增、编辑、删除、启用/禁用配方
- Toast提示时长统一为2000ms

## 视觉效果对比

### 改造前
- ❌ 传统表格式布局，信息密集
- ❌ 表头占用空间，视觉层次不够清晰
- ❌ 按钮为小图标样式，不够直观

### 改造后
- ✅ 卡片式布局，信息层次分明
- ✅ 层号徽章醒目，状态一目了然
- ✅ 按钮样式统一，操作更加直观
- ✅ 与2D配方页面风格完全一致

## 文件修改清单
- ✅ `templates/vision/rack_location_recipes.html` - 完整改造

## 功能保持不变
- ✅ 新增配方
- ✅ 编辑配方（ROI参数、标准坐标）
- ✅ 启用/禁用配方
- ✅ 删除配方
- ✅ 表单验证
- ✅ Toast提示
- ✅ Modal弹窗交互

## 兼容性
- 完全兼容现有后端API
- 不需要修改任何Python/Django代码
- 仅前端样式和HTML结构改造

## 测试建议
1. 访问3D配方管理页面，确认卡片布局正常显示
2. 测试新增配方功能
3. 测试编辑现有配方
4. 测试启用/禁用配方状态切换
5. 测试删除配方功能
6. 确认所有Toast提示正常显示
7. 对比2D配方页面，确认样式一致性

## 改造完成时间
2026年7月2日
