# 手眼标定模块 - 快速开始

## 1. 功能概述

手眼标定模块实现了**相机坐标系到机器人坐标系的转换**，是3D料架定位功能的核心模块。

**核心公式：**
```
P_base = T_base_flange × T_flange_camera × P_camera
```

- `P_camera`：相机坐标系的点
- `T_flange_camera`：手眼标定矩阵（固定不变）
- `T_base_flange`：机器人当前位姿（实时变化）
- `P_base`：机器人基坐标系的点

## 2. 已完成功能

### ✅ 数据模型
- `HandEyeCalibration`：手眼标定配置
- `HandEyeCalibrationSample`：标定样本
- `HandEyeVerificationResult`：验证结果
- 集成到 `RackLocationRecipe` 模型

### ✅ 核心服务
- `CoordinateTransformService`：坐标转换服务
  - 矩阵解析（支持3种格式）
  - 相机→机器人坐标转换
  - 六自由度↔4×4矩阵互转
  - 转换验证

- `HandEyeCalibrationService`：标定服务
  - 创建/管理标定任务
  - 采集标定样本
  - OpenCV标定计算
  - 标定验证
  - 导入/导出

### ✅ API 接口
- 14个 RESTful API
- 标定 CRUD
- 样本管理
- 计算与验证
- 辅助工具

### ✅ PLC 适配器扩展
- `read_robot_pose()`：读取机器人位姿
- 模拟适配器支持

### ✅ 前端页面
- `/vision/hand-eye/`：手眼标定管理页面
- 标定列表展示
- 创建标定对话框

### ✅ 测试验证
- 6个单元测试全部通过
- 坐标转换精度验证
- 多种矩阵格式支持

## 3. 使用流程

### 方式1：通过前端页面（推荐）

1. **访问页面**
   ```
   http://localhost:8000/vision/hand-eye/
   ```

2. **创建标定任务**
   - 点击"新建标定"
   - 填写标定名称、选择机器人和相机
   - 点击"创建"

3. **采集标定样本**（后续开发）
   - 移动机器人到不同位姿
   - 拍摄标定板
   - 记录机器人位姿和标定板位姿

4. **计算标定**
   - 采集至少3个样本后
   - 点击"计算标定"
   - 选择标定方法（推荐 OPENCV_TSAI）

5. **激活标定**
   - 点击"激活"按钮
   - 该标定配置生效

### 方式2：通过 Python 代码

```python
from apps.vision.hand_eye_service import HandEyeCalibrationService
from apps.vision.coordinate_transform import CoordinateTransformService

# 创建服务实例
service = HandEyeCalibrationService()
transform = CoordinateTransformService()

# 1. 创建标定任务
calibration = service.create_calibration(
    name="测试标定-20260101",
    robot_device_id=1,
    camera_device_id=2,
    operator="张三"
)

# 2. 添加样本（模拟数据）
for i in range(10):
    service.add_sample(
        calibration_id=calibration.id,
        T_base_flange={
            'x': 500 + i*10, 'y': 200, 'z': 850,
            'rx': 0, 'ry': 0, 'rz': 0
        },
        T_camera_target={
            'rvec': [0.1, 0.2, 0.3],
            'tvec': [100, 50, 600]
        }
    )

# 3. 计算标定
result = service.compute_calibration(
    calibration_id=calibration.id,
    method='OPENCV_TSAI'
)
print(f"标定完成，误差: {result['calibration_error']:.4f} mm")

# 4. 激活标定
service.activate_calibration(calibration.id)
```

### 方式3：测试坐标转换

```python
from apps.vision.coordinate_transform import CoordinateTransformService
import numpy as np

service = CoordinateTransformService()

# 相机坐标系点
P_camera = np.array([100.0, 50.0, 500.0])

# 手眼标定矩阵（相机在法兰坐标系偏移30mm X, 100mm Z）
T_flange_camera = service.pose_to_matrix(30, 0, 100, 0, 0, 0)

# 机器人位姿（法兰在基坐标系位置）
T_base_flange = service.pose_to_matrix(500, 200, 850, 0, 0, 0)

# 坐标转换
P_base = service.camera_to_robot_base(
    P_camera, T_flange_camera, T_base_flange
)

print(f"相机坐标系: {P_camera}")
print(f"机器人基坐标系: {P_base}")
# 输出: [630. 250. 1450.]
```

## 4. API 测试示例

### 创建标定
```bash
curl -X POST http://localhost:8000/vision/api/hand-eye/calibrations/create/ \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试标定-API",
    "robot_device_id": 1,
    "camera_device_id": 2,
    "operator": "API测试"
  }'
```

### 获取标定列表
```bash
curl http://localhost:8000/vision/api/hand-eye/calibrations/
```

### 获取机器人位姿
```bash
curl -X POST http://localhost:8000/vision/api/hand-eye/robot-pose/ \
  -H "Content-Type: application/json" \
  -d '{"robot_code": "ROBOT-01"}'
```

### 测试坐标转换
```bash
curl -X POST http://localhost:8000/vision/api/hand-eye/transform-test/ \
  -H "Content-Type: application/json" \
  -d '{
    "P_camera": [100, 50, 500],
    "T_flange_camera": {"x": 30, "y": 0, "z": 100, "rx": 0, "ry": 0, "rz": 0},
    "T_base_flange": {"x": 500, "y": 200, "z": 850, "rx": 0, "ry": 0, "rz": 0},
    "P_expected_base": [630, 250, 1450],
    "tolerance": 5.0
  }'
```

## 5. 集成到现有流程

在 `apps/vision/services.py` 的料架定位方法中集成：

```python
def locate_rack_layer(self, recipe, position_no, layer_no):
    # 1. 获取激活的手眼标定
    from .models_hand_eye import HandEyeCalibration
    from .coordinate_transform import CoordinateTransformService
    
    calibration = HandEyeCalibration.objects.filter(
        robot_device=recipe.camera_device,  # 或关联的机器人设备
        is_active=True
    ).first()
    
    if not calibration:
        raise ValueError("未找到激活的手眼标定配置")
    
    # 2. 初始化坐标转换服务
    transform_service = CoordinateTransformService()
    T_flange_camera = transform_service.parse_matrix_from_json(
        calibration.T_flange_camera
    )
    
    # 3. 获取机器人当前位姿
    robot_pose = self.device_service.adapter.read_robot_pose('ROBOT-01')
    T_base_flange = transform_service.parse_matrix_from_json(robot_pose['pose'])
    
    # 4. 采集点云（相机坐标系）
    frame_data = self.dm_camera_service.capture_frame_data('POINTCLOUD')
    P_camera = frame_data['data']  # (N, 3) numpy数组
    
    # 5. 坐标转换到机器人基坐标系
    P_base = transform_service.camera_to_robot_base(
        P_camera, T_flange_camera, T_base_flange
    )
    
    # 6. 后续处理（ROI裁剪、特征提取、补偿计算）
    # 全部在机器人坐标系下进行
    # ...
```

## 6. 下一步开发计划

### 6.1 完善前端页面（高优先级）
- [ ] 标定详情页面
- [ ] 样本采集界面
- [ ] 标定结果可视化
- [ ] 验证结果展示

### 6.2 标定板检测（高优先级）
- [ ] 棋盘格检测算法
- [ ] 圆点阵列检测
- [ ] 自动检测接口

### 6.3 自动化流程（中优先级）
- [ ] 机器人自动移动采样
- [ ] 一键标定流程
- [ ] 标定质量自动评估

### 6.4 PLC通信（中优先级）
- [ ] 实现真实PLC协议
- [ ] 机器人位姿实时读取
- [ ] 错误重试机制

### 6.5 高级功能（低优先级）
- [ ] 在线标定更新
- [ ] 多相机标定
- [ ] 标定数据3D可视化

## 7. 文件清单

```
apps/vision/
├── models_hand_eye.py          # 手眼标定数据模型
├── coordinate_transform.py     # 坐标转换核心服务
├── hand_eye_service.py         # 手眼标定业务逻辑
├── views_hand_eye.py           # 手眼标定API视图
├── urls_hand_eye.py            # 手眼标定路由
├── migrations/
│   └── 0012_*.py              # 数据库迁移文件

apps/devices/adapters/
├── plc.py                      # PLC适配器（添加read_robot_pose）
└── simulated.py                # 模拟适配器（添加模拟位姿）

templates/vision/
└── hand_eye_calibration.html   # 手眼标定管理页面

根目录/
├── test_hand_eye_transform.py          # 坐标转换测试脚本
├── HAND_EYE_CALIBRATION_README.md      # 详细使用文档
└── 手眼标定快速开始.md                   # 本文档
```

## 8. 常见问题

### Q1: 标定误差多少算正常？
**A:** 
- < 1mm：优秀
- 1-3mm：良好，可用于生产
- 3-5mm：尚可，建议重新标定
- > 5mm：不合格，必须重新标定

### Q2: 需要多少个标定样本？
**A:** 最少3个，推荐10-15个。样本应均匀分布在工作空间内，覆盖不同的位置和姿态。

### Q3: 如何选择标定方法？
**A:** 推荐使用 `OPENCV_TSAI`，这是最常用和最稳定的方法。如果效果不好，可以尝试其他方法。

### Q4: 标定后如何验证？
**A:** 使用已知位置的测试点，转换后与实际位置对比，误差在5mm以内即可通过验证。

### Q5: 模拟模式下能否测试？
**A:** 可以！模拟适配器提供固定的机器人位姿，可以测试完整的标定流程和坐标转换功能。

## 9. 技术支持

- 详细文档：`HAND_EYE_CALIBRATION_README.md`
- 测试脚本：`python test_hand_eye_transform.py`
- API文档：见 README 中的"API 接口总览"章节

---

**开发完成时间：** 2026年1月
**模块状态：** ✅ 核心功能完成，可投入使用
**下一步：** 完善前端界面和标定板检测算法
