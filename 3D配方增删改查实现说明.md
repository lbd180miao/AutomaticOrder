# 3D配方增删改查实现说明

## 概述

已将3D配方管理页面改造为**标准的增删改查（CRUD）表格界面**，用户可以在同一个页面完成所有配方的创建、编辑、查看和删除操作。

---

## 页面效果

### 表格式界面

```
┌─────────────────────────────────────────────────────────────────────┐
│  📋 3D配方管理                              [➕ 新增配方] [其他按钮]  │
├─────────────────────────────────────────────────────────────────────┤
│  3D料架定位配方                                        共 3 条配方   │
├─────────────────────────────────────────────────────────────────────┤
│ 配方名称   │ 层号  │ 标准坐标(X/Y/Z)  │ ROI范围  │ 状态    │ 操作 │
├────────────┼──────┼──────────────────┼──────────┼─────────┼──────┤
│ 3D-L1      │ 第1层│ X:100 Y:200 Z:800│ X(-100~)│ ✔已启用 │ ✏🚫🗑 │
│ 3D-L2      │ 第2层│ X:100 Y:200 Z:950│ X(-100~)│ ✔已启用 │ ✏🚫🗑 │
│ 3D-L3      │ 第3层│ X:100 Y:200 Z:1100│未配置  │ ✕已禁用 │ ✏✅🗑 │
└────────────┴──────┴──────────────────┴──────────┴─────────┴──────┘
```

### Modal 弹窗编辑

点击「新增配方」或「编辑」按钮时，弹出Modal对话框：

```
┌───────────────────────────────────────┐
│  新增配方/编辑配方               × 关闭│
├───────────────────────────────────────┤
│  配方名称 *: [3D-L1____________]      │
│  适用层号 *: [1__]                    │
│                                        │
│  ▸ ROI 参数（机器人基坐标系 mm）      │
│    X Min: [-100] X Max: [100]         │
│    Y Min: [-100] Y Max: [100]         │
│    Z Min: [700]  Z Max: [900]         │
│                                        │
│  ▸ 标准坐标（mm）                     │
│    标准 X: [100.000] 标准 Y: [200.000]│
│    标准 Z: [800.000] 标准 Rz: [0.000] │
├───────────────────────────────────────┤
│                        [取消] [保存]   │
└───────────────────────────────────────┘
```

---

## 功能列表

### ✅ 查（Read）
- 以表格形式展示所有配方
- 显示：配方名称、层号、标准坐标、ROI范围、启用状态
- 按层号排序

### ✅ 增（Create）
1. 点击「➕ 新增配方」按钮
2. 弹出Modal，填写：
   - 配方名称（必填）
   - 适用层号（必填）
   - ROI参数（选填）
   - 标准坐标（默认0）
3. 点击「保存」
4. 自动刷新页面显示新配方

### ✅ 改（Update）
1. 点击某行的「✏️ 编辑」按钮
2. 弹出Modal，预填当前配方数据
3. 修改任意字段
4. 点击「保存」
5. 自动刷新页面显示更新后的数据

### ✅ 删（Delete）
1. 点击某行的「🗑️ 删除」按钮
2. 弹出确认对话框："确定要删除配方「XX」吗？此操作不可恢复！"
3. 点击「确定」
4. 自动刷新页面，配方已删除

### ✅ 启用/禁用切换
- 点击「🚫」按钮：禁用配方
- 点击「✅」按钮：启用配方
- 状态标签实时更新

---

## 修改的文件

### 1. `templates/vision/rack_location_recipes.html`

**完全重写，改为表格式CRUD界面**

#### 主要变化：

**① 页面布局**
- 从卡片列表 → 改为数据表格
- 简洁的标题栏
- 表格头部显示配方总数

**② 表格结构**
```html
<table class="recipe-table">
  <thead>
    <tr>
      <th>配方名称</th>
      <th>层号</th>
      <th>标准坐标 (X/Y/Z)</th>
      <th>ROI范围</th>
      <th>状态</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <!-- 配方行 -->
  </tbody>
</table>
```

**③ 操作按钮**
```html
<td class="col-actions">
  <button onclick="editRecipe(id)">✏️</button>
  <button onclick="toggleEnabled(id)">🚫/✅</button>
  <button onclick="deleteRecipe(id, name)">🗑️</button>
</td>
```

**④ Modal弹窗**
- 新增/编辑共用一个Modal
- 根据操作类型动态更改标题
- 包含完整的表单字段：
  - 配方名称
  - 适用层号
  - ROI参数（6个字段）
  - 标准坐标（4个字段）

**⑤ JavaScript逻辑**
```javascript
// 新增配方
document.getElementById('btn-create-recipe').addEventListener('click', () => {
  // 清空表单
  // 显示Modal
});

// 编辑配方
async function editRecipe(id) {
  // 获取配方数据
  // 预填表单
  // 显示Modal
}

// 保存配方
async function saveRecipe() {
  // 收集表单数据
  // 判断是新增还是更新
  // 发送POST或PATCH请求
  // 刷新页面
}

// 删除配方
async function deleteRecipe(id, name) {
  // 确认对话框
  // 发送DELETE请求
  // 刷新页面
}

// 切换启用状态
async function toggleEnabled(id, currentlyEnabled) {
  // 发送PATCH请求
  // 更新页面状态
}
```

---

### 2. `apps/vision/views.py`

**修改 `api_vision_3d_recipes` 函数，支持完整CRUD**

#### 变化：

**① 装饰器**
```python
# 旧：
@require_http_methods(['GET', 'POST'])

# 新：
@require_http_methods(['GET', 'POST', 'PATCH', 'DELETE'])
```

**② GET - 查询**
```python
if request.method == 'GET':
    qs = RackLocationRecipe.objects.all().order_by('layer_no', '-updated_at')
    recipe_id = request.GET.get('id')  # 新增：支持按ID查询
    if recipe_id:
        qs = qs.filter(id=recipe_id)
    # ... 其他筛选
    return _api3d_success({'recipes': [...]})
```

**③ POST - 创建**
```python
# POST - 创建新配方
data = _request_data(request)
recipe = RackLocationRecipe.objects.create(
    recipe_name=data.get('recipe_name') or f"3D-L{data.get('layer_no', 1)}",
    rack_side='BOTH',  # 固定
    position_no=1,     # 固定
    layer_no=_as_int(data.get('layer_no'), 1),
    standard_x=_as_float(data.get('standard_x'), 0),
    standard_y=_as_float(data.get('standard_y'), 0),
    standard_z=_as_float(data.get('standard_z'), 0),
    standard_rz=_as_float(data.get('standard_rz'), 0),
    roi_config=data.get('roi_config') or {},
    # ...
)
```

**④ PATCH - 更新（新增）**
```python
if request.method == 'PATCH':
    data = _request_data(request)
    recipe_id = data.get('id')
    if not recipe_id:
        return _api3d_error('缺少配方ID')
    
    recipe = get_object_or_404(RackLocationRecipe, pk=recipe_id)
    
    # 更新字段
    if 'recipe_name' in data:
        recipe.recipe_name = data['recipe_name']
    if 'layer_no' in data:
        recipe.layer_no = _as_int(data['layer_no'], recipe.layer_no)
    if 'standard_x' in data:
        recipe.standard_x = _as_float(data['standard_x'], recipe.standard_x)
    # ... 其他字段
    
    recipe.position_no = 1  # 固定
    recipe.rack_side = 'BOTH'  # 固定
    recipe.save()
    return _api3d_success({'recipe': _serialize_3d_recipe(recipe)})
```

**⑤ DELETE - 删除（新增）**
```python
if request.method == 'DELETE':
    data = _request_data(request)
    recipe_id = data.get('id')
    if not recipe_id:
        return _api3d_error('缺少配方ID')
    
    recipe = get_object_or_404(RackLocationRecipe, pk=recipe_id)
    recipe_name = recipe.recipe_name
    recipe.delete()
    return _api3d_success({'message': f'配方「{recipe_name}」已删除'})
```

---

## API 接口说明

### 查询配方

**请求：**
```http
GET /api/vision/3d/recipes/
GET /api/vision/3d/recipes/?id=123
GET /api/vision/3d/recipes/?layer_no=1
GET /api/vision/3d/recipes/?enabled=true
```

**响应：**
```json
{
  "success": true,
  "recipes": [
    {
      "id": 1,
      "recipe_name": "3D-L1",
      "layer_no": 1,
      "standard_x": 100.0,
      "standard_y": 200.0,
      "standard_z": 800.0,
      "standard_rz": 0.0,
      "roi_config": {
        "target_roi": {
          "x_min": -100, "x_max": 100,
          "y_min": -100, "y_max": 100,
          "z_min": 700, "z_max": 900
        }
      },
      "enabled": true,
      "created_at": "2024-01-01T00:00:00Z",
      "updated_at": "2024-01-01T00:00:00Z"
    }
  ]
}
```

### 创建配方

**请求：**
```http
POST /api/vision/3d/recipes/
Content-Type: application/json

{
  "recipe_name": "3D-L1",
  "layer_no": 1,
  "standard_x": 100.0,
  "standard_y": 200.0,
  "standard_z": 800.0,
  "standard_rz": 0.0,
  "roi_config": {
    "target_roi": {
      "x_min": -100, "x_max": 100,
      "y_min": -100, "y_max": 100,
      "z_min": 700, "z_max": 900
    }
  },
  "enabled": true
}
```

**响应：**
```json
{
  "success": true,
  "recipe": { /* 新创建的配方对象 */ }
}
```

### 更新配方

**请求：**
```http
PATCH /api/vision/3d/recipes/
Content-Type: application/json

{
  "id": 1,
  "recipe_name": "3D-L1-Modified",
  "standard_z": 850.0,
  "enabled": false
}
```

**响应：**
```json
{
  "success": true,
  "recipe": { /* 更新后的配方对象 */ }
}
```

### 删除配方

**请求：**
```http
DELETE /api/vision/3d/recipes/
Content-Type: application/json

{
  "id": 1
}
```

**响应：**
```json
{
  "success": true,
  "message": "配方「3D-L1」已删除"
}
```

---

## 样式特点

### 表格样式
- 现代化渐变表头
- 行悬停高亮效果
- 禁用行半透明显示
- 等宽字体显示坐标数据
- 响应式布局

### 操作按钮
- 图标化按钮（Emoji）
- 悬停变色效果：
  - 编辑 → 蓝色
  - 删除 → 红色
  - 切换 → 灰色

### Modal弹窗
- 背景模糊效果
- 平滑动画进入/退出
- 分组表单布局
- 清晰的视觉层次

### Toast提示
- 右上角滑入动画
- 成功/失败/提示三种样式
- 自动消失（2.5秒）

---

## 用户操作流程

### 创建配方流程

1. 点击「➕ 新增配方」
2. 填写表单：
   ```
   配方名称: 3D-L1
   适用层号: 1
   
   ROI参数:
     X Min: -100  X Max: 100
     Y Min: -100  Y Max: 100
     Z Min: 700   Z Max: 900
   
   标准坐标:
     X: 100.000  Y: 200.000
     Z: 800.000  Rz: 0.000
   ```
3. 点击「保存」
4. 看到提示："成功 - 配方已创建"
5. 页面自动刷新，表格中出现新配方

### 编辑配方流程

1. 找到要编辑的配方行
2. 点击「✏️ 编辑」按钮
3. Modal弹出，显示当前配方数据
4. 修改需要的字段（如标准Z坐标：800 → 850）
5. 点击「保存」
6. 看到提示："成功 - 配方已更新"
7. 页面刷新，表格显示更新后的值

### 删除配方流程

1. 找到要删除的配方行
2. 点击「🗑️ 删除」按钮
3. 弹出确认对话框："确定要删除配方「3D-L1」吗？此操作不可恢复！"
4. 点击「确定」
5. 看到提示："成功 - 配方已删除"
6. 页面刷新，配方从表格中消失

### 切换启用状态流程

1. 找到要切换状态的配方行
2. 点击「🚫」（禁用）或「✅」（启用）按钮
3. 看到提示："成功 - 配方已禁用/已启用"
4. 状态标签和行样式实时更新

---

## 优点总结

### ✅ 用户体验提升
1. **操作更直观** - 所有操作在一个页面完成，不需要跳转
2. **信息更清晰** - 表格式展示，一目了然
3. **反馈更及时** - Toast提示 + 实时页面更新
4. **编辑更便捷** - Modal弹窗，不丢失上下文

### ✅ 功能更完整
1. **完整CRUD** - 增删改查全支持
2. **快速编辑** - 点击编辑立即弹出表单
3. **安全删除** - 二次确认，防止误删
4. **状态切换** - 一键启用/禁用

### ✅ 代码更规范
1. **RESTful API** - 标准的HTTP方法（GET/POST/PATCH/DELETE）
2. **前后端分离** - API返回JSON，前端处理展示
3. **代码复用** - 新增和编辑共用一个Modal

---

## 测试建议

### 1. 创建测试
```
1. 点击「新增配方」
2. 填写：配方名称=3D-L1, 层号=1, 标准坐标=100/200/800
3. 点击保存
4. 验证：表格中出现新配方，数据正确
```

### 2. 编辑测试
```
1. 点击某配方的「编辑」按钮
2. 修改：标准Z坐标 800 → 850
3. 点击保存
4. 验证：表格中Z坐标已更新为850
```

### 3. 删除测试
```
1. 点击某配方的「删除」按钮
2. 在确认对话框中点击「确定」
3. 验证：配方从表格中消失
```

### 4. 切换状态测试
```
1. 点击已启用配方的「禁用」按钮
2. 验证：状态标签变为「✕ 已禁用」，行变半透明
3. 点击「启用」按钮
4. 验证：状态标签变为「✔ 已启用」，行恢复正常
```

### 5. 验证测试
```
1. 尝试创建配方但不填写必填项
2. 验证：显示错误提示
3. 尝试删除不存在的配方
4. 验证：显示错误提示
```

---

## 注意事项

### ⚠️ 数据一致性
- 删除配方后不可恢复，请确保用户理解
- 建议在生产环境添加软删除功能

### ⚠️ 权限控制
- 当前未做权限校验，所有用户都可以增删改查
- 生产环境建议添加权限控制

### ⚠️ 并发处理
- 多用户同时编辑同一配方可能导致数据覆盖
- 建议添加乐观锁或版本号机制

---

## 总结

✅ **已完成：**
1. 页面从卡片列表改为数据表格
2. 实现完整的增删改查功能
3. Modal弹窗编辑，无需页面跳转
4. 支持启用/禁用状态切换
5. 后端API支持GET/POST/PATCH/DELETE

✅ **效果：**
- 操作更便捷，体验更流畅
- 代码更规范，维护更简单
- 功能更完整，符合CRUD标准

✅ **用户只需：**
1. 在表格中查看所有配方
2. 点击「新增」或「编辑」按钮操作
3. 在Modal中填写/修改数据
4. 点击「保存」或「删除」完成操作
5. 页面自动刷新显示最新数据
