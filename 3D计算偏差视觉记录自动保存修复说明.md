# 3D计算偏差视觉记录自动保存修复说明

## 📋 问题描述

**问题现象**：
- 用户在3D料架定位工作台点击"🎯 计算偏差"按钮后
- 计算结果显示正常（偏差值、置信度等）
- 但在"视觉记录"页面中找不到任何记录
- 只有点击"💾 保存结果到数据库"按钮后才有记录

**用户需求**：
- 点击"计算偏差"按钮时就应该自动保存到视觉记录
- 方便后续追溯和查询每次计算的历史数据

## 🔍 问题原因

查看代码后发现，原有的实现逻辑是：

1. **`calculate_workbench` 方法**：仅预览计算，不写数据库
   - 只计算偏差值并返回结果
   - 不创建 `VisionTask` 记录
   - 不创建 `RackLocationResult` 记录

2. **`save_workbench_result` 方法**：保存到数据库
   - 重新计算并保存记录
   - 创建 `VisionTask` 和 `RackLocationResult`

这种设计将"计算"和"保存"分离，但不符合用户实际使用习惯。

## ✅ 修复方案

### 方案1: 计算时自动保存（已实施）

修改 `calculate_workbench` 方法，增加自动保存功能：

**文件**：`apps/vision/rack_location.py`

```python
def calculate_workbench(self, *, token, roi_config, recipe_id=None,
                        recipe_data=None, layer_no=1, roi_3d=None,
                        rack_side=RackSide.LEFT, save_record=True) -> dict:
    """工作台「计算偏差」：计算并可选择保存到视觉记录。
    
    Args:
        save_record: 是否保存到数据库（默认True，创建VisionTask记录）
    """
    # ... 计算逻辑 ...
    
    # 如果需要保存记录，创建VisionTask和RackLocationResult
    if save_record:
        position_no = 1  # 固定为1（工作台模式）
        db_recipe = RackLocationRecipe.objects.filter(pk=recipe_id).first() if recipe_id else None
        
        task = VisionTask.objects.create(
            task_type=VisionTaskType.RACK_LOCATING,
            status=ResultStatus.SUCCESS if output.locate_ok else ResultStatus.FAILED,
            started_at=timezone.now(),
            finished_at=timezone.now(),
            error_message=output.error_message,
        )
        
        RackLocationResult.objects.create(
            vision_task=task,
            recipe=db_recipe,
            side=output.rack_side or RackSide.BOTH,
            position_no=position_no,
            layer_no=layer_no,
            offset_x=_decimal(output.offset_x),
            offset_y=_decimal(output.offset_y),
            offset_z=_decimal(output.offset_z),
            offset_rz=_decimal(output.offset_rz),
            actual_x=_decimal(output.actual_x),
            actual_y=_decimal(output.actual_y),
            actual_z=_decimal(output.actual_z),
            confidence=_decimal(output.confidence),
            is_success=output.locate_ok,
            error_code=output.error_code,
            error_message=output.error_message,
            raw_data_path='',
            result_image_path=result_rel,
            result_data=output.result_data or {},
        )
    
    # ... 返回结果 ...
```

**文件**：`apps/vision/views.py`

```python
@require_POST
def api_rack_location_workbench_calculate(request):
    """工作台「计算偏差」：按绘制的 ROI 裁剪持久化点云，并保存到视觉记录。
    
    优化：
    1. 自动加载配方中保存的ROI坐标（如果有），避免每次都需要重新绘制
    2. 自动保存到视觉记录（VisionTask），方便追溯和查询
    """
    # ... 
    
    # 默认保存到视觉记录，除非明确指定不保存
    save_record = data.get('save_record', True)
    
    payload = RackLocationService().calculate_workbench(
        # ...
        save_record=save_record,
    )
    # ...
```

## 🎯 修复效果

### 修复前

```
用户操作流程：
1. 选择配方
2. 采集点云
3. 点击「🎯 计算偏差」 → ❌ 视觉记录中无记录
4. 点击「💾 保存结果到数据库」 → ✅ 视觉记录中有记录
```

### 修复后

```
用户操作流程：
1. 选择配方
2. 采集点云
3. 点击「🎯 计算偏差」 → ✅ 视觉记录中自动创建记录
4. 可选：点击「💾 保存结果到数据库」（如需再次保存）
```

## 📊 数据库记录内容

每次点击"计算偏差"按钮后，会自动创建以下记录：

### VisionTask 记录
- `task_type`: `RACK_LOCATING` (料架定位)
- `status`: `SUCCESS` 或 `FAILED`
- `started_at`: 计算开始时间
- `finished_at`: 计算结束时间
- `error_message`: 错误信息（如有）

### RackLocationResult 记录
- `vision_task`: 关联的视觉任务
- `recipe`: 使用的配方
- `side`: 料架侧边（LEFT/RIGHT/BOTH）
- `position_no`: 位置编号（固定为1）
- `layer_no`: 层号
- `offset_x/y/z`: X/Y/Z轴偏差值
- `offset_rz`: 旋转偏差
- `actual_x/y/z`: 实际坐标
- `confidence`: 置信度
- `is_success`: 是否成功
- `error_code`: 错误代码
- `error_message`: 错误描述
- `result_image_path`: 结果图片路径
- `result_data`: 完整结果数据（JSON）

## 🔧 向后兼容性

修改保持了向后兼容性：

1. **默认行为改变**：默认保存记录（`save_record=True`）
2. **可选关闭**：如需禁用，可在请求中传递 `save_record: false`
3. **不影响原有保存功能**：`save_workbench_result` 方法仍可正常使用

## 📝 测试验证

### 测试步骤

1. 打开3D料架定位工作台页面
2. 选择一个配方
3. 点击"📡 采集点云"按钮
4. 等待点云图像显示
5. 点击"🎯 计算偏差"按钮
6. 等待计算完成
7. **验证**：打开"视觉记录"页面，应该能看到刚才的计算记录

### 预期结果

- ✅ 点击"计算偏差"后，视觉记录中立即出现新记录
- ✅ 记录包含完整的计算结果（偏差值、置信度等）
- ✅ 记录的时间戳与计算时间一致
- ✅ 如果计算失败，记录状态为 `FAILED`，包含错误信息

## 🎉 优化总结

### 优点

1. **用户体验提升**：一键计算即保存，无需额外操作
2. **数据可追溯**：每次计算都有完整记录
3. **便于调试**：可以查看历史计算记录，分析问题
4. **保持灵活性**：可通过参数控制是否保存

### 影响范围

- ✅ 修改文件：2个（`rack_location.py`, `views.py`）
- ✅ 数据库变化：无（使用现有表结构）
- ✅ 前端变化：无（API接口保持兼容）
- ✅ 测试影响：最小（增加了可选参数）

## 📚 相关文档

- [3D料架定位工作台开发指南](./docs/3D料架定位离线数据包系统开发指南.md)
- [料架定位工作台功能增强说明](./料架定位工作台功能增强说明.md)
- [视觉模块API文档](./apps/vision/README.md)

---

**修复日期**：2026-07-03  
**修复人员**：Kiro AI Assistant  
**影响版本**：当前版本及后续版本
