# Base Dependencies

Firstly, you need to install RVC SDK from https://rvbust.com/download.html.

# Installing PyRVC

## Python Dependencies

pip3 install opencv-python

## For Windows User

pip3 install PyRVC

### FAQ

1.dll not found error


## For Linux User
You need to install the following software.

1. Installing `cmake` using `sudo apt install cmake`

Executing the following commands to install PyRVC

```shell
cd /opt/RVC/RVCSDK/PyRVC
pip3 install ./
```