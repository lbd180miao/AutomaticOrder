# 料架定位工作台功能增强说明 v2.0

## 📋 新增功能

### 1. 选中配方时自动加载并显示 ROI 框 ✨

**功能描述：**
- 当用户点击选择某个配方卡片时，系统会自动执行以下操作：
  1. **加载 3D ROI 参数**：从后端 API 获取该配方保存的 3D ROI 边界（X/Y/Z Min/Max）
  2. **自动填充输入框**：将 ROI 参数自动填充到页面上的输入框中
  3. **加载 2D ROI 框**：如果配方中保存了 2D target_roi，准备在点云画布上显示
  4. **智能应用**：
     - 如果**已经采集了点云**：立即在画布上绘制绿色 ROI 框
     - 如果**还未采集点云**：保存 ROI 信息，等待采集后自动应用

**用户体验：**
- ✅ 选中配方 → 配方的 ROI 参数自动填充
- ✅ 采集点云 → ROI 框自动显示在画布上（绿色虚线框）
- ✅ 无需手动绘制 → 可直接点击"计算偏差"
- ✅ 仍可手动调整 → 用户可以重新拖拽绘制新的 ROI

**实现位置：**
- `templates/vision/rack_locator_panel.html` - 新增 `loadRecipeRoi()` 和 `showRoiOnCanvas()` 函数
- `static/vision/js/rack_locator_workbench.js` - 新增 `window.rackLocatorSetRoi()` 接口和自动应用逻辑
- 在 `selectRecipeCard()` 函数中调用 `loadRecipeRoi()`

**技术细节：**
```javascript
// 选中配方时自动加载 ROI
function selectRecipeCard(el) {
  // ... 原有逻辑
  applyRecipeCard(el);
  // 新增：加载配方的 ROI 参数和 2D 框
  loadRecipeRoi(el.dataset.recipeId);
}

// 异步加载配方 ROI 数据
async function loadRecipeRoi(recipeId) {
  // 1. 获取 3D ROI 参数（X/Y/Z Min/Max）
  const res = await fetch(`api_vision_3d_rois?recipe_id=${recipeId}`);
  // 填充到输入框
  
  // 2. 获取完整配方信息（包含 2D ROI）
  const recipeRes = await fetch(`api_vision_3d_recipes/${recipeId}/`);
  const targetRoi = recipeData.data.recipe.roi_config.target_roi;
  
  // 3. 在画布上显示 ROI 框
  if (targetRoi) {
    showRoiOnCanvas(targetRoi);
  }
}

// 在画布上显示 ROI（即使还没有点云图像）
function showRoiOnCanvas(targetRoi) {
  if (已有点云图像) {
    window.rackLocatorSetRoi(targetRoi); // 立即绘制
  } else {
    // 保存到 state.pendingRoi，等待点云采集
  }
}
```

**后端 API 依赖：**
- ✅ `GET /api/vision/3d/rois/?recipe_id=<id>` - 获取 3D ROI 参数
- ✅ `GET /api/vision/3d/recipes/<recipe_id>/` - 获取完整配方信息（包含 `roi_config.target_roi`）

### 2. 计算偏差完成后自动选中下一个配方 🔄

**功能描述：**
- 当用户点击"计算偏差"按钮并完成计算后，系统会自动选中下一个配方
- 自动选择是循环的：如果当前是最后一个配方，会自动跳回第一个配方
- 自动滚动到新选中的配方卡片，确保其在可视区域内
- **同时触发功能1**：自动加载新配方的 ROI 参数和框

**实现位置：**
- `static/vision/js/rack_locator_workbench.js` - 新增 `selectNextRecipe()` 函数
- 在"计算偏差"按钮的点击处理中调用 `selectNextRecipe()`

**技术细节：**
```javascript
// 计算偏差完成后
$('btn-calculate').addEventListener('click', async () => {
  // ... 计算逻辑
  renderResult(data.result);
  
  // 新增：自动选中下一个配方
  selectNextRecipe();
});

// 选中下一个配方
function selectNextRecipe() {
  const cards = Array.from(document.querySelectorAll('.rl-recipe-card:not(.disabled-card)'));
  const currentCard = document.querySelector('.rl-recipe-card.selected');
  const currentIndex = cards.indexOf(currentCard);
  const nextIndex = (currentIndex + 1) % cards.length; // 循环选择
  const nextCard = cards[nextIndex];
  
  if (nextCard) {
    window.selectRecipeCard(nextCard); // 触发选中（会自动加载ROI）
    nextCard.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }
}
```

## 🎯 优化后的使用流程

### 完整工作流程：

1. **选择第一个配方** 
   - ✅ 自动加载该配方的 3D ROI 参数（X/Y/Z Min/Max）
   - ✅ 自动填充到输入框
   - ✅ 如果配方有保存 2D ROI，准备在点云上显示

2. **点击"采集点云"** 
   - ✅ 获取 3D 深度相机画面
   - ✅ **自动显示配方的 ROI 框**（绿色虚线框）
   - ✅ 状态提示："点云已采集，配方 ROI 已自动显示"

3. **点击"计算偏差"** 
   - ✅ 计算坐标偏差
   - ✅ 显示结果（OK/NG）
   - ✅ **自动切换到下一个配方**
   - ✅ 自动加载下一个配方的 ROI 参数

4. **重复步骤 2-3** 
   - 快速完成多个配方的批量定位测试
   - 无需手动选择配方或绘制 ROI

### 用户体验提升：

- ✅ **自动化 ROI 显示**：不需要每次手动绘制 ROI 框
- ✅ **减少重复操作**：不需要每次手动选择下一个配方
- ✅ **提高测试效率**：批量测试多个配方时，操作步骤减少 50%+
- ✅ **保持配置连贯**：ROI 参数自动加载，保持配方配置的一致性
- ✅ **视觉引导清晰**：自动滚动到选中的配方卡片，清晰指示当前进度
- ✅ **智能容错**：如果 ROI 加载失败，不影响手动绘制功能

## 🔧 修改的文件

### 1. **templates/vision/rack_locator_panel.html**
   - 新增 `loadRecipeRoi()` 函数 - 异步加载配方的 3D 和 2D ROI 数据
   - 新增 `showRoiOnCanvas()` 函数 - 在画布上显示 ROI 框或准备显示
   - 修改 `selectRecipeCard()` - 集成 ROI 自动加载
   - 将 `selectRecipeCard` 暴露到全局作用域（`window.selectRecipeCard`）

### 2. **static/vision/js/rack_locator_workbench.js**
   - 新增 `state.pendingRoi` 状态 - 保存待应用的 ROI
   - 新增 `window.rackLocatorSetRoi()` 全局接口 - 供外部调用设置 ROI
   - 新增 `selectNextRecipe()` 函数 - 自动选中下一个配方
   - 修改采集点云逻辑 - 在点云加载完成后自动应用 `pendingRoi`
   - 在计算偏差完成后调用 `selectNextRecipe()`

## 📊 功能流程图

```
用户操作流程：
┌─────────────────┐
│ 选择配方        │
└────────┬────────┘
         │ 触发 loadRecipeRoi()
         ↓
┌─────────────────┐     ┌──────────────────┐
│ 加载 3D ROI     │────>│ 填充输入框       │
│ (X/Y/Z Min/Max) │     └──────────────────┘
└────────┬────────┘
         │
         ↓
┌─────────────────┐     ┌──────────────────┐
│ 加载 2D ROI     │────>│ 保存到 pendingRoi│
│ (target_roi)    │     └──────────────────┘
└─────────────────┘
         │
         ↓
┌─────────────────┐
│ 采集点云        │
└────────┬────────┘
         │ image.onload
         ↓
┌─────────────────┐     ┌──────────────────┐
│ 自动应用 ROI    │────>│ 在画布上绘制框   │
│ rackLocatorSetRoi│     │ (绿色虚线)       │
└────────┬────────┘     └──────────────────┘
         │
         ↓
┌─────────────────┐
│ 计算偏差        │
└────────┬────────┘
         │ 完成后
         ↓
┌─────────────────┐     ┌──────────────────┐
│ 自动选中下一个  │────>│ 滚动到可视区域   │
│ selectNextRecipe│     │ 重复上述流程     │
└─────────────────┘     └──────────────────┘
```

## ⚠️ 注意事项

1. **API 要求**：
   - 后端需要提供 `GET /api/vision/3d/rois/?recipe_id=<id>` 接口
   - 后端需要提供 `GET /api/vision/3d/recipes/<recipe_id>/` 接口
   - ✅ 这两个接口已在当前系统中实现

2. **ROI 数据结构**：
   - 3D ROI: `{x_min, x_max, y_min, y_max, z_min, z_max}`
   - 2D ROI: `{x, y, w, h, feature_type}`
   - 需要确保配方中保存了 `roi_config.target_roi`

3. **配方顺序**：
   - 自动选择下一个配方的顺序按照页面上配方卡片的 DOM 顺序
   - 如需自定义顺序，可在配方管理中调整配方排序

4. **循环选择**：
   - 选到最后一个配方后会自动回到第一个配方
   - 适合批量循环测试场景

5. **失败处理**：
   - 如果 ROI 加载失败，会在控制台输出 warning，不影响正常操作
   - 用户仍可手动绘制 ROI 或手动输入参数

## 🚀 后续优化建议

1. **配方 ROI 可视化预览**
   - 在配方卡片上显示缩略图或 ROI 大小
   - 让用户更直观地了解每个配方的 ROI 区域

2. **批量测试模式**
   - 添加"批量测试所有配方"按钮
   - 自动遍历所有配方，依次采集、计算、保存

3. **用户偏好设置**
   - 允许用户配置是否启用"自动选中下一个配方"功能
   - 允许用户配置是否自动应用配方 ROI

4. **历史记录增强**
   - 记录用户的配方遍历历史
   - 提供"返回上一个配方"功能

5. **ROI 模板管理**
   - 允许用户保存多个 ROI 模板
   - 快速切换不同的 ROI 配置

---

**更新时间：** 2026-07-02  
**版本：** v2.0  
**修改人员：** Kiro AI Assistant
