# 海康威视2D相机配置说明

## 问题诊断结果

经过测试，发现：
- ✅ 相机硬件连接正常（找到 1 个相机）
- ✅ SDK 和驱动正常
- ✅ Python 绑定库安装正常
- ❌ **相机无法采集图像（错误码: -2147483646）**

## 问题原因

海康威视工业相机默认处于 **硬触发模式**，需要外部触发信号才能采集图像。
而我们的应用需要相机处于 **连续采集模式** 或 **软触发模式**。

## 解决方案

### 方法 1：使用 MVS 客户端配置相机（推荐）

1. **打开 MVS 软件**
   - 海康威视 MVS (Machine Vision Studio)
   - 通常安装在：`C:\Program Files (x86)\MVS\`

2. **连接相机**
   - 在设备列表中找到相机
   - 双击打开相机

3. **设置触发模式为"连续采集"**
   - 在左侧参数树中找到：**"Acquisition Control"（采集控制）**
   - 找到 **"Trigger Mode"（触发模式）**
   - 设置为 **"Off"（关闭）** 或 **"Continuous"（连续）**
   - 这样相机就会持续采集图像

4. **设置采集模式**
   - 找到 **"Acquisition Mode"（采集模式）**
   - 设置为 **"Continuous"（连续采集）**

5. **调整曝光参数（可选）**
   - **Exposure Time（曝光时间）**：建议 5000-10000 微秒（5-10毫秒）
   - **Gain（增益）**：建议 0-15 dB
   - 或者启用 **Auto Exposure（自动曝光）** 和 **Auto Gain（自动增益）**

6. **保存配置**
   - 点击 **"User Set Control"（用户参数组）**
   - 选择 **"Save"（保存）**
   - 这样配置会永久保存到相机中

7. **测试拍照**
   - 在 MVS 中点击 **"Start Grabbing"（开始采集）** 按钮
   - 确认能看到实时图像
   - 如果能看到，说明配置成功

### 方法 2：代码中设置触发模式

如果不方便使用 MVS 软件，可以修改 `chg_hik` 库的源码添加触发模式设置。

需要在 `Hik_camera/src/lib.rs` 中添加设置触发模式的代码。

### 方法 3：使用模拟模式（临时测试）

如果只是想测试 Web 界面功能，可以暂时使用模拟相机。

修改 `AutomaticOrder/settings.py`：
```python
AUTOMATIC_ORDER = {
    'USE_SIMULATED_DEVICES': True,  # 改为 True
    # ...
}
```

但这只能用于测试UI，不会获取真实图像。

## 配置后的验证步骤

完成上述配置后，运行测试脚本验证：

```bash
# 进入项目目录
cd D:\workspace2\AutomaticOrder

# 激活虚拟环境
.venv\Scripts\activate

# 运行测试
python test_camera_traditional_api.py
```

如果看到：
```
✓ 拍照成功！
✓ 找到 1 张图片:
  - xxx.bmp: xxx 字节 (x.xx MB)
```

说明相机配置成功！

然后就可以在 Web 界面使用"刷新预览"功能了。

## 常见问题

### Q1: 找不到 MVS 软件
**A:** 需要从海康威视官网下载安装：
https://www.hikrobotics.com/cn/machinevision/service/download

选择 **MVS (Machine Vision Studio)** 下载。

### Q2: MVS 中也看不到图像
**A:** 可能的原因：
1. 镜头盖没有打开
2. 光线不足
3. 曝光时间过短
4. 相机本身有硬件问题

尝试：
- 增加曝光时间（Exposure Time 设置为 20000-50000 微秒）
- 增加增益（Gain 设置为 20 dB）
- 对着光源测试

### Q3: 配置保存后重启失效
**A:** 确保：
1. 保存到了 **User Set 1**
2. 设置 **Default User Set** 为 **User Set 1**
3. 点击了 **Save** 按钮

### Q4: 错误码含义
- **-2147483646**: 采集失败，通常是触发模式问题或相机参数问题
- **-2147483644**: 设备未找到
- **-2147483643**: 设备打开失败
- **-2147483642**: 参数设置失败

## 技术支持

如果以上方法都无法解决，建议：
1. 查看海康威视官方文档
2. 联系海康威视技术支持
3. 检查相机固件版本是否需要更新
