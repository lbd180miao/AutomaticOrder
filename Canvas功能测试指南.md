与# Canvas 绘制 ROI 功能测试指南

## 🚀 快速开始

### 1️⃣ 重启 Django 服务器

```bash
# 停止当前服务器（如果正在运行）
# Ctrl+C

# 启动服务器
python manage.py runserver
```

### 2️⃣ 清除浏览器缓存

**方法1：强制刷新（推荐）**
- 按 `Ctrl + F5` （Windows）
- 或 `Ctrl + Shift + R` （Windows/Linux）
- 或 `Cmd + Shift + R` （Mac）

**方法2：清除缓存**
- 按 `Ctrl + Shift + Delete`
- 选择"缓存的图片和文件"
- 点击"清除数据"

**方法3：使用无痕模式**
- `Ctrl + Shift + N` (Chrome/Edge)
- `Ctrl + Shift + P` (Firefox)

### 3️⃣ 打开测试页面

访问：`http://localhost:8000/vision/rack-location/recipes/new/`

或者从配方列表页点击"新增配方"或编辑现有配方

---

## 📋 功能测试清单

### ✅ 基础显示测试

打开页面后，检查：

- [ ] 左侧深度图区域显示正常
- [ ] 中间有一个绿色提示框："🎯 拖拽鼠标绘制 ROI"
- [ ] 右侧表单区域正常显示
- [ ] 有三个按钮：
  - 📡 采集标准图
  - 🔄 重画 ROI
  - 🎯 预计算标准坐标

### ✅ Canvas 初始化测试

**按 F12 打开浏览器开发者工具，查看 Console**

应该看到：
```
[Canvas ROI] 模块加载
[Canvas ROI] Canvas 初始化完成
```

如果看到错误，记录错误信息。

### ✅ 采集图像测试

1. 点击"📡 采集标准图"按钮
2. 等待图像加载

**预期结果：**
- [ ] 图像加载成功
- [ ] 绿色提示框自动消失
- [ ] 鼠标移到图像上变成十字光标（✛）
- [ ] Console 显示：
  ```
  [Canvas ROI] 图像已加载: 640x480
  ```

### ✅ ROI 绘制测试

1. 在图像上按住鼠标左键
2. 拖拽绘制一个矩形
3. 松开鼠标

**绘制过程中：**
- [ ] 实时显示半透明绿色矩形
- [ ] 鼠标旁边显示尺寸提示（例如："150 × 120 px"）
- [ ] 页面底部状态显示："正在绘制 ROI..."

**绘制完成后：**
- [ ] 矩形变为稍深的绿色
- [ ] 四个角有十字标记
- [ ] 中心有一个绿色圆点
- [ ] 页面底部状态显示："🔄 正在转换坐标..."

### ✅ 坐标转换测试

绘制完成后，自动进行坐标转换。

**Console 应该显示：**
```
[Canvas ROI] 像素 ROI: [100, 150, 250, 270]
[Canvas ROI] 物理尺寸: 202.5mm × 162.0mm
[Canvas ROI] 相机坐标 ROI: {x_min: -101.25, x_max: 101.25, ...}
[Canvas ROI] 机器人坐标 ROI: {x_min: 850.5, x_max: 1050.2, ...}
```

**表单应该自动填充：**

| 字段区域 | 字段 | 是否填充 |
|---------|------|---------|
| 相机坐标 ROI | X Min | ✅ |
| 相机坐标 ROI | X Max | ✅ |
| 相机坐标 ROI | Y Min | ✅ |
| 相机坐标 ROI | Y Max | ✅ |
| 相机坐标 ROI | Z Min | ✅ |
| 相机坐标 ROI | Z Max | ✅ |
| 3D ROI 配方坐标 | X Min | ✅ |
| 3D ROI 配方坐标 | X Max | ✅ |
| 3D ROI 配方坐标 | Y Min | ✅ |
| 3D ROI 配方坐标 | Y Max | ✅ |
| 3D ROI 配方坐标 | Z Min | ✅ |
| 3D ROI 配方坐标 | Z Max | ✅ |

**状态提示：**
- [ ] 页面底部显示："✅ ROI 坐标已自动转换并填充"（绿色）
- [ ] "相机 → 机器人坐标" 按钮下方显示："✅ 已通过 Canvas 绘制自动转换"（绿色）

### ✅ 重画 ROI 测试

1. 点击"🔄 重画 ROI"按钮

**预期结果：**
- [ ] Canvas 上的 ROI 矩形消失
- [ ] 所有表单字段被清空（相机坐标 + 机器人坐标）
- [ ] 页面底部状态显示："ROI 已清除，可重新绘制"
- [ ] 可以重新绘制新的 ROI

### ✅ 边界情况测试

#### 测试1：绘制太小的 ROI

1. 绘制一个非常小的矩形（小于 10×10 像素）
2. 松开鼠标

**预期结果：**
- [ ] ROI 不会保留
- [ ] Canvas 被清空
- [ ] 页面底部显示："ROI 太小，请重新绘制（至少 10×10 像素）"（红色）

#### 测试2：绘制超出边界的 ROI

1. 从图像内部开始绘制
2. 拖拽到图像外部
3. 松开鼠标

**预期结果：**
- [ ] ROI 不会保留
- [ ] Canvas 被清空
- [ ] 页面底部显示："ROI 超出图像边界，请重新绘制"（红色）

### ✅ 集成测试

#### 测试：完整流程

1. 填写"配方名称"：`测试配方Canvas`
2. 填写"适用层号"：`1`
3. 点击"📡 采集标准图"
4. 在图像上绘制 ROI
5. 等待坐标自动填充
6. 点击"🎯 预计算标准坐标"
7. 查看预计算结果是否正常
8. 点击"保存配方"

**预期结果：**
- [ ] 配方保存成功
- [ ] 跳转到配方列表页
- [ ] 新配方出现在列表中

#### 测试：编辑现有配方

1. 从列表页点击"编辑"某个配方
2. 页面加载后，如果有图像，Canvas 应该显示
3. 点击"🔄 重画 ROI"
4. 重新绘制 ROI
5. 保存配方

**预期结果：**
- [ ] ROI 坐标更新成功

---

## 🐛 常见问题排查

### 问题1：Canvas 没有显示

**可能原因：**
- 图像未加载
- JavaScript 文件未加载

**排查步骤：**
1. 打开 F12 开发者工具 → Network 标签页
2. 刷新页面
3. 检查是否加载了 `rack_location_canvas_roi.js?v=1`
4. 检查是否有 404 错误

### 问题2：绘制没有反应

**可能原因：**
- 图像未加载完成
- JavaScript 报错

**排查步骤：**
1. 打开 F12 → Console 标签页
2. 查看是否有红色错误信息
3. 检查是否显示 `[Canvas ROI] 图像已加载`

### 问题3：坐标转换失败

**可能原因：**
- API 调用失败
- 网络问题
- 后端错误

**排查步骤：**
1. 打开 F12 → Network 标签页
2. 绘制 ROI 后，查找 `transform-roi` 请求
3. 查看响应状态码和响应内容
4. 检查 Console 是否有错误信息

### 问题4：表单没有自动填充

**可能原因：**
- 字段 ID 不匹配
- readonly 属性问题

**排查步骤：**
1. 打开 F12 → Elements 标签页
2. 检查输入框的 ID：
   - `camera-roi-x-min`
   - `camera-roi-x-max`
   - `roi-x-min`
   - `roi-x-max`
   - 等等
3. 查看 Console 是否有 `[Canvas ROI] 相机坐标 ROI:` 日志
4. 查看 Console 是否有 `[Canvas ROI] 机器人坐标 ROI:` 日志

---

## 📊 性能检查

### 检查点：

1. **初始加载时间**
   - 打开 F12 → Network 标签页
   - 刷新页面
   - 查看 `rack_location_canvas_roi.js` 的加载时间
   - 应该 < 100ms

2. **绘制响应速度**
   - 拖拽时应该流畅，无卡顿
   - 如果有卡顿，查看 Console Performance

3. **坐标转换时间**
   - Console 应该显示坐标转换耗时
   - 应该 < 500ms

---

## ✅ 验收标准

### 必须通过的测试：

- [x] Canvas 正确显示和初始化
- [x] 可以正常绘制 ROI
- [x] 实时显示尺寸提示
- [x] 坐标自动转换
- [x] 表单自动填充
- [x] 重画功能正常
- [x] 边界检查生效
- [x] 错误提示正确显示
- [x] 可以成功保存配方

### 可选但推荐的测试：

- [ ] 响应式布局（缩放浏览器窗口）
- [ ] 不同浏览器（Chrome, Firefox, Edge）
- [ ] 不同图像尺寸（640x480, 800x600 等）
- [ ] 多次重画和保存

---

## 📸 测试截图建议

建议截图以下画面：

1. **初始状态**：显示提示框
2. **绘制中**：半透明矩形 + 尺寸提示
3. **绘制完成**：绿色矩形 + 角点标记
4. **表单填充**：所有字段已填充
5. **Console 日志**：显示坐标转换日志

---

## 🎉 测试完成

如果所有测试都通过，恭喜！Canvas 绘制 ROI 功能已经成功集成！

如果遇到问题，请：
1. 记录错误信息（Console 中的红色错误）
2. 记录复现步骤
3. 提供截图
4. 检查 Network 标签页的请求响应

---

## 📞 需要帮助？

如果测试过程中遇到问题，请提供：

1. **浏览器和版本**（例如：Chrome 120）
2. **Console 错误信息**（完整的红色错误）
3. **Network 请求详情**（特别是 transform-roi 请求）
4. **复现步骤**
5. **截图**
