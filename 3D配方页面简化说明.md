# 3D配方页面简化说明

## 修改概述

已将3D配方管理页面和编辑页面简化，**只保留核心参数**，删除了冗余字段。

---

## 简化后保留的参数

### ✅ 保留的核心参数（4类）

1. **配方名称** - `recipe_name`
   - 必填
   - 建议格式：`3D-L{层号}`，例如 `3D-L1`、`3D-L2`

2. **适用层号** - `layer_no`
   - 必填
   - 取值范围：1~10
   - 说明：第几层

3. **ROI参数（机器人基坐标系 mm）** - 6个字段
   - `roi_x_min` - X最小值
   - `roi_x_max` - X最大值
   - `roi_y_min` - Y最小值
   - `roi_y_max` - Y最大值
   - `roi_z_min` - Z最小值
   - `roi_z_max` - Z最大值

4. **标准坐标（mm）** - 4个字段
   - `standard_x` - 标准X坐标
   - `standard_y` - 标准Y坐标
   - `standard_z` - 标准Z坐标
   - `standard_rz` - 标准Rz旋转角

### ❌ 已隐藏/固定的参数

以下参数仍存在于后端，但在前端页面中已隐藏并设置为固定值：

| 参数 | 固定值 | 说明 |
|------|--------|------|
| `rack_type` | `""` | 料架类型（空） |
| `rack_side` | `"BOTH"` | 料架侧别（左右通用） |
| `position_no` | `1` | 工位号（固定为1） |
| `layer_count` | `3` | 层数（固定为3） |
| `camera_device` | `""` | 3D相机设备（空） |
| `capture_pose_name` | `""` | 机器人拍照位名称（空） |

---

## 修改的文件

### 1. `templates/vision/rack_location_recipe_form.html`

**配方编辑表单页面**

#### 修改内容：

**① 隐藏了以下输入框：**
```html
<!-- 已删除的字段 -->
<label>料架类型
<label>料架侧别
<label>配方位置 POS
<label>层数
<label>3D 相机设备
<label>机器人拍照位名称
```

**② 替换为隐藏字段（固定值）：**
```html
<input type="hidden" name="rack_type" value="">
<input type="hidden" name="rack_side" value="BOTH">
<input type="hidden" name="position_no" value="1">
<input type="hidden" name="layer_count" value="3">
<input type="hidden" name="camera_device" value="">
<input type="hidden" name="capture_pose_name" value="">
```

**③ 保留的显示字段：**
```html
<!-- 配方名称 -->
<label class="rack-location-wide">配方名称 <span style="color:red;">*</span>
  <input name="recipe_name" required>
</label>

<!-- 适用层号 -->
<label>适用层号 <span style="color:red;">*</span>
  <input name="layer_no" type="number" min="1" max="10" required>
  <small>第几层（1/2/3...）</small>
</label>

<!-- ROI参数 -->
<fieldset class="rack-location-fieldset">
  <legend>ROI 参数（机器人基坐标系 mm）</legend>
  <label>X Min <input name="roi_x_min" type="number" step="0.1"></label>
  <label>X Max <input name="roi_x_max" type="number" step="0.1"></label>
  <label>Y Min <input name="roi_y_min" type="number" step="0.1"></label>
  <label>Y Max <input name="roi_y_max" type="number" step="0.1"></label>
  <label>Z Min <input name="roi_z_min" type="number" step="0.1"></label>
  <label>Z Max <input name="roi_z_max" type="number" step="0.1"></label>
</fieldset>

<!-- 标准坐标 -->
<fieldset class="rack-location-fieldset">
  <legend>标准坐标（mm）</legend>
  <label>X <input name="standard_x" type="number" step="0.001"></label>
  <label>Y <input name="standard_y" type="number" step="0.001"></label>
  <label>Z <input name="standard_z" type="number" step="0.001"></label>
  <label>Rz <input name="standard_rz" type="number" step="0.001"></label>
</fieldset>
```

**④ 简化后的布局：**
- 字段更少，页面更清爽
- ROI和标准坐标分区更清晰
- 添加了占位符提示（placeholder）
- 添加了字段说明（小灰字）

---

### 2. `templates/vision/rack_location_recipes.html`

**配方列表页面**

#### 修改内容：

**① 简化了配方卡片显示：**

**旧版（显示过多信息）：**
```html
<h3>
  配方名称
  <span>POS 1 · 层1</span>
</h3>
<div class="recipe-card-meta">
  <span>标准X 100</span>
  <span>标准Y 200</span>
  <span>标准Z 800</span>
  <span>Rz 0</span>
</div>
<div class="recipe-card-meta">
  <span>最大偏差 X≤10 Y≤10 Z≤10</span>
  <span>置信度≥0.7</span>
  <span>拍照位 POSE-1</span>
</div>
```

**新版（只显示核心）：**
```html
<h3>
  配方名称
  <span>第 1 层</span>
</h3>
<div class="recipe-card-meta">
  <span>标准坐标 X:100 Y:200 Z:800</span>
</div>
```

**② 简化了新增配方Modal：**

**旧版（3个输入框）：**
```html
配方名称：3D-POS-1-L1
POS 位置号：1
层号：1
拍照位名称：POSE-POS-1-L1
```

**新版（2个输入框）：**
```html
配方名称：3D-L1
适用层号：1
💡 提示：ROI参数和标准坐标请在下一步配置页面中输入
```

**③ 更新了JavaScript逻辑：**
```javascript
// 旧版：需要4个字段
const params = new URLSearchParams({
  recipe_name: name,
  position_no: pos,
  layer_no: layer,
  capture_pose_name: pose,
});

// 新版：只需2个字段
const params = new URLSearchParams({
  recipe_name: name,
  layer_no: layer,
});
```

---

## 页面效果对比

### 配方列表页面

**旧版：**
```
┌─────────────────────────────────────────────────┐
│ 3D-POS-1-L1         [POS 1 · 层1] [已禁用]      │
│ 标准X 100  标准Y 200  标准Z 800  Rz 0          │
│ 最大偏差 X≤10 Y≤10 Z≤10  置信度≥0.7           │
│ 拍照位 POSE-POS-1-L1                          │
│ [✏️ 编辑] [✔ 已启用]                           │
└─────────────────────────────────────────────────┘
```

**新版：**
```
┌─────────────────────────────────────────────────┐
│ 3D-L1               [第 1 层]                  │
│ 标准坐标 X:100 Y:200 Z:800                     │
│ [✏️ 编辑] [✔ 已启用]                           │
└─────────────────────────────────────────────────┘
```

### 配方编辑页面

**旧版（14个输入框）：**
```
配方名称：_______
料架类型：_______
料架侧别：[下拉选择]
适用层号：_______
配方位置POS：_______
层数：_______
3D相机设备：[下拉选择]
机器人拍照位名称：_______

ROI参数（6个）
标准坐标（4个）
```

**新版（12个输入框，显示10个）：**
```
配方名称：_______  [必填]
适用层号：_______  [必填] (第几层)

ROI参数（6个）
  X Min: _______ (例：-100)
  X Max: _______ (例：100)
  Y Min: _______ (例：-100)
  Y Max: _______ (例：100)
  Z Min: _______ (例：700)
  Z Max: _______ (例：900)

标准坐标（4个）
  X: _______ (例：100.000)
  Y: _______ (例：200.000)
  Z: _______ (例：800.000)
  Rz: _______ (例：0.000)

预计算结果（只读，显示实际测量值）
```

---

## 新增配方流程

### 旧流程（4步）：
1. 点击「新增3D配方」
2. 填写配方名称、POS位置号、层号、拍照位名称
3. 点击「前往配置」
4. 填写ROI参数和标准坐标

### 新流程（3步）：
1. 点击「新增3D配方」
2. 填写配方名称、适用层号
3. 点击「前往配置」→ 填写ROI参数和标准坐标

---

## 用户操作指南

### 创建第1层配方

1. 点击「➕ 新增 3D 配方」
2. 输入：
   - 配方名称：`3D-L1`
   - 适用层号：`1`
3. 点击「前往配置」
4. 在配置页面输入：
   - **ROI参数**（机器人基坐标系，单位mm）
     ```
     X Min: -100    X Max: 100
     Y Min: -100    Y Max: 100
     Z Min: 700     Z Max: 900
     ```
   - **标准坐标**（单位mm）
     ```
     X: 100.000
     Y: 200.000
     Z: 800.000
     Rz: 0.000
     ```
5. 点击「保存配方」

### 创建第2层配方

重复上述步骤，但：
- 配方名称：`3D-L2`
- 适用层号：`2`
- 标准Z坐标：`950.000`（假设层间距150mm）

### 创建第3层配方

- 配方名称：`3D-L3`
- 适用层号：`3`
- 标准Z坐标：`1100.000`

---

## 后端兼容性

### ✅ 完全兼容
- 后端模型字段未改变
- API接口未改变
- 数据库结构未改变

### 🔧 自动填充
当前端提交表单时，隐藏字段会自动带上固定值：
```python
# 前端提交的数据（包含隐藏字段）
{
  "recipe_name": "3D-L1",
  "layer_no": 1,
  "rack_type": "",           # 隐藏字段
  "rack_side": "BOTH",       # 隐藏字段
  "position_no": 1,          # 隐藏字段
  "layer_count": 3,          # 隐藏字段
  "camera_device": "",       # 隐藏字段
  "capture_pose_name": "",   # 隐藏字段
  "roi_x_min": -100,
  "roi_x_max": 100,
  ...
}
```

后端接收时正常处理，不需要修改任何代码。

---

## 优点总结

### ✅ 用户体验改善
1. **信息密度降低** - 页面更清爽，不会眼花缭乱
2. **核心参数突出** - 用户只需关注真正需要的4类参数
3. **操作步骤减少** - 新增配方从4个字段减少到2个
4. **字段说明清晰** - 添加了占位符和小灰字提示

### ✅ 维护性提升
1. **代码整洁** - 删除了不必要的UI代码
2. **逻辑简化** - JavaScript代码更少
3. **易于理解** - 新手也能快速上手

### ✅ 兼容性保持
1. **数据库不变** - 不需要迁移数据
2. **API不变** - 后端代码无需修改
3. **向后兼容** - 现有数据可以正常显示和编辑

---

## 测试建议

### 1. 新增配方测试
```
1. 点击「新增3D配方」
2. 输入配方名称：3D-L1
3. 输入层号：1
4. 点击「前往配置」
5. 验证：配方名称和层号已自动填入
6. 输入ROI参数和标准坐标
7. 点击「保存配方」
8. 验证：返回列表页，新配方显示正常
```

### 2. 编辑配方测试
```
1. 点击某个配方的「编辑」按钮
2. 验证：只显示4类核心参数
3. 修改适用层号：2 → 3
4. 修改标准Z坐标：800 → 1100
5. 点击「保存配方」
6. 验证：配方更新成功，隐藏字段保持不变
```

### 3. 列表显示测试
```
1. 创建多个配方（L1、L2、L3）
2. 验证：每个卡片只显示配方名称、层号、标准坐标
3. 验证：不再显示POS、最大偏差、置信度等信息
4. 验证：启用/禁用按钮功能正常
```

---

## 注意事项

### ⚠️ 隐藏字段的固定值

如果以后需要修改固定值（如 `position_no` 改为其他值），需要修改：

**文件：** `templates/vision/rack_location_recipe_form.html`

**位置：** 第 36-41 行

```html
<input type="hidden" name="position_no" value="1">  <!-- 在这里改 -->
```

### ⚠️ 字段校验

后端代码中可能还有字段校验逻辑，如果需要完全移除某些字段的约束，需要修改：

**文件：** `apps/vision/views.py`

**方法：** `_save_rack_location_recipe_from_request()`

### ⚠️ 数据库唯一性约束

模型中仍然保留了 `(position_no, layer_no)` 的唯一性约束：

```python
models.UniqueConstraint(
    fields=['position_no', 'layer_no'],
    condition=models.Q(enabled=True),
    name='unique_enabled_recipe_per_position_layer',
)
```

由于 `position_no` 固定为1，实际效果是：**每个层号只能有一个启用的配方**。

---

## 总结

✅ **已完成：**
1. 配方编辑页面简化为4类核心参数
2. 配方列表页面简化显示内容
3. 新增配方Modal简化为2个必填字段
4. 隐藏不必要的字段，自动填充固定值
5. 保持后端完全兼容

✅ **效果：**
- 页面更简洁清爽
- 用户操作更简单
- 核心参数更突出
- 维护成本更低

✅ **用户只需关注：**
1. 配方名称（如：3D-L1）
2. 适用层号（如：1）
3. ROI参数（6个坐标值）
4. 标准坐标（X/Y/Z/Rz）

其他所有参数都已隐藏或自动处理！
