# 料架定位工作台 - ROI 自动显示功能测试指南

## 🧪 测试目标

验证以下两个新功能：
1. ✅ 选中配方时自动加载并显示 ROI 框
2. ✅ 计算偏差完成后自动选中下一个配方

## 📋 测试前准备

### 1. 确保至少有 2-3 个已启用的配方
   - 访问：`配方管理` 页面
   - 确认至少有 2 个配方状态为"已启用"
   - 建议配方具有不同的 POS 和层号

### 2. 确保配方已保存 ROI 数据
   - 配方需要有 `roi_config.target_roi` 数据
   - 配方需要有 3D ROI 边界数据（x_min, x_max, y_min, y_max, z_min, z_max）

## 🔬 测试步骤

### 测试 1：选中配方时自动加载 ROI（未采集点云状态）

**步骤：**
1. 打开浏览器控制台（F12）→ Console 标签
2. 访问 `3D 料架定位工作台` 页面
3. 点击选择第二个配方卡片

**预期结果：**
- ✅ 配方卡片高亮显示（蓝色边框）
- ✅ 下方信息条更新显示配方信息
- ✅ 3D ROI 输入框（X/Y/Z Min/Max）自动填充数值
- ✅ 控制台输出：`已加载配方 3D ROI 参数`
- ✅ 控制台输出：`找到配方的 2D ROI: {x:..., y:..., w:..., h:...}`
- ✅ 控制台输出：`配方 ROI 已就绪，等待点云采集`
- ✅ 页面底部显示：`配方 ROI: x=... y=... w=... h=...`
- ✅ 状态提示：`已加载配方 ROI (宽×高)，采集点云后将自动显示框选区域。`

---

### 测试 2：采集点云后自动显示 ROI 框

**步骤：**
1. 保持在上一测试状态（已选中配方）
2. 点击 `📡 采集点云` 按钮
3. 等待点云图像加载完成

**预期结果：**
- ✅ 点云深度图显示在画布上
- ✅ **绿色虚线 ROI 框自动显示**在点云图像上
- ✅ 控制台输出：`ROI 已应用到画布`
- ✅ 画布左下角显示：`ROI  x=... y=... w=... h=...`
- ✅ 状态提示：`点云已采集，配方 ROI 已自动显示。`
- ✅ "计算偏差" 按钮变为可点击状态（蓝色）

---

### 测试 3：采集点云前选择配方，采集后 ROI 自动应用

**步骤：**
1. 刷新页面
2. 选择第一个配方卡片（此时还没有点云图像）
3. 点击 `📡 采集点云` 按钮
4. 观察点云加载完成后的效果

**预期结果：**
- ✅ 点云图像加载完成后，**ROI 框自动显示**
- ✅ 无需手动绘制 ROI
- ✅ 可以直接点击"计算偏差"

---

### 测试 4：计算完成后自动选中下一个配方

**步骤：**
1. 保持在上一测试状态（已采集点云，ROI 已显示）
2. 点击 `🎯 计算偏差` 按钮
3. 等待计算完成

**预期结果：**
- ✅ 右侧显示计算结果（OK 或 NG）
- ✅ **自动选中下一个配方卡片**（蓝色高亮）
- ✅ 页面自动滚动到新选中的配方
- ✅ 下方信息条更新为新配方的信息
- ✅ 3D ROI 输入框更新为新配方的参数
- ✅ 控制台输出新配方的 ROI 加载日志

---

### 测试 5：循环选择（最后一个配方 → 第一个配方）

**步骤：**
1. 多次执行：采集点云 → 计算偏差
2. 直到选中最后一个配方
3. 再次执行：采集点云 → 计算偏差

**预期结果：**
- ✅ 计算完成后，**自动跳回第一个配方**
- ✅ 形成循环，可以持续批量测试

---

### 测试 6：手动绘制 ROI 覆盖自动加载的 ROI

**步骤：**
1. 选择一个配方，采集点云（ROI 自动显示）
2. 点击 `重画 ROI` 按钮
3. 手动在画布上拖拽绘制新的 ROI 框

**预期结果：**
- ✅ 旧的 ROI 框消失
- ✅ 可以正常手动绘制新的 ROI 框
- ✅ 手动绘制的 ROI 生效，自动加载的 ROI 被覆盖

---

## 🐛 常见问题排查

### 问题 1：选中配方后，ROI 没有自动显示

**可能原因：**
- 配方中没有保存 `roi_config.target_roi` 数据
- 后端 API 返回的数据结构不正确

**排查步骤：**
1. 打开浏览器控制台
2. 查看 Network 标签中的 API 请求
3. 检查 `api/vision/3d/recipes/<id>/` 的响应
4. 确认响应中包含 `roi_config.target_roi` 字段

---

### 问题 2：采集点云后，ROI 框没有自动显示

**可能原因：**
- `image.onload` 事件没有触发
- `state.pendingRoi` 没有正确保存

**排查步骤：**
1. 在控制台检查 `window.rackLocatorSetRoi` 是否存在
2. 手动调用：`window.rackLocatorSetRoi({x:100, y:100, w:200, h:150})`
3. 观察是否能正常绘制 ROI 框

---

### 问题 3：计算完成后，没有自动选中下一个配方

**可能原因：**
- `window.selectRecipeCard` 函数不存在
- 配方卡片没有正确的 class 名称

**排查步骤：**
1. 在控制台检查：`typeof window.selectRecipeCard`（应该是 'function'）
2. 检查页面上是否有 `.rl-recipe-card` 元素
3. 手动测试：`window.selectRecipeCard(document.querySelectorAll('.rl-recipe-card')[1])`

---

## ✅ 测试检查清单

完成以下测试项，确认功能正常：

- [ ] 选中配方时，3D ROI 输入框自动填充
- [ ] 选中配方时，控制台输出 ROI 加载日志
- [ ] 选中配方后采集点云，ROI 框自动显示在画布上
- [ ] 先采集点云再选中配方，不会自动显示 ROI（需重新采集）
- [ ] 计算偏差完成后，自动选中下一个配方
- [ ] 最后一个配方计算完成后，自动回到第一个配方
- [ ] 可以手动重画 ROI，覆盖自动加载的 ROI
- [ ] 多个配方可以连续批量测试

---

## 📊 性能测试

### 批量测试 10 个配方的时间对比

**优化前（手动操作）：**
```
每个配方：选择配方(3秒) + 采集点云(5秒) + 手动绘制ROI(8秒) + 计算(3秒) = 19秒
10个配方：19秒 × 10 = 190秒 ≈ 3分10秒
```

**优化后（自动操作）：**
```
每个配方：采集点云(5秒) + 计算(3秒) + 自动切换(0.5秒) = 8.5秒
10个配方：8.5秒 × 10 = 85秒 ≈ 1分25秒
```

**效率提升：55% ⚡**

---

## 📝 测试报告模板

```
测试日期：2026-07-02
测试人员：[您的名字]
浏览器：Chrome / Firefox / Edge [版本号]

测试结果：
✅ 测试 1：选中配方自动加载 ROI（未采集点云） - 通过
✅ 测试 2：采集点云后自动显示 ROI 框 - 通过
✅ 测试 3：采集点云前选择配方，ROI 自动应用 - 通过
✅ 测试 4：计算完成后自动选中下一个配方 - 通过
✅ 测试 5：循环选择（最后 → 第一个） - 通过
✅ 测试 6：手动绘制覆盖自动 ROI - 通过

发现的问题：
[列出任何问题或异常行为]

建议改进：
[列出任何改进建议]
```

---

**文档版本：** v1.0  
**最后更新：** 2026-07-02
