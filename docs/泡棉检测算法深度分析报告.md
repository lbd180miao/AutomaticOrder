# 泡棉检测算法深度分析报告

## 文档信息
- **分析日期**：2026年7月13日
- **代码版本**：当前生产版本
- **分析范围**：`apps/vision/algorithms/foam_inspector.py` 完整实现

---

## 核心发现：算法已经实现像素级分割！

### 🎯 重要结论

**您的算法实际上已经是基于像素掩码的分割算法了！**

通过深入分析代码，我发现：
1. ✅ 算法使用 `generate_foam_mask()` 生成完整的二值像素掩膜
2. ✅ 算法使用 `compute_mask_centroid()` 计算掩膜质心（非外接矩形中心）
3. ✅ 算法使用 `compute_iou()` 计算与标准模板的交并比
4. ✅ 算法使用 `np.count_nonzero(mask)` 计算实际像素数
5. ✅ 算法支持加载标准模板掩膜进行比对

**结论**：您的泡棉检测算法**已经是像素分割算法**，而不是简单的轮廓外接矩形定位！

---

## 1. 算法架构深度解析

### 1.1 核心函数关系图

```
inspect()  # 主入口
    │
    ├─> generate_foam_mask()  # 生成像素掩膜（核心）
    │       ├─> 5种白色检测策略（HSV/LAB/OTSU/固定/自适应）
    │       ├─> 掩膜融合（OR逻辑）
    │       ├─> 排除绿色区域
    │       └─> 形态学处理（闭→开→闭）
    │
    ├─> _load_standard_mask_for_side()  # 加载标准模板
    │
    ├─> _detect_foam_side()  # 单侧检测
    │       ├─> generate_foam_mask()  # 生成检测掩膜
    │       ├─> compute_mask_centroid()  # 计算质心
    │       ├─> np.count_nonzero()  # 统计像素数
    │       ├─> compute_iou()  # 计算IoU
    │       └─> compute_physical_offset()  # 计算物理偏移
    │
    └─> _inspect_calibrated_sides()  # 左右侧联合判定
            ├─> _detect_foam_side(left)
            ├─> _detect_foam_side(right)
            └─> 综合判定
```

---

## 2. 像素级分割算法详解

### 2.1 掩膜生成（generate_foam_mask）

**函数签名**：
```python
def generate_foam_mask(roi_image, cfg=None):
    """Generate a binary foam mask for a single ROI."""
```

**算法流程**：

