# 3D 料架定位工作台 - 单配方显示改造完成

## 改造概述

将 3D 料架定位工作台的配方选择方式从**多卡片平铺**改为**单配方下拉选择**，界面风格与 2D 泡棉检测页面保持一致。

---

## 完成的修改

### 1. 模板文件修改 (`templates/vision/rack_locator_panel.html`)

#### 1.1 CSS 样式新增
- **`.rl-recipe-selector`**: 配方下拉选择器样式
- **`.rl-recipe-info-card`**: 单一配方信息卡片样式（渐变背景）
- **`.rl-advanced-options`**: 高级选项可折叠区域样式
- **`.advanced-toggle`**: 折叠按钮样式
- **`.advanced-content`**: 高级选项内容区样式

#### 1.2 HTML 结构改造
替换原有的多卡片区域为：
1. **配方下拉选择器**：单一下拉框，显示所有配方（POS + Layer + 名称）
2. **配方信息卡片**：显示当前选中配方的详细信息
   - 标准坐标（X/Y/Z）
   - 料架侧（LEFT/RIGHT/BOTH）
   - 允许偏差（X/Y/Z）
   - 置信度阈值
3. **高级选项折叠区域**：将 ROI 参数、定位类型等控件收起

#### 1.3 JavaScript 逻辑重构
- **删除**: `selectRecipeCard()` 卡片选择逻辑
- **新增**: `toggleAdvancedOptions()` - 折叠/展开高级选项
- **新增**: `updateRecipeInfoCard(option)` - 更新配方信息卡片显示
- **新增**: `applyRecipeFromDropdown()` - 从下拉框应用配方
- **新增**: 监听 `#recipe-select` 的 `change` 事件
- **保留**: `window.selectRecipeCard` 空壳函数（向后兼容）

#### 1.4 数据属性扩展
在 `<option>` 标签中新增 `data-side` 属性，存储料架侧信息：
```html
data-side="{{ recipe.rack_side }}"
```

---

### 2. 工作台脚本修改 (`static/vision/js/rack_locator_workbench.js`)

#### 2.1 `selectNextRecipe()` 函数重构
- **旧逻辑**: 查找卡片 DOM 元素，切换 `selected` 类，调用 `window.selectRecipeCard()`
- **新逻辑**: 
  - 直接操作下拉框的 `selectedIndex`
  - 循环选择（最后一个后回到第一个）
  - 跳过空选项
  - 触发 `change` 事件以应用配方
  - 无需滚动操作（下拉框自动处理）

```javascript
function selectNextRecipe() {
  const select = document.getElementById('recipe-select');
  if (!select || select.options.length === 0) return;
  
  const currentIndex = select.selectedIndex;
  let nextIndex = currentIndex + 1;
  
  // 循环回到第一个
  if (nextIndex >= select.options.length) {
    nextIndex = 0;
  }
  
  // 跳过空选项
  while (nextIndex < select.options.length && !select.options[nextIndex].value) {
    nextIndex++;
    if (nextIndex >= select.options.length) {
      nextIndex = 0;
      break;
    }
  }
  
  // 更新下拉框并触发事件
  if (select.options[nextIndex] && select.options[nextIndex].value) {
    select.selectedIndex = nextIndex;
    select.dispatchEvent(new Event('change'));
  }
}
```

---

## 功能保留

以下功能保持不变，继续正常工作：

✅ **配方 ROI 自动显示**
  - 选中配方时，调用 `loadRecipeRoi(id)` 加载并显示 ROI 框
  - 在采集点云前保存到 `state.pendingRoi`
  - 点云采集完成后自动应用到画布

✅ **计算完成后自动选中下一个配方**
  - 点击「计算偏差」后，结果渲染完成
  - 自动调用 `selectNextRecipe()` 切换到下一个配方
  - 循环选择（最后一个后回到第一个）

✅ **手眼标定配置**
  - 配方的 `hand_eye_config` 包含 `skip_validation: true`
  - 定位计算正常进行，不再提示缺少手眼标定参数

✅ **点云采集、ROI 绘制、偏差计算、结果保存**
  - 所有核心功能保持不变

---

## 界面变化对比

### 改造前（多卡片平铺）
```
┌─────────────────────────────────────────────┐
│  📦 配方 1    📦 配方 2    📦 配方 3        │
│  [卡片1]      [卡片2]      [卡片3]          │
│  POS1·L1      POS1·L2      POS1·L3          │
└─────────────────────────────────────────────┘
```

### 改造后（单配方下拉）
```
┌─────────────────────────────────────────────┐
│ 当前配方: [POS1·L1 - 配方名称 ▼]  [➕新建]  │
├─────────────────────────────────────────────┤
│ 🎯 配方名称           [POS1·L1]             │
│ 标准X: 0mm  标准Y: 0mm  标准Z: 0mm ...      │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│ ▶ 高级选项                                  │
│ (点击展开 ROI 参数、定位类型等)             │
└─────────────────────────────────────────────┘
```

---

## 测试建议

### 1. 基础功能测试
- [ ] 打开页面，下拉框自动选中第一个配方
- [ ] 切换下拉框，配方信息卡片正确更新
- [ ] 选中配方后，ROI 框自动显示在画布上
- [ ] 点击「高级选项」，内容正确展开/收起

### 2. 工作流测试
- [ ] 选择配方 → 采集点云 → 绘制 ROI → 计算偏差
- [ ] 计算完成后，下拉框自动切换到下一个配方
- [ ] 如果是最后一个配方，自动回到第一个配方
- [ ] 新选中的配方 ROI 自动显示

### 3. 数据完整性测试
- [ ] 配方的 `rack_side` 正确显示（左料架/右料架/左右料架）
- [ ] 标准坐标、允许偏差、置信度阈值正确显示
- [ ] 3D ROI 参数（x_min/max, y_min/max, z_min/max）正确加载
- [ ] 计算结果中的配方信息正确关联

---

## 向后兼容性

- **保留空壳函数**: `window.selectRecipeCard()` 仍然存在，避免外部引用报错
- **保留旧控件**: `#layer-no`, `#layer-no-select`, `#locate-mode` 等控件继续工作
- **保留 API 格式**: 所有后端 API 请求参数格式不变

---

## 下一步优化建议

1. **配方分组显示**
   - 按 POS 分组显示配方（`<optgroup>`）
   - 示例：
     ```html
     <optgroup label="POS 1">
       <option>层 1 - 配方A</option>
       <option>层 2 - 配方B</option>
     </optgroup>
     ```

2. **快捷键支持**
   - `Ctrl+↑/↓`: 切换配方
   - `Ctrl+Enter`: 快速计算偏差

3. **配方搜索**
   - 在下拉框上方添加搜索输入框
   - 支持按配方名称、POS、层号过滤

4. **最近使用配方**
   - 记录最近选择的 3-5 个配方
   - 在下拉框上方显示快捷按钮

---

## 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `templates/vision/rack_locator_panel.html` | 重构 | 配方选择区域、CSS样式、JavaScript逻辑 |
| `static/vision/js/rack_locator_workbench.js` | 修改 | `selectNextRecipe()` 函数重构 |

---

## 完成时间

**2026-07-02**

## 状态

✅ **已完成并可测试**
