# 泡棉检测前端显示升级说明

## 升级时间
2026年7月13日

## 升级内容
将泡棉检测结果显示升级为**方案A：详细模式**，完整展示像素分割算法的所有关键指标。

---

## ✅ 升级完成的功能

### 1. 左右分侧详细显示

**原显示**：
```
综合得分: 0.632
覆盖率: 37.9%
X偏移: +26.4 px
Y偏移: -25.4 px
缺陷: 泡棉缺失/漏贴
```

**新显示**：
```
【POS0 配方 - 第1层泡棉检测】

━━━ 左侧泡棉 ━━━
✗ 不合格 (缺陷: 覆盖不足)

覆盖率:      37.9%  (1215 / 3200 像素) ⚠️ 低于85%阈值
交并比(IoU): 42.3%  ⚠️ 低于70%阈值
位置偏移:    2.73 mm  (26.4px, -25.4px) ⚠️ 超出2.0mm限值
综合得分:    0.632

━━━ 右侧泡棉 ━━━
✓ 合格

覆盖率:      94.2%  (3018 / 3200 像素)
交并比(IoU): 88.5%
位置偏移:    0.82 mm  (8.2px, -6.1px)
综合得分:    0.912

━━━ 最终判定 ━━━
✗ 不合格 - 左侧覆盖不足
```

---

### 2. 新增显示的关键指标

| 指标 | 说明 | 用途 |
|------|------|------|
| **检测像素数 / 标准像素数** | 实际检测到的像素 vs 标准模板像素 | 精确了解覆盖情况 |
| **交并比(IoU)** | 与标准模板的重叠程度 | 评估贴合完整度 |
| **物理偏移(mm)** | 毫米级偏移量 | 便于机器人调整 |
| **阈值对比** | 实时对比配置的阈值 | 一眼看出是否达标 |
| **智能警告** | ⚠️ 标识不达标项 | 快速定位问题 |

---

### 3. 缺陷分类细化

| 原分类 | 新分类 | 判定依据 |
|--------|--------|---------|
| 泡棉缺失/漏贴 | **缺失** | 覆盖率 < 5% |
| 泡棉缺失/漏贴 | **覆盖不足** | 5% ≤ 覆盖率 < 85% |
| （无细分） | **位置偏移** | 覆盖率≥85%, IoU≥70%, 偏移>2mm |
| （无细分） | **形状不完整** | 覆盖率≥85%, IoU<70%, 偏移≤2mm |
| （无） | **合格** | 覆盖率≥85%, IoU≥70%, 偏移≤2mm |

---

## 🎯 显示效果对比

### 场景1：左侧覆盖不足

**原显示问题**：
- ❌ 显示"泡棉缺失/漏贴"，但覆盖率37.9%（不是缺失是不足）
- ❌ 没有IoU信息，不知道贴合质量
- ❌ 只显示像素偏移，不显示毫米偏移

**新显示优势**：
- ✅ 明确显示"覆盖不足 (37.9%)"
- ✅ 显示IoU 42.3%，知道贴合不完整
- ✅ 显示2.73mm偏移，便于调整机器人
- ✅ 显示实际像素数 1215/3200，便于调试

### 场景2：左右均合格

**新显示**：
```
━━━ 左侧泡棉 ━━━
✓ 合格

覆盖率:      94.2%  (3018 / 3200 像素)
交并比(IoU): 88.5%
位置偏移:    0.82 mm  (8.2px, -6.1px)
综合得分:    0.912

━━━ 右侧泡棉 ━━━
✓ 合格

覆盖率:      96.1%  (3075 / 3200 像素)
交并比(IoU): 91.2%
位置偏移:    0.51 mm  (5.1px, -3.2px)
综合得分:    0.941

━━━ 最终判定 ━━━
✓ 合格
```

---

## 📂 修改的文件

### 前端模板
- `templates/vision/foam_inspector_interactive.html`
  - HTML结构：新增详细结果区域
  - JavaScript：新增`renderSideResult()`、`getDefectReason()`等函数

### 后端（无需修改）
- 算法已经是像素分割算法，返回数据已包含所有必要字段
- `r.sides.left` 和 `r.sides.right` 已包含所有指标
- 无需修改后端代码

---

## 🔧 配置说明

### 判定阈值（在配方中配置）

```python
threshold_config = {
    'coverage_threshold': 0.85,   # 覆盖率阈值（85%）
    'iou_threshold': 0.70,        # IoU阈值（70%）
    'max_offset_mm': 2.0,         # 最大偏移（2.0mm）
}
```

### 前端显示逻辑

```javascript
// 覆盖率判定
if (coverage < 0.05) {
  缺陷 = "缺失";
} else if (coverage < 0.85) {
  缺陷 = "覆盖不足";
}

// IoU判定
if (iou < 0.70) {
  缺陷 = "形状不完整";
}

// 偏移判定
if (offset_mm > 2.0) {
  缺陷 = "位置偏移";
}
```

---

## 🚀 使用建议

### 操作员视角
1. **快速判定**：看顶部横幅（✓合格 / ✗不合格）
2. **定位问题**：看左右侧的红色⚠️警告
3. **调整机器人**：看物理偏移（mm），而非像素
4. **调试时**：看实际像素数，判断ROI是否标定准确

### 调试视角
1. **覆盖率异常**：
   - 检查实际像素数/标准像素数比例
   - 可能是标准模板不准确或ROI位置变化

2. **IoU异常**：
   - 说明泡棉形状与标准模板差异大
   - 可能是褶皱、边缘缺损或形变

3. **偏移异常**：
   - 直接根据mm值调整机器人坐标
   - 像素值仅供参考

---

## 📝 后续优化建议

### 短期（可选）
1. 点击左/右侧区域，可展开更多调试信息（质心坐标、边界框等）
2. 添加历史趋势图，监控每个POS的覆盖率变化
3. 添加导出功能，保存详细检测日志

### 长期（规划）
1. 引入深度学习模型，提高脏污泡棉检测准确率
2. 实时监控多条产线，集中显示
3. 自动报警推送（钉钉/企业微信）

---

## ✅ 测试建议

### 测试场景
1. **合格样本**：左右均合格
2. **覆盖不足**：覆盖率30-70%
3. **完全缺失**：覆盖率 < 5%
4. **位置偏移**：覆盖率OK，但偏移>2mm
5. **形状异常**：覆盖率OK，但IoU < 70%

### 验证点
- [ ] 阈值警告⚠️正确显示
- [ ] 像素数/标准像素数比例正确
- [ ] IoU百分比正确
- [ ] 物理偏移（mm）正确
- [ ] 缺陷分类准确（缺失 vs 覆盖不足）
- [ ] 最终判定与左右侧结果一致

---

## 📞 技术支持

如有问题，请查看：
1. 浏览器控制台日志（F12 → Console）
2. 检查返回数据结构是否包含 `r.sides.left` 和 `r.sides.right`
3. 确认后端算法返回了IoU、detected_pixels等字段

**完成时间**：2026-07-13
**升级状态**：✅ 已完成
