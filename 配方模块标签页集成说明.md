# 配方模块标签页集成说明

## 改造概述
将3D料架定位配方集成到视觉配方模块主页面（`recipe_management.html`）中，作为标签页显示，而不是跳转到独立页面。现在用户可以在同一个页面中通过标签页切换查看和管理2D和3D配方。

## 改造内容

### 1. 标签页结构改造
**改造前：**
- 2D配方：标签页按钮（`<button>`）
- 3D配方：跳转链接（`<a href="...">`）

**改造后：**
- 2D配方：标签页按钮（`<button data-recipe-tab="foam2d">`）
- 3D配方：标签页按钮（`<button data-recipe-tab="rack3d">`）

### 2. 3D配方内容集成
在 `recipe_management.html` 中添加了3D配方的显示区域：

```html
<section id="recipe-tab-rack3d" style="display:none;">
  <div id="rack3d-recipe-list"></div>
</section>
```

### 3. JavaScript功能增强

#### 3.1 标签页切换逻辑
- 添加 `currentTab` 变量跟踪当前激活的标签页
- 支持2D和3D标签页的完整切换
- 切换到3D标签页时自动加载3D配方数据

#### 3.2 3D配方管理功能
新增以下函数：
- `load3DRecipes()` - 加载3D配方列表
- `render3DRecipeCards()` - 渲染3D配方卡片
- `create3DRecipe()` - 新增3D配方（跳转到独立页面）
- `edit3DRecipe(id)` - 编辑3D配方（跳转到独立页面）
- `toggle3DEnabled(id, enabled)` - 切换启用/禁用状态
- `delete3DRecipe(id, name)` - 删除3D配方

#### 3.3 动态工作台按钮
根据当前标签页动态显示相关工作台入口：
- **2D配方标签页**：显示"泡棉检测工作台"
- **3D配方标签页**：显示"料架定位工作台"和"ROI工作台"

#### 3.4 新增配方按钮智能化
"新增配方"按钮根据当前标签页执行不同操作：
- 2D配方标签页：弹出2D配方创建弹窗
- 3D配方标签页：跳转到3D配方独立页面

### 4. 用户体验优化

#### 一致的卡片式设计
3D配方在标签页内也采用与2D配方一致的卡片式布局：
```
┌─────────────────────────────────────────┐
│ 🔵第X层  配方名称              ✔已启用 │
│ 标准坐标（mm）：X: xx Y: xx Z: xx     │
│ ROI范围（mm）：X(...) Y(...) Z(...)   │
│ [编辑] [禁用] [删除]                   │
└─────────────────────────────────────────┘
```

#### 快速操作
- ✅ 在主页面查看所有3D配方
- ✅ 在主页面启用/禁用3D配方
- ✅ 在主页面删除3D配方
- ✅ 点击"编辑"或"新增"跳转到独立页面进行详细编辑

### 5. API调用
3D配方使用的API端点：
- 查询：`GET {% url "vision:api_vision_3d_recipes" %}`
- 更新：`PATCH {% url "vision:api_vision_3d_recipes" %}`
- 删除：`DELETE {% url "vision:api_vision_3d_recipes" %}`

## 页面路由保持不变
- 主配方页面：`/vision/recipes/` - 支持标签页切换
- 3D配方独立页面：`/vision/rack-location/recipes/` - 用于新增和详细编辑

## 功能对比

### 改造前
- 点击"料架定位配方（3D）"→ 跳转到独立页面
- 需要返回才能查看2D配方
- 页面切换频繁

### 改造后
- 点击"料架定位配方（3D）"→ 切换标签页
- 在同一页面快速切换2D/3D配方
- 简单操作在主页面完成，复杂编辑跳转到独立页面

## 优势
1. **统一体验**：2D和3D配方在同一页面管理
2. **快速切换**：标签页切换无需页面跳转
3. **操作便捷**：常用操作（查看、启用/禁用、删除）在主页面完成
4. **灵活编辑**：复杂的新增和编辑操作仍在独立页面完成

## 文件修改清单
- ✅ `templates/vision/recipe_management.html` - 主配方页面
  - 标签页按钮改造
  - 3D配方内容区域
  - JavaScript逻辑增强
  - 动态按钮显示

## 测试建议
1. 访问视觉配方模块主页面
2. 点击"料架定位配方（3D）"标签页
3. 确认3D配方列表正常显示
4. 测试启用/禁用功能
5. 测试删除功能
6. 点击"新增配方"确认跳转到独立页面
7. 点击"编辑"确认跳转到独立页面
8. 切换回"泡棉检测配方（2D）"确认功能正常
9. 确认工作台按钮根据标签页正确显示

## 改造完成时间
2026年7月2日
