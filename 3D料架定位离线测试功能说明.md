# 3D 料架定位离线测试功能说明

## 功能概述

在3D料架定位工作台中新增了**导入数据进行离线测试**功能，允许用户导入已保存的点云数据进行相机离线测试，测试数据会自动保存到指定路径。

## 功能特性

### 1. 数据导入
- 支持多种格式的点云数据导入：
  - `.npy` - NumPy数组格式（推荐）
  - `.json` - JSON格式的点云数据
  - `.txt` - 文本格式（每行包含x,y,z坐标）

### 2. 自动保存
导入的数据会自动保存到：`C:\Users\11410\Desktop\pic`

保存文件包括：
- **深度数据**: `depth_YYYYMMDD_HHMMSS_mmm.npy` - 原始点云数据（NumPy格式）
- **2D图片**: `image_YYYYMMDD_HHMMSS_mmm.png` - 深度伪彩色可视化图

### 3. 自动生成可视化
如果导入的数据中没有2D图片，系统会自动从深度数据生成伪彩色可视化图：
- 使用Jet配色方案
- 深度值自动归一化
- 适用于快速查看点云分布

## 使用方法

### 步骤1: 打开工作台
访问：http://localhost:8000/vision/rack-locator/

### 步骤2: 选择配方
从下拉列表中选择一个3D料架定位配方

### 步骤3: 导入数据
点击 **📂 导入数据** 按钮

### 步骤4: 选择文件
在文件选择对话框中选择点云数据文件：
- `.npy` 文件（NumPy保存的点云数组）
- `.json` 文件（JSON格式的点云数据）
- `.txt` 文件（文本格式，每行为 x,y,z 坐标）

### 步骤5: 查看结果
- 导入成功后会显示保存路径
- 可以在 `C:\Users\11410\Desktop\pic` 目录下查看保存的文件

## 点云数据格式要求

### .npy 格式
```python
import numpy as np

# 点云数据应为 (N, 3) 形状的NumPy数组
# N: 点的数量
# 3: x, y, z 坐标
pointcloud = np.array([
    [x1, y1, z1],
    [x2, y2, z2],
    ...
], dtype=np.float32)

# 保存
np.save('pointcloud.npy', pointcloud)
```

### .json 格式
```json
{
  "pointcloud": [
    [x1, y1, z1],
    [x2, y2, z2],
    [x3, y3, z3]
  ]
}
```

或简化格式：
```json
[
  [x1, y1, z1],
  [x2, y2, z2],
  [x3, y3, z3]
]
```

### .txt 格式
```
x1 y1 z1
x2 y2 z2
x3 y3 z3
```
或使用逗号分隔：
```
x1,y1,z1
x2,y2,z2
x3,y3,z3
```

## API 接口

### 1. 离线测试接口
**端点**: `POST /vision/api/rack/offline-test/`

**请求体**:
```json
{
  "pointcloud": [[x1,y1,z1], [x2,y2,z2], ...],
  "image": "base64图片数据或文件路径（可选）",
  "image_width": 640,
  "image_height": 480
}
```

**响应**:
```json
{
  "success": true,
  "message": "数据保存成功",
  "files": {
    "depth": "C:\\Users\\11410\\Desktop\\pic\\depth_20261207_143022_123.npy",
    "image": "C:\\Users\\11410\\Desktop\\pic\\image_20261207_143022_123.png"
  },
  "pointcloud_shape": [307200, 3],
  "timestamp": "20261207_143022_123"
}
```

### 2. .npy文件解析接口
**端点**: `POST /vision/api/rack/import-npy/`

**请求**: Multipart/form-data，包含文件字段

**响应**:
```json
{
  "success": true,
  "pointcloud": [[x1,y1,z1], [x2,y2,z2], ...],
  "shape": [307200, 3],
  "point_count": 307200
}
```

## 测试脚本

运行测试脚本验证功能：

```bash
python test_offline_import.py
```

测试脚本会：
1. 生成模拟点云数据
2. 测试离线API
3. 测试.npy文件导入API
4. 验证文件保存

## 技术实现

### 前端
- 文件选择对话框（`<input type="file">`）
- FileReader API读取文件内容
- 支持多种格式自动识别
- AJAX上传和处理

### 后端
1. **视图函数**:
   - `api_rack_locator_offline_test`: 处理数据保存
   - `api_rack_locator_import_npy`: 解析.npy文件

2. **数据处理**:
   - NumPy数组处理
   - PIL图像生成
   - Matplotlib伪彩色映射

3. **文件保存**:
   - 自动创建目录
   - 时间戳命名
   - 同时保存深度和图像

## 应用场景

### 1. 离线算法调试
- 保存现场采集的点云数据
- 在实验室环境下反复测试
- 无需连接实际相机

### 2. 数据集构建
- 批量保存测试数据
- 建立标准测试集
- 用于算法验证和回归测试

### 3. 问题复现
- 保存异常情况的点云数据
- 离线分析和调试
- 快速定位问题原因

### 4. 算法对比
- 使用相同数据测试不同算法
- 公平对比算法性能
- 记录优化效果

## 注意事项

1. **文件大小**: 点云数据可能较大，建议：
   - 640×480点云约为7.3MB（.npy格式）
   - 导入前可以进行采样降低数据量

2. **保存路径**: 确保目标路径存在且有写入权限：
   - `C:\Users\11410\Desktop\pic`
   - 系统会自动创建目录

3. **数据格式**: 确保点云数据格式正确：
   - 必须是 (N, 3) 形状
   - 包含 x, y, z 三维坐标
   - 推荐使用float32类型

4. **浏览器兼容性**: 
   - 需要支持FileReader API
   - 推荐使用现代浏览器（Chrome、Firefox、Edge）

## 后续优化建议

1. **批量导入**: 支持一次导入多个文件
2. **数据预览**: 导入后在前端显示点云可视化
3. **格式转换**: 提供在线格式转换工具
4. **数据管理**: 提供导入历史和数据管理功能
5. **压缩支持**: 支持.zip等压缩格式
6. **自定义路径**: 允许用户选择保存路径

## 相关文件

### 后端
- `apps/vision/views.py`: 视图函数
  - `api_rack_locator_offline_test()`
  - `api_rack_locator_import_npy()`
- `apps/vision/urls.py`: URL路由配置

### 前端
- `templates/vision/rack_locator_panel.html`: 工作台模板
- `static/vision/js/rack_locator_workbench_simple.js`: JavaScript逻辑

### 测试
- `test_offline_import.py`: 功能测试脚本

## 联系与支持

如有问题或建议，请联系开发团队。
