# 3D配方增删改查 - 调试优化说明

## 问题诊断

### 问题1：编辑配方时提示"获取配方数据失败"

**原因：**
API响应格式不一致：
```python
# 旧的API使用了包装函数
return _api3d_success({'recipes': [...]})
# 实际返回：
{
  "success": true,
  "data": {
    "recipes": [...]  # recipes在data里面
  },
  "error": ""
}

# 但前端期望：
{
  "success": true,
  "recipes": [...]  # recipes直接在顶层
}
```

**解决方案：**
修改API直接返回 `JsonResponse`，不使用包装函数：
```python
return JsonResponse({
    'success': True,
    'recipes': [_serialize_3d_recipe(recipe) for recipe in qs],
})
```

---

## 已完成的优化

### 1. API响应格式统一

**修改文件：** `apps/vision/views.py`

**所有API端点现在返回统一格式：**

#### GET - 查询配方
```json
{
  "success": true,
  "recipes": [
    {
      "id": 1,
      "recipe_name": "3D-L1",
      "layer_no": 1,
      "standard_x": 100.0,
      "standard_y": 200.0,
      "standard_z": 800.0,
      "standard_rz": 0.0,
      "roi_config": {...},
      "enabled": true
    }
  ]
}
```

#### POST - 创建配方
```json
{
  "success": true,
  "recipe": { /* 新创建的配方对象 */ }
}
```

#### PATCH - 更新配方
```json
{
  "success": true,
  "recipe": { /* 更新后的配方对象 */ }
}
```

#### DELETE - 删除配方
```json
{
  "success": true,
  "message": "配方「3D-L1」已删除"
}
```

#### 错误响应（所有端点）
```json
{
  "success": false,
  "error": "具体错误信息"
}
```

---

### 2. 前端错误处理增强

**修改文件：** `templates/vision/rack_location_recipes.html`

#### 编辑配方函数优化

**旧版：**
```javascript
async function editRecipe(id) {
  const data = await fetch(...).json();
  if (data.success && data.recipes && data.recipes.length > 0) {
    // 正常处理
  } else {
    showToast('获取配方数据失败', 'error');  // 错误信息不够明确
  }
}
```

**新版：**
```javascript
async function editRecipe(id) {
  try {
    const res = await fetch(`${API_URL}?id=${id}`);
    const data = await res.json();
    
    // 详细的错误检查
    if (!data.success) {
      showToast('获取配方失败：' + (data.error || '未知错误'), 'error');
      return;
    }
    
    if (!data.recipes || data.recipes.length === 0) {
      showToast('配方不存在', 'error');
      return;
    }
    
    const recipe = data.recipes[0];
    // ... 填充表单
    
  } catch (e) {
    console.error('编辑配方失败:', e);  // 浏览器控制台输出详细错误
    showToast('网络请求失败：' + e.message, 'error');
  }
}
```

#### 保存配方函数优化

**新增：**
- HTTP状态码检查：`if (!res.ok) throw new Error(...)`
- 表单验证：焦点定位到错误字段
- 详细的错误日志：`console.error(...)`

```javascript
async function saveRecipe() {
  // 表单验证
  if (!name) { 
    showToast('请填写配方名称', 'error'); 
    document.getElementById('modal-name').focus();  // 焦点定位
    return; 
  }
  
  try {
    const res = await fetch(...);
    
    if (!res.ok) {
      throw new Error(`HTTP ${res.status}: ${res.statusText}`);
    }
    
    const data = await res.json();
    // ...
  } catch (e) {
    console.error('保存配方失败:', e);
    showToast('网络请求失败：' + e.message, 'error');
  }
}
```

#### 删除和切换状态函数优化

同样添加了：
- HTTP状态码检查
- 详细错误日志
- 更明确的错误提示

---

### 3. 删除无用的"预计算"功能

**修改文件：** `templates/vision/rack_location_recipe_form.html`

**删除了整个预计算结果区域：**
```html
<!-- 已删除 -->
<fieldset class="rack-location-fieldset rack-location-result">
  <legend>预计算结果（实际测量坐标）</legend>
  <div class="rl-result-head rack-location-wide">
    <span id="locate-badge">未预计算</span>
    <span id="rl-confidence-chip">置信度 —</span>
    <span id="rl-points-chip">有效点 —</span>
    <span id="rl-source-chip">数据源 —</span>
  </div>
  <label>actual_x <input readonly></label>
  <label>actual_y <input readonly></label>
  <label>actual_z <input readonly></label>
  <label>offset_x <input readonly></label>
  <label>offset_y <input readonly></label>
  <label>offset_z <input readonly></label>
  <label>confidence <input readonly></label>
  <label>message <input readonly></label>
</fieldset>
```

**原因：**
- 配方编辑页面应该**只做配置**，不做实际定位计算
- "置信度"、"有效点"、"actual_x/y/z"等是**运行时数据**，不应该出现在配置页面
- 这些数据应该在"定位工作台"或"历史记录"中查看

---

## 调试方法

### 1. 浏览器开发者工具

打开浏览器控制台（F12），查看：

#### Console（控制台）标签
- 查看JavaScript错误
- 查看 `console.error()` 输出的详细错误信息

**示例输出：**
```
编辑配方失败: TypeError: Cannot read property 'recipe_name' of undefined
  at editRecipe (rack_location_recipes.html:123)
```

#### Network（网络）标签
- 查看API请求和响应
- 检查HTTP状态码（200/400/500）
- 查看请求和响应的JSON数据

**示例：**
```
GET /api/vision/3d/recipes/?id=1
Status: 200 OK
Response:
{
  "success": true,
  "recipes": [...]
}
```

### 2. Django服务器日志

在运行Django的终端窗口查看：

**示例输出：**
```
[INFO] GET /api/vision/3d/recipes/?id=1
[ERROR] KeyError: 'recipe_name'
Traceback (most recent call last):
  ...
```

### 3. 测试API接口

使用命令行工具测试API：

#### 查询配方
```bash
curl -X GET "http://localhost:8000/api/vision/3d/recipes/?id=1"
```

#### 创建配方
```bash
curl -X POST "http://localhost:8000/api/vision/3d/recipes/" \
  -H "Content-Type: application/json" \
  -H "X-CSRFToken: YOUR_CSRF_TOKEN" \
  -d '{
    "recipe_name": "3D-L1",
    "layer_no": 1,
    "standard_x": 100.0,
    "standard_y": 200.0,
    "standard_z": 800.0
  }'
```

#### 更新配方
```bash
curl -X PATCH "http://localhost:8000/api/vision/3d/recipes/" \
  -H "Content-Type: application/json" \
  -H "X-CSRFToken: YOUR_CSRF_TOKEN" \
  -d '{
    "id": 1,
    "standard_z": 850.0
  }'
```

#### 删除配方
```bash
curl -X DELETE "http://localhost:8000/api/vision/3d/recipes/" \
  -H "Content-Type: application/json" \
  -H "X-CSRFToken: YOUR_CSRF_TOKEN" \
  -d '{
    "id": 1
  }'
```

---

## 常见错误及解决方案

### 错误1：获取配方数据失败

**症状：**
点击编辑按钮，提示"❌失败 获取配方数据失败"

**可能原因：**
1. API响应格式不正确
2. 配方ID不存在
3. 网络请求失败

**调试步骤：**
1. 打开浏览器控制台（F12）
2. 查看Console标签，找到错误信息
3. 查看Network标签，检查API请求：
   - 请求URL是否正确：`/api/vision/3d/recipes/?id=1`
   - HTTP状态码：200（成功）还是 404/500（失败）
   - 响应内容是否包含 `success: true` 和 `recipes: [...]`

**解决方案：**
- 已修复API响应格式，确保返回 `{success: true, recipes: [...]}`
- 已添加详细错误提示和日志

### 错误2：保存配方失败

**症状：**
填写表单后点击保存，提示"❌失败 保存失败：XXX"

**可能原因：**
1. 必填字段未填写
2. 数据格式不正确
3. 数据库约束冲突（如同一层号已有启用的配方）

**调试步骤：**
1. 检查表单验证是否通过
2. 查看浏览器控制台的错误信息
3. 查看Django服务器日志

**解决方案：**
- 已添加表单验证，焦点定位到错误字段
- 已添加HTTP状态码检查
- 错误信息会显示具体原因

### 错误3：CSRF token错误

**症状：**
提示"403 Forbidden: CSRF verification failed"

**原因：**
Django的CSRF保护机制

**解决方案：**
确保页面有CSRF token：
```html
<input type="hidden" name="csrfmiddlewaretoken" value="{{ csrf_token }}">
```

JavaScript获取token：
```javascript
function getCsrf() {
  return document.querySelector('[name=csrfmiddlewaretoken]')?.value || '';
}
```

---

## 测试清单

### ✅ 功能测试

#### 1. 查询功能
- [ ] 打开页面，能看到配方列表
- [ ] 列表显示：配方名称、层号、标准坐标、ROI范围、状态
- [ ] 按层号正确排序

#### 2. 新增功能
- [ ] 点击「新增配方」，Modal弹出
- [ ] 必填项验证（配方名称、层号）
- [ ] 填写完整数据后保存成功
- [ ] 页面刷新后新配方出现在列表中

#### 3. 编辑功能
- [ ] 点击某配方的「编辑」按钮
- [ ] Modal弹出并预填当前数据
- [ ] 修改数据后保存成功
- [ ] 页面刷新后数据已更新

#### 4. 删除功能
- [ ] 点击「删除」按钮，弹出确认对话框
- [ ] 点击「确定」后配方被删除
- [ ] 页面刷新后配方从列表消失

#### 5. 切换状态功能
- [ ] 点击启用/禁用按钮
- [ ] 状态标签和行样式实时更新
- [ ] 页面刷新后状态保持

### ✅ 错误处理测试

#### 1. 表单验证
- [ ] 不填配方名称，提示错误并聚焦到该字段
- [ ] 层号输入0或负数，提示错误

#### 2. 网络错误
- [ ] 断网状态下操作，提示"网络请求失败"
- [ ] 服务器返回错误时，显示具体错误信息

#### 3. 数据冲突
- [ ] 尝试创建同一层号的多个启用配方，提示错误

---

## 性能优化建议

### 1. 减少页面刷新

**当前：**
每次操作后都 `location.reload()` 刷新整个页面

**优化方案：**
使用局部更新，不刷新页面：

```javascript
// 保存后
if (data.success) {
  if (id) {
    // 更新：修改表格中的对应行
    updateTableRow(id, data.recipe);
  } else {
    // 新增：在表格中添加新行
    addTableRow(data.recipe);
  }
  closeModal();
  showToast('保存成功', 'success');
  // 不刷新页面
}
```

### 2. 防抖处理

对于快速连续点击，添加防抖：

```javascript
let saving = false;
async function saveRecipe() {
  if (saving) return;
  saving = true;
  try {
    // ... 保存逻辑
  } finally {
    saving = false;
  }
}
```

### 3. 加载状态提示

```javascript
async function editRecipe(id) {
  showToast('加载中...', 'info');
  try {
    // ... 获取数据
    closeToast();  // 关闭加载提示
  } catch (e) {
    showToast('加载失败', 'error');
  }
}
```

---

## 总结

### ✅ 已解决的问题

1. **API响应格式统一** - 所有端点返回一致的JSON格式
2. **错误处理增强** - 详细的错误提示和日志输出
3. **表单验证优化** - 焦点定位到错误字段
4. **删除无用功能** - 移除"预计算结果"区域

### ✅ 改进效果

1. **调试更容易** - 浏览器控制台有详细错误信息
2. **错误提示更清晰** - 用户知道具体哪里出错了
3. **页面更简洁** - 去除了不必要的"预计算"功能
4. **代码更健壮** - 完善的错误处理和验证

### 📝 使用建议

1. **开发时始终打开浏览器控制台（F12）** - 查看详细错误信息
2. **遇到问题先看Console标签** - 有 `console.error()` 输出
3. **再看Network标签** - 检查API请求和响应
4. **最后看Django日志** - 查看服务器端错误

---

## 快速故障排查流程

```
1. 页面操作失败（如编辑、保存、删除）
   ↓
2. 打开浏览器控制台（F12）
   ↓
3. 查看Console标签
   - 有 "编辑配方失败: XXX" 类似的错误？
   - 记录错误信息
   ↓
4. 查看Network标签
   - 找到对应的API请求
   - 检查HTTP状态码（200/400/500）
   - 查看Response内容
   ↓
5. 根据错误信息定位问题：
   - 400错误 → 请求参数问题（检查前端代码）
   - 500错误 → 服务器内部错误（检查Django日志）
   - 网络错误 → 检查服务器是否运行
   ↓
6. 修复并重新测试
```

按照这个流程，99%的问题都能快速定位和解决！
