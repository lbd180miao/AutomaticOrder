# 泡棉检测前端显示升级 - 测试指南

## 📋 测试前准备

### 1. 启动开发服务器
```bash
cd d:\workspace2\AutomaticOrder
python manage.py runserver
```

### 2. 访问泡棉检测工作台
```
http://127.0.0.1:8000/vision/foam-inspector/
```

### 3. 打开浏览器开发者工具
按 `F12` 打开控制台，用于查看日志和调试

---

## ✅ 测试场景

### 场景1：左右均合格（理想状态）

**期待显示**：
```
【POSx 配方 - 第x层泡棉检测】

━━━ 左侧泡棉 ━━━
✓ 合格

覆盖率:      94.2%  (3018 / 3200 像素)
交并比(IoU): 88.5%
位置偏移:    0.82 mm  (8.2px, -6.1px)
综合得分:    0.912

━━━ 右侧泡棉 ━━━
✓ 合格

覆盖率:      96.1%  (3075 / 3200 像素)
交并比(IoU): 91.2%
位置偏移:    0.51 mm  (5.1px, -3.2px)
综合得分:    0.941

━━━ 最终判定 ━━━
✓ 合格
```

**验证点**：
- [ ] 左右侧都显示绿色"✓ 合格"徽章
- [ ] 覆盖率显示百分比和实际像素数
- [ ] IoU显示百分比
- [ ] 位置偏移显示毫米值和像素值
- [ ] 综合得分显示3位小数
- [ ] 最终判定显示"✓ 合格"（绿色背景）

---

### 场景2：左侧覆盖不足（常见异常）

**模拟方法**：
1. 使用一张左侧泡棉部分遮挡或缺损的图片
2. 或调整覆盖率阈值为很高（如95%），让正常图片也触发

**期待显示**：
```
【POSx 配方 - 第x层泡棉检测】

━━━ 左侧泡棉 ━━━
✗ 不合格 (缺陷: 覆盖不足)

覆盖率:      37.9%  (1215 / 3200 像素) ⚠️ 低于85%阈值
交并比(IoU): 42.3%  ⚠️ 低于70%阈值
位置偏移:    2.73 mm  (26.4px, -25.4px) ⚠️ 超出2.0mm限值
综合得分:    0.632

━━━ 右侧泡棉 ━━━
✓ 合格

覆盖率:      94.2%  (3018 / 3200 像素)
交并比(IoU): 88.5%
位置偏移:    0.82 mm  (8.2px, -6.1px)
综合得分:    0.912

━━━ 最终判定 ━━━
✗ 不合格 - 左侧覆盖不足
```

**验证点**：
- [ ] 左侧显示红色"✗ 不合格"徽章，注明"缺陷: 覆盖不足"
- [ ] 覆盖率行显示 ⚠️ 警告："低于85%阈值"
- [ ] IoU行显示 ⚠️ 警告："低于70%阈值"
- [ ] 位置偏移行显示 ⚠️ 警告："超出2.0mm限值"
- [ ] 右侧仍显示合格（绿色）
- [ ] 最终判定显示"✗ 不合格 - 左侧覆盖不足"（红色背景）

---

### 场景3：左侧完全缺失

**期待显示**：
```
━━━ 左侧泡棉 ━━━
✗ 不合格 (缺陷: 缺失)

覆盖率:      0.8%  (26 / 3200 像素) ⚠️ 低于85%阈值
交并比(IoU): 1.2%  ⚠️ 低于70%阈值
位置偏移:    0.00 mm  (0.0px, 0.0px)
综合得分:    0.000

━━━ 最终判定 ━━━
✗ 不合格 - 左侧缺失
```

**验证点**：
- [ ] 缺陷类型显示为"缺失"（而不是"覆盖不足"）
- [ ] 覆盖率接近0%
- [ ] IoU接近0%
- [ ] 最终判定明确指出"左侧缺失"

---

### 场景4：位置偏移（覆盖率OK但偏移大）

**期待显示**：
```
━━━ 左侧泡棉 ━━━
✗ 不合格 (缺陷: 位置偏移)

覆盖率:      91.2%  (2918 / 3200 像素)
交并比(IoU): 78.5%
位置偏移:    3.84 mm  (38.4px, -25.1px) ⚠️ 超出2.0mm限值
综合得分:    0.785

━━━ 最终判定 ━━━
✗ 不合格 - 左侧位置偏移
```

**验证点**：
- [ ] 缺陷类型显示为"位置偏移"
- [ ] 覆盖率正常（>85%）
- [ ] IoU正常（>70%）
- [ ] 只有位置偏移显示⚠️警告
- [ ] 最终判定指出"位置偏移"

---

### 场景5：形状不完整（覆盖率OK但IoU低）

**期待显示**：
```
━━━ 左侧泡棉 ━━━
✗ 不合格 (缺陷: 形状不完整)

覆盖率:      88.5%  (2832 / 3200 像素)
交并比(IoU): 62.3%  ⚠️ 低于70%阈值
位置偏移:    1.21 mm  (12.1px, -8.3px)
综合得分:    0.623

━━━ 最终判定 ━━━
✗ 不合格 - 左侧形状不完整
```

**验证点**：
- [ ] 缺陷类型显示为"形状不完整"
- [ ] 覆盖率正常（>85%）
- [ ] IoU异常（<70%），显示⚠️警告
- [ ] 位置偏移正常（<2mm）
- [ ] 最终判定指出"形状不完整"

---

## 🔍 调试检查点

### 1. 检查返回数据结构

打开浏览器控制台（F12），在检测后查看日志：

```javascript
// 应该能看到这样的日志
渲染检测结果: {
  position_index: 0,
  is_passed: false,
  sides: {
    left: {
      is_present: true,
      coverage_ratio: 0.379,
      iou: 0.423,
      detected_pixels: 1215,
      standard_pixels: 3200,
      offset_x_px: 26.4,
      offset_y_px: -25.4,
      offset_distance_mm: 2.73,
      score: 0.632,
      ...
    },
    right: {
      is_present: true,
      coverage_ratio: 0.942,
      iou: 0.885,
      ...
    }
  }
}
```

**验证点**：
- [ ] `sides.left` 存在且包含所有字段
- [ ] `sides.right` 存在且包含所有字段
- [ ] `detected_pixels` 和 `standard_pixels` 字段存在
- [ ] `iou` 字段存在
- [ ] `offset_distance_mm` 字段存在

### 2. 检查HTML元素

在控制台执行：

```javascript
// 检查元素是否存在
console.log(document.getElementById('detailed-result'));
console.log(document.getElementById('left-coverage-detail'));
console.log(document.getElementById('left-iou-detail'));
console.log(document.getElementById('left-offset-detail'));
console.log(document.getElementById('right-coverage-detail'));
```

**验证点**：
- [ ] 所有元素都不为 `null`
- [ ] 元素内容正确填充

### 3. 检查样式

**验证点**：
- [ ] 合格徽章背景为绿色（#dcfce7）
- [ ] 不合格徽章背景为红色（#fee2e2）
- [ ] ⚠️ 警告文本正确显示
- [ ] 最终判定区域背景色正确

---

## 🐛 常见问题排查

### 问题1：详细结果区域不显示

**可能原因**：
- 后端未返回 `sides` 字段
- JavaScript出错

**排查步骤**：
1. 打开控制台查看是否有JavaScript错误
2. 检查返回数据：`console.log('返回数据:', data.result)`
3. 确认 `data.result.sides` 是否存在

**解决方案**：
```javascript
// 在控制台执行，手动触发渲染测试
const testData = {
  position_index: 0,
  is_passed: false,
  sides: {
    left: {
      is_present: true,
      coverage_ratio: 0.379,
      iou: 0.423,
      detected_pixels: 1215,
      standard_pixels: 3200,
      offset_x_px: 26.4,
      offset_y_px: -25.4,
      offset_distance_mm: 2.73,
      score: 0.632,
      is_aligned: false,
      is_complete: false
    },
    right: {
      is_present: true,
      coverage_ratio: 0.942,
      iou: 0.885,
      detected_pixels: 3018,
      standard_pixels: 3200,
      offset_x_px: 8.2,
      offset_y_px: -6.1,
      offset_distance_mm: 0.82,
      score: 0.912,
      is_aligned: true,
      is_complete: true
    }
  }
};
renderResult(testData);
```

### 问题2：IoU显示"N/A (无标准模板)"

**原因**：未创建标准模板掩膜

**解决方案**：
1. 需要先创建标准模板掩膜（参考《泡棉检测算法深度分析报告.md》）
2. 或接受显示"N/A"（表示使用ROI面积而非标准模板）

### 问题3：警告⚠️不显示

**排查**：
```javascript
// 检查阈值
console.log('覆盖率:', data.sides.left.coverage_ratio);
console.log('阈值:', data.sides.left.coverage_threshold || 0.85);
console.log('是否低于:', data.sides.left.coverage_ratio < 0.85);
```

**验证**：
- 确保阈值字段正确
- 确保比较逻辑正确

### 问题4：缺陷分类不准确

**检查逻辑**：
```javascript
// getDefectReason() 函数逻辑
if (!data.is_present) {
  const coverage = data.coverage_ratio || 0;
  if (coverage < 0.05) {
    return '缺失';  // < 5%
  } else {
    return '覆盖不足';  // 5% - 85%
  }
}
if (!data.is_aligned) return '位置偏移';  // 偏移 > 2mm
if (!data.is_complete) return '形状不完整';  // IoU < 70%
```

**验证边界值**：
- 覆盖率 0.8% → "缺失"
- 覆盖率 5.0% → "覆盖不足"
- 覆盖率 37.9% → "覆盖不足"
- 覆盖率 84.9% → "覆盖不足"
- 覆盖率 85.0% + 偏移3mm → "位置偏移"
- 覆盖率 85.0% + IoU 65% → "形状不完整"

---

## 📝 测试记录表

| 场景 | 测试时间 | 结果 | 问题 | 备注 |
|------|---------|------|------|------|
| 左右均合格 | | ☐ 通过 / ☐ 失败 | | |
| 左侧覆盖不足 | | ☐ 通过 / ☐ 失败 | | |
| 左侧完全缺失 | | ☐ 通过 / ☐ 失败 | | |
| 位置偏移 | | ☐ 通过 / ☐ 失败 | | |
| 形状不完整 | | ☐ 通过 / ☐ 失败 | | |
| 数据结构检查 | | ☐ 通过 / ☐ 失败 | | |
| HTML元素检查 | | ☐ 通过 / ☐ 失败 | | |
| 样式检查 | | ☐ 通过 / ☐ 失败 | | |

---

## ✅ 测试通过标准

所有以下条件都满足：

- [x] 5个测试场景全部通过
- [x] 数据结构检查通过
- [x] HTML元素正常渲染
- [x] 样式显示正确
- [x] 无JavaScript错误
- [x] 缺陷分类准确
- [x] 警告⚠️正确显示
- [x] 最终判定与左右侧结果一致

---

## 🚀 下一步

测试通过后：

1. **提交代码**
   ```bash
   git add templates/vision/foam_inspector_interactive.html
   git commit -m "feat: 升级泡棉检测前端显示为详细模式（方案A）

   - 新增左右分侧详细显示
   - 显示覆盖率（像素数/标准像素数）
   - 显示IoU（交并比）
   - 显示物理偏移（mm）
   - 智能缺陷分类（缺失/覆盖不足/位置偏移/形状不完整）
   - 阈值实时对比和警告提示
   "
   ```

2. **部署到生产环境**
   - 备份当前前端文件
   - 部署新版本
   - 验证生产环境功能

3. **培训操作员**
   - 讲解新显示的含义
   - 说明如何根据指标调整机器人
   - 强调物理偏移（mm）的重要性

---

**测试人**：__________  
**测试日期**：__________  
**测试结果**：☐ 通过 / ☐ 失败  
**签名**：__________
