# 3D 料架定位离线测试 - 快速开始

## 🎯 功能简介

在3D料架定位工作台中导入点云数据进行离线测试，数据会自动保存到桌面指定文件夹。

## 📋 准备工作

### 1. 确认保存路径
数据会保存到: `C:\Users\11410\Desktop\pic`

如果目录不存在，系统会自动创建。

### 2. 准备点云数据文件
支持以下格式:
- `.npy` - NumPy数组格式 ⭐推荐
- `.json` - JSON格式
- `.txt` - 文本格式（每行x,y,z）

## 🚀 使用步骤

### 第一步: 打开工作台
在浏览器中访问:
```
http://localhost:8000/vision/rack-locator/
```

### 第二步: 选择配方
从下拉列表中选择一个3D料架定位配方

### 第三步: 导入数据
点击 **📂 导入数据** 按钮

### 第四步: 选择文件
在弹出的文件选择对话框中选择点云数据文件

### 第五步: 查看结果
- 系统会显示保存路径
- 到 `C:\Users\11410\Desktop\pic` 查看文件

## 📦 生成的文件

每次导入会生成两个文件:

1. **深度数据** (`.npy`格式)
   ```
   depth_20260702_173920_317.npy
   ```
   - 原始点云数据
   - NumPy数组格式
   - 可用于后续分析

2. **2D图片** (`.png`格式)
   ```
   image_20260702_173920_317.png
   ```
   - 深度伪彩色可视化
   - 蓝色=近，红色=远
   - 直观查看深度分布

## 💡 测试数据生成

如果没有现成的点云数据，运行测试脚本生成:

```bash
python test_offline_import.py
```

这会生成一个示例点云文件: `test_pointcloud.npy`

## 📝 点云数据格式示例

### Python生成.npy文件
```python
import numpy as np

# 创建点云数据 (N个点, 每个点有x,y,z坐标)
pointcloud = np.array([
    [100.5, 200.3, 800.1],  # 点1
    [101.2, 201.5, 799.8],  # 点2
    [102.0, 202.1, 801.3],  # 点3
    # ... 更多点
], dtype=np.float32)

# 保存
np.save('my_pointcloud.npy', pointcloud)
```

### JSON格式
```json
{
  "pointcloud": [
    [100.5, 200.3, 800.1],
    [101.2, 201.5, 799.8],
    [102.0, 202.1, 801.3]
  ]
}
```

### TXT格式
```
100.5 200.3 800.1
101.2 201.5 799.8
102.0 202.1 801.3
```

## ❓ 常见问题

### Q: 导入失败怎么办?
A: 检查:
1. 文件格式是否正确（.npy/.json/.txt）
2. 点云数据是否为 (N, 3) 形状
3. 浏览器控制台是否有错误信息

### Q: 保存路径在哪里?
A: `C:\Users\11410\Desktop\pic`
   系统会自动创建此目录

### Q: 生成的图片是什么?
A: 深度伪彩色图，用颜色表示深度:
   - 蓝色 = 近距离
   - 绿色 = 中距离
   - 红色 = 远距离

### Q: 支持多大的点云?
A: 理论上无限制，但建议:
   - 小文件 (< 1MB): 直接导入
   - 大文件 (> 10MB): 考虑采样降低点数

## 🔧 技术支持

- 详细文档: `3D料架定位离线测试功能说明.md`
- 完成说明: `3D料架定位离线测试功能完成说明.md`
- 测试脚本: `test_offline_import.py`

## ✅ 验证安装

运行测试验证功能是否正常:

```bash
python test_offline_import.py
```

如果看到以下输出，说明功能正常:

```
✓ 深度文件已创建: 11.84 KB
✓ 图片文件已创建: 1.90 KB
所有测试完成！
```

---

**开始使用吧！** 🎉
