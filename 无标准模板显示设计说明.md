# 无标准模板情况下的显示设计说明

## 设计目标
当检测时没有配置标准模板时，既要说明情况，又要提供有用信息，并引导用户创建标准模板。

---

## ✅ 最终方案：智能降级显示

### 1. IoU（交并比）显示

**有标准模板**：
```
交并比(IoU): 88.5%
```

**无标准模板**：
```
交并比(IoU): - (需要标准模板)
             💡 创建左侧标准模板可提高检测精度
```

**设计特点**：
- ✅ 明确说明需要标准模板
- ✅ 提供可点击的创建引导链接
- ✅ 说明创建标准模板的好处

---

### 2. 位置偏移显示

#### 情况A：有标准模板（精确模式）
```
位置偏移: 2.73 mm  (+26.4px, -25.4px) ⚠️ 超出2.0mm限值
```
- 基于标准质心计算
- 毫米级精度
- 阈值对比

#### 情况B：无标准模板但有偏移数据（降级模式）
```
位置偏移: 1.24 mm  (+12.3px, -8.5px)
          (相对ROI中心，创建标准模板可精确测量)
```
- 相对ROI中心计算（不够精确）
- 显示实际数据，不隐藏
- 明确说明这是相对ROI中心
- 引导创建标准模板

#### 情况C：完全无偏移数据
```
位置偏移: - (需要标准模板)
          (创建标准模板后可计算精确偏移)
```
- 明确说明需要标准模板
- 引导下一步操作

---

### 3. 覆盖率显示（始终可用）

**有标准模板（精确）**：
```
覆盖率: 94.2%  (3018 / 3200 像素)
```
- 基于标准像素数计算
- coverage = detected_pixels / standard_pixels
- 精度高

**无标准模板（降级）**：
```
覆盖率: 31.8%  低于60%阈值 ⚠️
        (基于ROI面积，建议创建标准模板以提高精度)
```
- 基于ROI面积计算
- coverage = detected_pixels / roi_area
- 精度较低，但仍有参考价值
- 明确说明计算基准

---

## 🎯 设计原则

### 1. 渐进式信息披露
- **第一层**：核心数据（能显示就显示）
- **第二层**：计算基准说明（相对ROI vs 相对标准）
- **第三层**：改进建议（创建标准模板）

### 2. 不隐藏有用信息
- ❌ 不显示"N/A"就结束
- ✅ 即使不精确也显示降级数据
- ✅ 明确标注数据来源和精度

### 3. 提供操作引导
- 不只是说"无标准模板"
- 提供可点击的创建入口
- 说明创建后的好处

---

## 📊 显示效果对比

### 场景1：完全没有标准模板

**原设计（不友好）**：
```
交并比(IoU): N/A (无标准模板)
位置偏移:    N/A (未配置标准模板)
```
❌ 问题：
- 信息量为零
- 不知道下一步怎么办
- 看起来像系统故障

**新设计（友好）**：
```
覆盖率:      31.8%  低于60%阈值 ⚠️
             (基于ROI面积，建议创建标准模板以提高精度)

交并比(IoU): - (需要标准模板)
             💡 创建左侧标准模板可提高检测精度

位置偏移:    1.24 mm  (+12.3px, -8.5px)
             (相对ROI中心，创建标准模板可精确测量)

综合得分:    0.530
```
✅ 优势：
- 仍然提供覆盖率和偏移量
- 明确说明数据来源
- 引导用户创建标准模板
- 信息完整，不让用户困惑

---

### 场景2：有标准模板（完整模式）

```
覆盖率:      94.2%  (3018 / 3200 像素)
交并比(IoU): 88.5%
位置偏移:    0.82 mm  (+8.2px, -6.1px)
综合得分:    0.912
```

---

## 🔧 技术实现

### JavaScript逻辑

```javascript
// IoU显示
if (iou !== null && iou !== undefined) {
  // 有标准模板：显示IoU
  显示IoU百分比 + 阈值对比
} else {
  // 无标准模板：引导创建
  显示 "- (需要标准模板)"
  显示 "💡 创建标准模板可提高精度" （可点击）
}

// 位置偏移显示
if (有标准模板 && alignment_metric === 'mm') {
  // 模式1：精确毫米测量
  显示毫米偏移 + 像素偏移 + 阈值对比
} else if (有偏移数据) {
  // 模式2：降级模式（相对ROI中心）
  显示偏移值
  显示 "(相对ROI中心，创建标准模板可精确测量)"
} else {
  // 模式3：完全无数据
  显示 "- (需要标准模板)"
  显示 "(创建标准模板后可计算精确偏移)"
}

// 覆盖率显示（始终显示）
if (有标准像素数) {
  // 精确模式
  显示 "覆盖率% (detected / standard 像素)"
} else {
  // 降级模式
  显示 "覆盖率% 低于XX%阈值"
  显示 "(基于ROI面积，建议创建标准模板以提高精度)"
}
```

### 创建引导函数

```javascript
function showStandardMaskGuide(side) {
  const sideName = side === 'left' ? '左侧' : '右侧';
  const message = `
💡 创建${sideName}标准模板可以：
✅ 计算精确的IoU（交并比）
✅ 基于标准质心计算偏移
✅ 提高覆盖率计算精度

📝 创建步骤：
1. 准备合格样本图片
2. 导入图片
3. 标定ROI
4. 保存到配方

是否前往配方管理页面？
  `;
  
  if (confirm(message)) {
    window.location.href = '/vision/recipes/';
  }
}
```

---

## 📝 用户体验流程

### 初次使用（无标准模板）

1. **检测结果**：看到降级模式显示
   ```
   覆盖率: 31.8% (基于ROI面积)
   IoU: - (需要标准模板) 💡 点此创建
   偏移: 1.24mm (相对ROI中心)
   ```

2. **点击引导链接**：弹出详细说明
   ```
   [弹窗]
   💡 创建标准模板可以：
   ✅ 精确IoU
   ✅ 精确偏移
   ✅ 精确覆盖率
   
   是否前往配方管理？
   ```

3. **前往配方管理**：创建标准模板

4. **再次检测**：看到完整模式显示
   ```
   覆盖率: 94.2% (3018 / 3200 像素)
   IoU: 88.5%
   偏移: 0.82mm (+8.2px, -6.1px)
   ```

---

## ✅ 设计优势总结

### vs "N/A"显示
| 对比项 | N/A显示 | 智能降级显示 |
|--------|---------|-------------|
| **信息量** | 无 | 有（降级数据） |
| **用户感知** | 像系统故障 | 功能完整但可优化 |
| **下一步** | 不知道 | 明确引导 |
| **覆盖率** | 不显示 | 始终显示 |
| **偏移量** | 不显示 | 显示（标注为相对ROI） |

### vs 隐藏指标
| 对比项 | 隐藏指标 | 智能降级显示 |
|--------|---------|-------------|
| **IoU行** | 不显示 | 显示但注明需要模板 |
| **偏移行** | 不显示 | 显示降级数据 |
| **一致性** | UI布局变化 | UI布局固定 |
| **引导性** | 无 | 有可点击引导 |

---

## 🎯 最终效果

**无标准模板时的完整显示**：
```
━━━ 左侧泡棉 ━━━
✗ 不合格 (缺陷: 覆盖不足)

覆盖率:      31.8%  低于60%阈值 ⚠️
             (基于ROI面积，建议创建标准模板以提高精度)

交并比(IoU): - (需要标准模板)
             💡 创建左侧标准模板可提高检测精度

位置偏移:    1.24 mm  (+12.3px, -8.5px)
             (相对ROI中心，创建标准模板可精确测量)

综合得分:    0.530
```

**优势**：
✅ 信息完整不缺失
✅ 降级模式明确标注
✅ 引导操作清晰可见
✅ 用户体验流畅友好

---

**完成时间**：2026-07-13
**设计状态**：✅ 已实现
