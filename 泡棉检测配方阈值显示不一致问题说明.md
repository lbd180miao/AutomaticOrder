# 泡棉检测配方阈值显示不一致问题说明

## 问题描述

在泡棉检测工作台页面（AutomaticOrder）中，前端显示的配方阈值与配方模块显示的数据不一致：

- **前端工作台显示**：覆盖率≥0.75, 得分≥0.8
- **配方模块实际数据**：可能是其他值（如覆盖率≥0.08）

## 根本原因

问题出在前端 JavaScript 代码中的**默认值硬编码**。

### 问题代码位置

**文件**: `templates/vision/foam_inspector_interactive.html`  
**行号**: 1180-1182

```javascript
const thresh = recipe.threshold_config || {};
document.getElementById('recipe-thresholds').textContent = 
  `覆盖率≥${thresh.minCoverage || 0.75}, 得分≥${thresh.minScore || 0.8}`;
```

### 问题分析

1. **后端数据结构** (`apps/vision/views.py` 343-348行):
   ```python
   threshold_config = body.get('threshold_config') or {
       'coverage_threshold': 0.75,   # 后端使用 coverage_threshold
       'score_threshold': 0.8,        # 后端使用 score_threshold
       'max_offset_px': 30
   }
   ```

2. **前端显示逻辑**存在两个问题：
   - **字段名不匹配**: 前端读取 `minCoverage` 和 `minScore`，但后端存储的是 `coverage_threshold` 和 `score_threshold`
   - **默认值错误**: 当字段不存在时，前端使用 `0.75` 和 `0.8` 作为默认值，这与实际的算法默认值（0.08）不匹配

3. **实际算法使用的阈值** (`apps/vision/algorithms/foam_inspector.py` 381行):
   ```python
   coverage_threshold = float(cfg.get('coverage_threshold', 0.08))
   ```
   算法默认使用 **0.08 (8%)** 作为覆盖率阈值，这是经过生产场景优化的值。

## 影响范围

- ❌ 前端工作台显示错误的阈值信息
- ✅ 实际检测逻辑正确（使用后端配方中的实际值或算法默认值 0.08）
- ⚠️ 用户可能被误导，以为使用的是 75% 覆盖率阈值

## 解决方案

### 方案1：修正前端字段名和默认值（推荐）

修改 `templates/vision/foam_inspector_interactive.html` 第 1180-1182 行：

```javascript
const thresh = recipe.threshold_config || {};
document.getElementById('recipe-thresholds').textContent = 
  `覆盖率≥${thresh.coverage_threshold || thresh.minCoverage || 0.08}, 得分≥${thresh.score_threshold || thresh.minScore || 0.8}`;
```

**优点**: 
- 兼容新旧字段名
- 默认值与算法一致
- 最小改动

### 方案2：统一后端字段名

修改后端代码，将 `coverage_threshold` 改为 `minCoverage`，使前后端字段名一致。

**缺点**: 
- 需要修改多处代码
- 需要数据库迁移
- 影响范围大

## 建议采取的行动

1. **立即修复**: 采用方案1修正前端显示逻辑
2. **后续优化**: 统一前后端字段命名规范
3. **文档更新**: 在配方管理文档中明确说明阈值字段的含义

## 相关文件

- 前端显示: `templates/vision/foam_inspector_interactive.html`
- 后端配方创建: `apps/vision/views.py` (343-360行)
- 算法逻辑: `apps/vision/algorithms/foam_inspector.py` (381行)
- 数据模型: `apps/vision/models.py` (310行)

## 阈值说明

### 覆盖率阈值 (coverage_threshold)
- **默认值**: 0.08 (8%)
- **含义**: 泡棉在ROI区域中的白色像素占比
- **调整建议**: 根据实际ROI大小和泡棉尺寸调整
  - ROI很大，泡棉占比小 → 降低至 0.03-0.06
  - ROI适中，泡棉占比高 → 使用 0.08-0.15
  - 严格检测场景 → 提高至 0.15-0.30

### 得分阈值 (score_threshold)
- **默认值**: 0.8 (80%)
- **含义**: 综合评分，考虑覆盖率、对齐度等因素
- **调整建议**: 一般保持 0.7-0.9 之间

---

**创建时间**: 2026-07-04  
**问题等级**: 中等（显示错误，但不影响检测逻辑）
