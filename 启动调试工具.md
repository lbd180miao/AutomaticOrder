# 采集点云API调试指南

## 问题现象
点击「采集点云」按钮时出现错误：
```
数据源 —网络请求失败：Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```

## 已完成的修复

### 1. 增强前端错误处理
修改了 `static/vision/js/rack_locator_workbench.js` 中的 `postJson` 函数：
- ✅ 添加Content-Type检查
- ✅ 提供详细的错误信息
- ✅ 在控制台记录完整的响应内容

### 2. 增强后端错误日志
修改了 `apps/vision/views.py` 中的API视图：
- ✅ `api_rack_location_workbench_capture` - 添加详细错误日志
- ✅ `api_vision_3d_capture` - 添加异常追踪

### 3. 创建调试工具
- ✅ 创建了 `/vision/test-capture-debug/` 调试页面
- ✅ 可以独立测试两个API端点

## 调试步骤

### 步骤1: 启动Django开发服务器
```bash
cd d:\workspace2\AutomaticOrder
python manage.py runserver
```

### 步骤2: 打开调试页面
在浏览器中访问：
```
http://127.0.0.1:8000/vision/test-capture-debug/
```

### 步骤3: 测试API
点击页面上的按钮测试两个API：
1. "测试 api_vision_3d_capture"
2. "测试 api_rack_location_workbench_capture"

查看日志输出，确认哪个API能正常工作。

### 步骤4: 检查实际工作台页面
访问：
```
http://127.0.0.1:8000/vision/rack-locator/
```

打开浏览器开发者工具（F12），切换到Console和Network标签：
1. 选择一个配方
2. 点击「采集点云」按钮
3. 在Network标签中查看请求的URL和响应
4. 在Console标签中查看详细错误信息

## 可能的问题原因

### 原因1: CSRF Token问题
**症状**: 403 Forbidden错误
**解决**: 检查页面中是否正确包含了 `{% csrf_token %}`

### 原因2: URL路由配置错误
**症状**: 404 Not Found错误
**解决**: 
- 检查 `templates/vision/rack_locator_panel.html` 中的URL配置
- 确认 `CFG.captureUrl` 或 `CFG.legacyCaptureUrl` 指向正确的端点

### 原因3: 中间件拦截
**症状**: 重定向到登录页面或其他HTML页面
**解决**: 检查Django设置中的中间件配置

### 原因4: 服务未启动或端口冲突
**症状**: 连接被拒绝
**解决**: 确认Django服务器正在运行

## 验证API是否正常工作

运行测试脚本：
```bash
cd d:\workspace2\AutomaticOrder
python test_capture_api.py
```

预期输出：
```
✓ HTTP状态码: 200
✓ Content-Type: application/json
✓ JSON解析成功!
  - success: True
  - pointcloud_token: vision/rack_workbench/2026/07/02/...
  - source: dm_camera
```

## 下一步

如果API测试通过，但前端仍然失败，请：
1. 检查浏览器Console中的详细错误日志
2. 检查Network标签中实际请求的URL
3. 确认前端配置文件中的URL是否正确
4. 检查是否有前端JavaScript错误阻止了请求发送
