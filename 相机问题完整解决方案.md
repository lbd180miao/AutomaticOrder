# 海康威视2D相机问题完整解决方案

## 当前问题诊断

### 错误信息
```
获取预览失败：Hik camera capture failed for CAM-INSPECT-FOAM-01 (PREVIEW): 
相机捕获失败: 连接相机失败: 相机 0 打开设备失败，错误码: -2147483115
```

### 错误码含义
- **-2147483115**: 设备被占用或无法访问
- 这是最常见的工业相机问题之一

## 快速解决方案（按优先级）

### 🚀 方案 1：一键修复（推荐）

双击运行：`一键修复相机问题.bat`

这个脚本会自动：
1. 关闭 MVS 相关进程
2. 等待系统释放资源
3. 运行诊断测试

### 🔧 方案 2：手动关闭占用程序

1. **关闭 MVS 软件**
   - 检查任务栏是否有 MVS 图标
   - 打开任务管理器 (Ctrl+Shift+Esc)
   - 结束所有 MVS 相关进程

2. **关闭其他 Python 进程**
   - 在任务管理器中查找 `python.exe`
   - 结束所有不相关的 Python 进程

3. **重启 Django 开发服务器**
   - 在终端按 `Ctrl+C` 停止服务器
   - 重新运行 `python manage.py runserver`

4. **刷新浏览器页面**
   - 按 `Ctrl+F5` 强制刷新

### 🔄 方案 3：使用子进程模式（已自动启用）

配置文件 `settings.py` 中已经设置：
```python
'RUN_IN_SUBPROCESS': True,  # 使用子进程模式，更稳定
```

这个模式会在独立的进程中运行相机操作，避免资源占用问题。

### 📡 方案 4：使用 IP 直连模式（网络相机推荐）

如果你的相机是网络相机（GigE），可以使用 IP 直连模式：

1. **查找相机 IP 地址**
   - 打开 MVS 软件
   - 在设备列表中查看相机 IP
   - 例如：`192.168.1.245`

2. **查找本机网卡 IP**
   - 打开命令提示符
   - 运行 `ipconfig`
   - 找到与相机同网段的网卡 IP
   - 例如：`192.168.1.123`

3. **修改配置文件** `AutomaticOrder/settings.py`：
   ```python
   'HIK_CAMERA': {
       'CAMERA_IP': '192.168.1.245',  # 改为你的相机 IP
       'PC_IP': '192.168.1.123',       # 改为你的本机 IP
       # ... 其他配置保持不变
   }
   ```

4. **重启 Django 服务器**

**优点：**
- IP 直连更稳定
- 不会被其他程序干扰
- 速度更快

### 🔌 方案 5：重置相机连接

1. **物理断开相机**
   - 拔掉相机的 USB 线或网线
   - 等待 10 秒

2. **重新连接**
   - 插回相机连接线
   - 等待系统识别设备

3. **测试连接**
   - 运行 `一键修复相机问题.bat`

### 💻 方案 6：设备管理器重置

1. **打开设备管理器**
   - Win+X → 设备管理器
   - 或搜索"设备管理器"

2. **找到相机设备**
   - 展开"图像设备"或"通用串行总线控制器"
   - 找到海康威视相机

3. **禁用并重新启用**
   - 右键 → 禁用设备
   - 等待 5 秒
   - 右键 → 启用设备

4. **测试**
   - 运行诊断脚本

### 🖥️ 方案 7：重启计算机（最彻底）

如果以上方法都不行：
1. 保存所有工作
2. 重启计算机
3. 重新测试

## 代码层面的改进（已完成）

### ✅ 已添加重试机制

在 `apps/devices/adapters/camera.py` 中添加了：
- 自动重试（默认 2 次）
- 重试延迟（默认 1 秒）
- 智能错误检测

### ✅ 已改进错误提示

前端会显示友好的错误信息，包括：
- 错误原因分析
- 具体解决步骤
- 相关文档链接

### ✅ 已添加配置选项

可以在 `settings.py` 中调整：
```python
'HIK_CAMERA': {
    'MAX_RETRIES': 2,        # 重试次数
    'RETRY_DELAY': 1,        # 重试延迟（秒）
    'RUN_IN_SUBPROCESS': True,  # 子进程模式
    # ...
}
```

## 测试步骤

### 1. 命令行测试
```bash
# 进入项目目录
cd D:\workspace2\AutomaticOrder

# 激活虚拟环境
.venv\Scripts\activate

# 运行诊断脚本
python fix_camera_access.py
```

### 2. Web 界面测试
1. 启动 Django 服务器
2. 打开浏览器访问 `http://127.0.0.1:8083/vision/foam-inspector/`
3. 点击"🔄 刷新预览"按钮
4. 查看结果

## 常见问题 FAQ

### Q1: 为什么重启 Django 服务器后就能用了？
**A:** Python 进程崩溃或异常退出时，可能没有正常释放相机资源。重启服务器会强制清理这些资源。

### Q2: 能否同时在 MVS 和 Web 界面使用相机？
**A:** 不能。工业相机同一时间只能被一个程序独占访问。使用 Web 界面时必须关闭 MVS。

### Q3: IP 直连模式和枚举模式有什么区别？
**A:** 
- **枚举模式**：自动搜索所有相机，灵活但可能被占用
- **IP 直连模式**：直接连接指定 IP 的相机，更快更稳定

### Q4: 错误码 -2147483115 和 -2147483646 有什么区别？
**A:**
- **-2147483115**: 设备被占用或无法打开
- **-2147483646**: 设备已打开，但无法获取图像（通常是触发模式问题）

### Q5: 如何确认相机已经释放？
**A:** 运行诊断脚本，如果能成功拍照并保存图片，说明相机已释放。

## 预防措施

### 1. 正确的开发流程
```
1. 启动 Django 服务器
2. 打开 Web 界面
3. 使用完毕后正常关闭浏览器标签
4. 停止 Django 服务器 (Ctrl+C)
```

### 2. 避免的操作
❌ 在使用 Web 界面时打开 MVS
❌ 强制终止 Python 进程
❌ 在拍照过程中刷新页面
❌ 同时打开多个浏览器标签访问相机

### 3. 最佳实践
✅ 使用 IP 直连模式（网络相机）
✅ 启用子进程模式 (RUN_IN_SUBPROCESS=True)
✅ 定期重启开发服务器
✅ 出现问题立即运行诊断脚本

## 进阶配置

### 调整重试参数
如果网络或硬件响应较慢，可以增加重试次数：

```python
# settings.py
'HIK_CAMERA': {
    'MAX_RETRIES': 3,        # 增加到 3 次
    'RETRY_DELAY': 2,        # 延迟增加到 2 秒
    # ...
}
```

### 日志调试
如果需要查看详细的错误信息：

```python
# settings.py
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 获取帮助

如果以上方法都无法解决问题：

1. **查看日志**
   - Django 控制台输出
   - 浏览器控制台 (F12)

2. **运行完整诊断**
   ```bash
   python test_camera_preview.py
   ```

3. **联系技术支持**
   - 海康威视官方支持
   - 提供完整的错误信息和日志

## 相关文件

- ✅ `fix_camera_access.py` - 诊断和修复脚本
- ✅ `一键修复相机问题.bat` - 一键修复批处理
- ✅ `相机配置说明.md` - 相机配置详细步骤
- ✅ `test_camera_preview.py` - 完整诊断脚本
- ✅ `相机问题完整解决方案.md` - 本文档

## 更新日志

- 2024-xx-xx: 添加重试机制
- 2024-xx-xx: 改进错误提示
- 2024-xx-xx: 创建一键修复工具
