# 手眼标定问题 - 快速修复指南 ⚡

## 🔴 遇到这个问题？

```
❌ 定位 NG · 请核查
3D料架定位配方缺少手眼标定参数

X 轴: 0.00mm
Y 轴: 0.00mm  
Z 轴: 0.00mm
Rz:   0.00deg
```

**👉 用这个方法 2 分钟解决！**

---

## ⚡ 超快速修复（3步搞定）

### Windows 用户

```bash
# 双击运行这个文件
quick_fix_hand_eye.bat
```

### Linux/Mac 用户

```bash
# 给脚本添加执行权限
chmod +x quick_fix_hand_eye.sh

# 运行脚本
./quick_fix_hand_eye.sh
```

### 完成后

1. **刷新浏览器** - 按 `Ctrl + F5`
2. **重新计算** - 选择配方 → 采集点云 → 计算偏差
3. **查看结果** - 应该能看到实际的偏差值了！✅

---

## 🛠️ 手动修复（如果脚本不可用）

### 方法 1：使用 Django Shell

```bash
# 1. 打开 Django Shell
python manage.py shell
```

```python
# 2. 运行以下代码
from apps.vision.models import RackLocationRecipe

for recipe in RackLocationRecipe.objects.all():
    config = recipe.hand_eye_config or {}
    config['skip_validation'] = True
    config['matrix'] = 'identity'
    config['note'] = '开发测试模式'
    recipe.hand_eye_config = config
    recipe.save()
    print(f"✅ {recipe.recipe_name}")

print("\n✅ 所有配方已更新！")
```

```bash
# 3. 退出 Shell
exit()
```

### 方法 2：通过管理页面

1. 访问 `/vision/rack-location/recipes/`
2. 编辑每个配方
3. 找到 `hand_eye_config` 字段
4. 修改为：
   ```json
   {
     "matrix": "identity",
     "skip_validation": true,
     "note": "开发测试模式"
   }
   ```
5. 保存

---

## ✅ 验证是否修复成功

### 检查 1：运行验证脚本

```bash
python check_hand_eye_config.py
```

**应该看到：**
```
✅ 有效配置: X 个 (100%)
❌ 无效配置: 0 个 (0%)
🎉 所有配方配置正确！
```

### 检查 2：在工作台测试

1. 打开 `3D 料架定位工作台`
2. 选择任一配方
3. 点击 `📡 采集点云`
4. 点击 `🎯 计算偏差`
5. **不应该再提示"缺少手眼标定参数"**
6. **应该显示实际的偏差值**（不是全0）

---

## ❓ 常见问题

### Q1: 修复后仍然提示缺少手眼标定？

**A:** 重启 Django 服务器

```bash
# Ctrl + C 停止服务
# 然后重新启动
python manage.py runserver
```

### Q2: 计算结果仍然全为 0？

**A:** 检查以下几点
- 点云采集是否成功（有深度图显示）
- ROI 是否正确绘制
- 标准坐标是否已设置

### Q3: 开发模式和生产模式有什么区别？

**A:** 

| 模式 | 配置 | 精度 | 适用场景 |
|------|------|------|---------|
| **开发模式** | `skip_validation: true` | 低（无坐标转换） | 功能测试 |
| **生产模式** | 真实手眼标定 | 高（±0.5mm） | 实际应用 |

---

## 📋 完整工具清单

### 修复工具
- ✅ `quick_fix_hand_eye.bat` - Windows 快速修复脚本
- ✅ `quick_fix_hand_eye.sh` - Linux/Mac 快速修复脚本
- ✅ `update_recipes_hand_eye_config.py` - 配方更新脚本

### 检查工具
- ✅ `check_hand_eye_config.py` - 配置检查脚本

### 文档
- ✅ `手眼标定问题-快速修复指南.md` - 本文档
- ✅ `手眼标定问题解决方案.md` - 详细解决方案
- ✅ `手眼标定问题修复总结.md` - 技术总结

---

## 🎯 一句话总结

**问题：** 配方缺少 `skip_validation` 标志  
**解决：** 运行 `quick_fix_hand_eye.bat`（或 `.sh`）  
**结果：** 可以正常计算定位偏差了！✅

---

## 💡 温馨提示

### 开发测试阶段
- ✅ 使用 `skip_validation: true` 模式
- ✅ 可以正常进行功能测试
- ⚠️ 结果可能有偏差（无坐标转换）

### 生产环境
- 📋 需要完成真实的手眼标定
- 📋 访问 `/vision/hand-eye/` 进行标定
- 📋 标定完成后配置 `calibration_id`

---

## 📞 需要帮助？

查看详细文档：
- **手眼标定问题解决方案.md** - 完整解决方案
- **手眼标定模块交付文档.md** - 手眼标定功能说明

---

**最后更新：** 2026-07-02  
**版本：** v1.0
