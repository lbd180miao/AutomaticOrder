# 🚀 3D ROI工作台 - 5分钟快速上手

## 访问地址

```
http://localhost:8000/vision/roi-3d-workbench/
```

---

## 📋 快速配置流程

### 步骤1：选择配方（10秒）

1. 打开ROI工作台
2. 从"当前配方"下拉列表选择配方
3. 自动显示：
   - 料架型号
   - 层号按钮（如第1层、第2层、第3层）

### 步骤2：配置第1层主ROI（30秒）

1. 确认"第1层"按钮已激活
2. 确认"主ROI" Tab已选中
3. 点击 **"📷 采集点云"** → 等待3秒
4. 点击 **"🤖 自动填充"** → ROI参数自动填充
5. 点击 **"👁️ 预览裁剪"** → 查看统计信息和补偿值
6. 点击 **"💾 保存ROI"** → 完成

### 步骤3：配置其他3种ROI（1分钟）

1. 点击 **"支撑面"** Tab
2. 点击 **"🤖 自动填充"** → 参数自动调整（Z范围缩小）
3. 点击 **"👁️ 预览裁剪"**
4. 点击 **"💾 保存ROI"**
5. 重复配置 **"前边缘"** 和 **"立柱"** Tab

### 步骤4：配置第2层（30秒）

1. 点击 **"第2层"** 按钮
2. 点击 **"📋 复制上层"** → 自动复制第1层ROI
3. 预览并保存所有4种ROI类型

### 步骤5：配置第3层（30秒）

1. 点击 **"第3层"** 按钮
2. 点击 **"📋 复制上层"**
3. 预览并保存

---

## 🎯 核心功能

### 1. 自动填充（最常用）

```
点击"🤖 自动填充" → ROI参数自动计算完成
```

**智能调整**：
- **主ROI**：全部点云范围
- **支撑面**：Z范围缩小到顶部10mm
- **前边缘**：Y范围缩小到前部50mm
- **立柱**：X范围缩小到侧边50mm

### 2. 预览裁剪

```
点击"👁️ 预览裁剪" → 查看统计 + 实时补偿
```

**显示信息**：
- ROI内有效点：18520
- 支撑面点数量：6200
- 裁剪状态：✅ OK
- **X轴补偿：-2.23mm**（绿色）
- **Y轴补偿：4.60mm**（黄色）
- **Z轴补偿：2.05mm**（绿色）

### 3. 复制上层（批量配置）

```
点击"📋 复制上层" → 一键复制上一层所有ROI
```

### 4. Canvas绘制（手动模式）

```
在点云视图上：
1. 点击并按住鼠标左键
2. 拖动绘制矩形框
3. 松开鼠标 → ROI参数自动填充
```

---

## 📊 界面说明

### 左侧面板

```
┌─────────────────────┐
│ 📋 配方信息          │
│  配方：料架A配方      │
│  型号：标准料架       │
│  层数：3层           │
├─────────────────────┤
│ 🔧 ROI参数          │
│  X Min: -200        │
│  X Max:  200        │
│  Y Min: -150        │
│  Y Max:  150        │
│  Z Min:  790        │
│  Z Max:  800        │
├─────────────────────┤
│ 📊 裁剪预览          │
│  有效点: 18520      │
│  状态: ✅ OK        │
└─────────────────────┘
```

### 右侧面板

```
┌─────────────────────────────┐
│ 🖼️ 点云显示区              │
│  ┌───────────────────────┐│
│  │  [Canvas画布]          ││
│  │  - 点云2D投影          ││
│  │  - ROI框显示           ││
│  │  - 可拖动绘制          ││
│  └───────────────────────┘│
├─────────────────────────────┤
│ 📈 实时补偿值（4个卡片）     │
│ ┌────┐ ┌────┐ ┌────┐ ┌───┐│
│ │X轴 │ │Y轴 │ │Z轴 │ │置信│
│ │-2.23│ │4.60│ │2.05│ │86%││
│ └────┘ └────┘ └────┘ └───┘│
└─────────────────────────────┘
```

---

## 🎨 状态颜色说明

### 补偿值颜色

| 颜色 | 范围 | 含义 |
|------|------|------|
| 🟢 绿色 | < 1mm | 良好 |
| 🟡 黄色 | 1-5mm | 警告 |
| 🔴 红色 | > 5mm | 超限 |

### 裁剪状态

| 状态 | 含义 |
|------|------|
| ✅ OK | 正常 |
| ⚠️ 点数过少 | < 1000点 |
| ⚠️ 点数过多 | > 100000点 |
| ❌ ROI过小 | 占比 < 1% |

---

## ⚡ 快捷操作

### 批量配置（推荐）

```
第1层：
  主ROI → 采集 → 自动填充 → 保存
  支撑面 → 自动填充 → 保存
  前边缘 → 自动填充 → 保存
  立柱 → 自动填充 → 保存

第2层：复制上层 → 保存全部
第3层：复制上层 → 保存全部

总耗时：约3分钟
```

### 精确配置（需要时）

```
1. 采集点云
2. 在Canvas上手动绘制ROI框
3. 微调X/Y/Z参数
4. 预览验证
5. 保存
```

---

## 🔧 按钮功能

| 按钮 | 功能 | 使用场景 |
|------|------|---------|
| 📷 采集点云 | 从相机采集当前层点云 | 首次配置 |
| 🤖 自动填充 | 基于点云自动计算ROI | 推荐使用 |
| 👁️ 预览裁剪 | 显示统计+实时补偿 | 验证配置 |
| 📋 复制上层 | 复制上一层ROI | 相似层结构 |
| 🔄 重置 | 重置为默认值 | 重新开始 |
| 💾 保存ROI | 保存当前配置 | 完成配置 |

---

## ❓ 常见问题

**Q: 点云采集失败？**
```
检查：
1. 相机设备是否连接
2. 配方中是否配置了相机
3. 网络连接是否正常
```

**Q: 补偿值不显示？**
```
解决：
1. 先点击"预览裁剪"
2. 预览时会自动计算补偿值
```

**Q: ROI参数不合适？**
```
调整：
1. 可以直接修改6个参数框
2. 或在Canvas上重新绘制
3. 然后点击"预览裁剪"验证
```

**Q: 如何快速配置多层？**
```
方法：
1. 配置完第1层
2. 从第2层开始使用"复制上层"
3. 批量保存
```

---

## 💡 最佳实践

### 1. 首次配置

```
1. 采集点云（看到点云显示）
2. 自动填充（参数自动生成）
3. 预览裁剪（验证效果）
4. 查看补偿值（确认合理）
5. 保存ROI
```

### 2. 批量配置

```
第1层配置好后：
- 第2层：复制 → 保存
- 第3层：复制 → 保存
- 效率提升80%
```

### 3. 微调参数

```
自动填充后：
- 手动微调Z范围（支撑面）
- 手动微调Y范围（前边缘）
- 手动微调X范围（立柱）
```

---

## 🎯 操作提示

### 左侧面板操作流程

```
1. 选配方 ↓
2. 选层号 ↓
3. 选ROI类型 ↓
4. 填充参数 ↓
5. 预览验证 ↓
6. 保存配置 ✓
```

### 右侧面板观察重点

```
1. Canvas显示点云
2. ROI框位置正确
3. 补偿值在合理范围
4. 置信度足够高
```

---

## 📱 界面布局提示

```
┌──────────────────────────────────────┐
│         顶部：页面标题                  │
├──────────────┬───────────────────────┤
│ 左(35%)      │ 右(65%)                │
│              │                       │
│ 配置参数     │ 可视化显示             │
│              │                       │
│ 1.选择配方   │ 1.点云Canvas           │
│ 2.选择层号   │ 2.ROI框显示            │
│ 3.选择类型   │ 3.实时补偿             │
│ 4.填充参数   │ 4.结果卡片             │
│ 5.预览裁剪   │                       │
│ 6.保存ROI    │                       │
└──────────────┴───────────────────────┘
```

---

**🎉 开始使用ROI工作台，享受可视化配置体验！**

**关键提示**：
- ✅ 推荐使用"自动填充"功能
- ✅ 使用"复制上层"快速配置
- ✅ 预览裁剪查看实时补偿值
- ✅ 颜色编码帮助判断状态

**平均配置时间**：3-5分钟/配方
