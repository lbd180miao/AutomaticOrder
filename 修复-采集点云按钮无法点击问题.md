# 修复：3D 料架定位工作台 - 采集点云按钮无法点击

## 🔴 问题现象

用户在打开 **3D 料架定位工作台** 页面后，无法点击 **「📡 采集点云」** 按钮，按钮处于禁用状态。

## 🔍 问题原因

在 `rack_locator_workbench.js` 的 `refreshActionState()` 函数中，只配置了以下按钮的启用/禁用逻辑：

```javascript
function refreshActionState() {
  setButton('btn-auto-align', Boolean(state.token));
  setButton('btn-redraw', Boolean(state.token));
  setButton('btn-calculate', Boolean(state.token));
  setButton('btn-save-roi', Boolean(state.alignmentToken));
  setButton('btn-write-plc', Boolean(state.lastResultId && state.lastResultOk));
}
```

**缺少了对 `btn-capture`（采集点云按钮）的启用设置**，导致：
1. 页面初始化时，按钮可能被设置为禁用状态
2. `refreshActionState()` 函数在页面加载和状态变化时被调用
3. 但该函数从未主动启用 `btn-capture` 按钮
4. **结果：按钮始终处于禁用状态，用户无法点击**

## ✅ 解决方案

在 `refreshActionState()` 函数中添加对采集点云按钮的启用设置：

```javascript
function refreshActionState() {
  // 采集点云按钮：始终可用（只要页面正常加载）
  setButton('btn-capture', true);
  setButton('btn-auto-align', Boolean(state.token));
  setButton('btn-redraw', Boolean(state.token));
  setButton('btn-calculate', Boolean(state.token));
  setButton('btn-save-roi', Boolean(state.alignmentToken));
  setButton('btn-write-plc', Boolean(state.lastResultId && state.lastResultOk));
}
```

## 🔧 修改文件

**文件路径**：`d:\workspace2\AutomaticOrder\static\vision\js\rack_locator_workbench.js`

**修改内容**：在第 173-174 行添加了采集点云按钮的启用逻辑

## 🧪 测试步骤

1. **清除浏览器缓存**
   - 按 `Ctrl + F5` 强制刷新页面（清除 JS 缓存）
   - 或使用浏览器开发者工具（F12）→ Network 标签 → 勾选 "Disable cache"

2. **访问工作台页面**
   ```
   http://localhost:8000/vision/rack-location/workbench/
   ```

3. **验证按钮状态**
   - ✅ 页面加载完成后，**「📡 采集点云」** 按钮应该是**可点击状态**
   - ✅ 按钮背景色应为蓝色（`var(--info)`），不是灰色
   - ✅ 鼠标悬停时应有交互效果

4. **验证功能**
   - 选择一个配方（下拉框）
   - 点击 **「📡 采集点云」** 按钮
   - 应该能成功触发点云采集，显示加载提示

## 📋 相关按钮的启用逻辑

| 按钮 | 启用条件 | 说明 |
|-----|---------|------|
| 📡 采集点云 | 始终可用 | 用户可随时采集点云 |
| 自动对齐 | `state.token` 存在 | 需要先采集点云 |
| 重画 ROI | `state.token` 存在 | 需要先采集点云 |
| 🎯 计算偏差 | `state.token` 存在 | 需要先采集点云 |
| 保存 ROI | `state.alignmentToken` 存在 | 需要先自动对齐 |
| 写入 PLC | `state.lastResultId` 且 `state.lastResultOk` | 需要先完成成功的定位计算 |

## 🎯 预期效果

- ✅ 用户打开工作台页面后，立即可以点击 **「📡 采集点云」** 按钮
- ✅ 无需任何前置操作，按钮处于可用状态
- ✅ 配合配方选择功能，用户可以直接开始工作流程：
  1. 选择配方（下拉框）
  2. 点击 **采集点云**
  3. ROI 自动显示或手动绘制
  4. 点击 **计算偏差**
  5. 查看结果

## 📝 其他说明

- 此修复不影响其他按钮的逻辑
- 其他按钮仍然按照原有的状态条件启用/禁用
- 修复后，工作台的完整工作流程恢复正常

---

**修复完成时间**：2026-07-02  
**修复文件**：`static/vision/js/rack_locator_workbench.js`  
**修复行数**：+2 行（第 174-175 行）
