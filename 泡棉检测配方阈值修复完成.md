# 泡棉检测配方阈值显示不一致问题 - 修复完成

## 问题回顾

在泡棉检测工作台页面（AutomaticOrder）中，前端显示的配方阈值与配方模块实际使用的阈值不一致：

- **错误显示**：覆盖率≥0.75 (75%)
- **实际算法**：覆盖率≥0.08 (8%)

这个差异会误导用户，以为系统使用了非常严格的检测标准（75%），但实际使用的是适合大ROI场景的宽松标准（8%）。

## 根本原因

1. **前端JavaScript硬编码错误的默认值**: 当配方数据中缺少阈值字段时，使用 0.75 而不是 0.08
2. **后端创建配方时使用错误的默认值**: `apps/vision/views.py` 中创建配方时默认设置为 0.75
3. **前后端字段名不一致**: 前端曾尝试读取 `minCoverage`，但后端存储的是 `coverage_threshold`

## 修复内容

### ✅ 1. 泡棉检测工作台前端显示 (foam_inspector_interactive.html)

**文件**: `templates/vision/foam_inspector_interactive.html`  
**修改位置**: 第 1180-1186 行

```javascript
// 修复前：
const thresh = recipe.threshold_config || {};
document.getElementById('recipe-thresholds').textContent = 
  `覆盖率≥${thresh.minCoverage || 0.75}, 得分≥${thresh.minScore || 0.8}`;

// 修复后：
const thresh = recipe.threshold_config || {};
// 兼容新旧字段名：coverage_threshold(新) 或 minCoverage(旧)
// 默认值与算法保持一致：覆盖率 0.08 (8%), 得分 0.8 (80%)
const coverageThreshold = thresh.coverage_threshold || thresh.minCoverage || 0.08;
const scoreThreshold = thresh.score_threshold || thresh.minScore || 0.8;
document.getElementById('recipe-thresholds').textContent = 
  `覆盖率≥${coverageThreshold}, 得分≥${scoreThreshold}`;
```

### ✅ 2. 配方管理页面显示 (recipe_management.html)

**文件**: `templates/vision/recipe_management.html`  
**修改位置**: 
- 第 735 行（配方卡片显示）
- 第 799 行（编辑表单输入框默认值）

```javascript
// 修复前：
阈值: 覆盖率 ≥ ${thresh.coverage_threshold || 0.75}, 得分 ≥ ${thresh.score_threshold || 0.8}
<input type="number" id="thresh-cov" value="${thresh.coverage_threshold || 0.75}" ...>

// 修复后：
阈值: 覆盖率 ≥ ${thresh.coverage_threshold || 0.08}, 得分 ≥ ${thresh.score_threshold || 0.8}
<input type="number" id="thresh-cov" value="${thresh.coverage_threshold || 0.08}" ...>
```

### ✅ 3. 后端配方创建默认值 (views.py)

**文件**: `apps/vision/views.py`  
**修改位置**: 第 343-348 行

```python
# 修复前：
threshold_config = body.get('threshold_config') or {
    'coverage_threshold': 0.75,
    'score_threshold': 0.8,
    'max_offset_px': 30
}

# 修复后：
# 默认阈值配置（与算法默认值保持一致）
threshold_config = body.get('threshold_config') or {
    'coverage_threshold': 0.08,  # 8% 覆盖率，适配大ROI场景
    'score_threshold': 0.8,      # 80% 综合得分
    'max_offset_px': 30
}
```

## 修复效果

### 修复前
- 工作台显示: 覆盖率≥0.75, 得分≥0.8
- 配方管理显示: 覆盖率≥0.75, 得分≥0.8
- 后端创建配方: coverage_threshold = 0.75
- 实际检测使用: coverage_threshold = 0.08 (算法默认值)
- ❌ **前后端不一致，误导用户**

### 修复后
- 工作台显示: 覆盖率≥0.08, 得分≥0.8
- 配方管理显示: 覆盖率≥0.08, 得分≥0.8
- 后端创建配方: coverage_threshold = 0.08
- 实际检测使用: coverage_threshold = 0.08
- ✅ **前后端完全一致，用户看到的就是实际使用的**

## 兼容性说明

✅ **向下兼容**: 修复后的代码同时支持新旧字段名
- 优先读取 `coverage_threshold` (标准字段名)
- 兼容 `minCoverage` (旧字段名)
- 都不存在时使用 0.08 作为默认值

✅ **已有配方不受影响**: 
- 如果配方中已经保存了 `coverage_threshold: 0.75`，将继续使用 0.75
- 只有新创建的配方或缺少阈值配置的配方才使用新默认值 0.08

## 测试建议

### 1. 工作台页面测试
1. 打开泡棉检测工作台
2. 检查「当前配方」卡片中显示的阈值
3. 确认显示为「覆盖率≥0.08, 得分≥0.8」（对于新配方）

### 2. 配方管理页面测试
1. 进入配方管理页面
2. 查看现有配方卡片，确认阈值显示正确
3. 点击「编辑」配方，检查输入框默认值
4. 创建新配方，确认默认阈值为 0.08

### 3. 实际检测测试
1. 使用新配方进行泡棉检测
2. 对比检测结果是否符合预期
3. 调整阈值参数，验证修改生效

## 阈值调整指南

### 覆盖率阈值 (coverage_threshold)

#### 推荐值范围
- **0.03 - 0.06**: ROI很大，泡棉占比极小（<5%）
- **0.08 - 0.15**: ROI适中，泡棉占比正常（8%-15%） ← **当前默认值**
- **0.20 - 0.30**: ROI较小，泡棉占比大（20%-30%）
- **0.40 - 0.70**: 严格检测，泡棉应该占据大部分ROI

#### 如何选择合适的阈值？

1. **查看实际覆盖率**:
   - 在检测结果中查看「覆盖率」数值
   - 对正常贴附的泡棉进行多次检测
   - 取平均覆盖率的 60-80% 作为阈值

2. **逐步调整**:
   - 从 0.08 开始
   - 如果误报率高（正常泡棉被判为NG）→ 降低阈值
   - 如果漏检率高（缺失泡棉未被检出）→ 提高阈值

3. **ROI优化**:
   - 如果覆盖率太低（<5%）→ 考虑缩小ROI框
   - 如果覆盖率太高（>80%）→ 考虑放大ROI框，留出对齐余量

### 得分阈值 (score_threshold)

- **默认值**: 0.8 (80%)
- **推荐范围**: 0.7 - 0.9
- **调整原则**: 
  - 一般不需要调整
  - 如果需要更严格的综合判定 → 提高至 0.85-0.9
  - 如果需要更宽松的判定 → 降低至 0.7-0.75

## 相关文档

- 问题分析: `泡棉检测配方阈值显示不一致问题说明.md`
- 算法说明: `apps/vision/algorithms/foam_inspector.py` (第11-19行注释)
- 配方模型: `apps/vision/models.py` (VisionRecipe 类)

---

**修复时间**: 2026-07-04  
**修复状态**: ✅ 已完成  
**影响范围**: 前端显示 + 后端默认值  
**向下兼容**: ✅ 是
