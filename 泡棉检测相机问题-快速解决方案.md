# 泡棉检测相机无法拍照 - 快速解决方案

## 🔴 问题确认

✅ **已确认问题**：点击"拍照检测"按钮后相机无法拍照

## 🎯 根本原因（已诊断）

通过诊断脚本发现了**两个主要问题**：

### 问题 1：网络连接异常 ⭐️ 主要问题
```
❌ 相机 169.254.160.253 无法 ping 通
❌ 本机 IP 169.254.160.95 未配置
```

### 问题 2：相机访问权限
```
错误码: -2147483115 (设备被占用或权限不足)
```

### 历史记录显示
- ✅ 最后一次成功捕获：2026-06-24 08:29:16
- ⚠️  之后有 18 次失败的捕获尝试

## 🚀 快速解决步骤

### 第一步：配置网络（必须）⭐️

#### 1. 找到连接相机的网卡
```
1. 按 Win + R，输入 ncpa.cpl，回车
2. 找到连接相机的网卡（通常是"以太网"或标注为相机的网卡）
```

#### 2. 配置网卡 IP
```
1. 右键网卡 -> 属性
2. 双击「Internet 协议版本 4 (TCP/IPv4)」
3. 选择「使用下面的 IP 地址」
4. 填写：
   IP 地址：    169.254.160.95
   子网掩码：   255.255.0.0
   默认网关：   （留空）
5. 点击确定
```

#### 3. 验证网络连通
```cmd
# 打开命令提示符，输入：
ping 169.254.160.253

# 应该看到：
# 来自 169.254.160.253 的回复: 字节=32 时间<1ms TTL=64
```

### 第二步：释放相机占用

#### 1. 关闭可能占用相机的程序
```
1. 按 Ctrl + Shift + Esc 打开任务管理器
2. 在「进程」标签页中，查找并结束以下进程：
   □ MVS Client
   □ MVSClient.exe  
   □ Hikrobot.exe
   □ 其他视觉检测软件
3. 如果找不到这些进程，跳过此步骤
```

#### 2. 重启 Django 服务器
```
1. 在 Django 服务器的命令行窗口按 Ctrl + C 停止
2. 重新运行：python manage.py runserver
```

### 第三步：验证修复

#### 运行诊断脚本
```cmd
cd d:\workspace2\AutomaticOrder
.venv\Scripts\python.exe diagnose_hik_camera.py
```

**期望看到**：
```
【4】检查网络配置
----------------------------------------------------------------------
✅ 相机 169.254.160.253 网络可达
✅ 找到配置的本机 IP: 169.254.160.95

【6】尝试连接相机并拍照
----------------------------------------------------------------------
✅ 相机捕获成功！
```

#### 在网页上测试
```
1. 访问：http://127.0.0.1:8000/vision/foam-inspector-interactive/
2. 点击「刷新预览」
3. 点击「拍照检测」
4. 应该能看到检测结果
```

## 🔧 如果问题仍然存在

### 方案 A：相机电源重启
```
1. 断开相机电源（拔掉电源适配器）
2. 等待 10 秒
3. 重新接通电源
4. 等待相机初始化完成（约 30-60 秒，直到指示灯稳定）
5. 重新运行诊断脚本
```

### 方案 B：检查相机物理连接
```
1. 检查相机电源指示灯是否亮起
2. 检查网线是否插紧（两端都检查）
3. 尝试更换网线
4. 如果使用交换机，检查交换机指示灯
```

### 方案 C：使用 MVS Client 测试
```
1. 打开海康威视 MVS Client 软件
2. 点击「设备列表」-> 「刷新设备列表」
3. 查看是否能发现相机
4. 如果能发现：
   - 尝试连接相机并获取图像
   - 如果成功，关闭 MVS Client
   - 重启 Django 服务器
   - 再次测试
5. 如果不能发现：
   - 说明是硬件/网络问题
   - 按方案 A 重启相机
   - 检查网络配置
```

### 方案 D：临时关闭防火墙测试
```
1. 控制面板 -> Windows Defender 防火墙
2. 点击「启用或关闭 Windows Defender 防火墙」
3. 选择「关闭 Windows Defender 防火墙」（测试用）
4. 重新运行诊断脚本
5. 如果可以工作：
   - 重新开启防火墙
   - 添加防火墙例外：
     * 允许程序：python.exe（Django 服务器）
     * 允许端口：GigE Vision (UDP 3956)
```

## 📊 诊断命令速查

```cmd
# 1. 完整诊断
cd d:\workspace2\AutomaticOrder
.venv\Scripts\python.exe diagnose_hik_camera.py

# 2. 快速测试拍照
.venv\Scripts\python.exe test_camera_capture.py

# 3. 检查网络配置
ipconfig

# 4. 测试相机连通性
ping 169.254.160.253

# 5. 查看网卡列表
ncpa.cpl
```

## ✅ 问题解决检查清单

完成以下所有项目，问题应该就能解决：

- [ ] 网卡 IP 已配置为 169.254.160.95
- [ ] 能够 ping 通相机 169.254.160.253
- [ ] 已关闭所有可能占用相机的程序（MVS Client 等）
- [ ] Django 服务器已重启
- [ ] 诊断脚本显示「相机捕获成功」
- [ ] 网页上点击「拍照检测」能正常工作

## 🎓 预防措施

为避免问题再次发生：

1. **不要同时运行多个相机程序**
   - 使用检测程序时，不要开启 MVS Client
   - 使用 MVS Client 调试时，先停止 Django 服务器

2. **网络配置固定**
   - 不要频繁修改网卡 IP 配置
   - 如果有多张网卡，给相机网卡重命名（如"相机网卡"）

3. **定期重启相机**
   - 建议每周重启一次相机电源
   - 长时间运行可能导致相机状态异常

4. **正常关闭程序**
   - Django 服务器停止时使用 Ctrl+C，不要强制终止窗口
   - 这样可以正确释放相机资源

## 📞 需要帮助？

如果尝试以上所有方案后问题仍然存在，请准备以下信息：

1. 诊断脚本的完整输出
2. `ipconfig` 命令的输出
3. MVS Client 能否发现相机
4. 相机型号和 SDK 版本
5. 错误截图

## 🎯 总结

**最可能的原因**：网络配置问题（本机 IP 未配置）

**解决概率**：
- 配置网络 IP → 90% 概率解决
- 配置网络 + 重启相机 → 95% 概率解决
- 配置网络 + 释放占用 + 重启相机 → 99% 概率解决

**预计耗时**：5-10 分钟

祝你顺利解决问题！🚀
