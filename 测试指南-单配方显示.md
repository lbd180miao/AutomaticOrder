# 3D 料架定位工作台 - 单配方显示测试指南

## 测试前准备

1. **清空浏览器缓存**
   ```
   Chrome/Edge: Ctrl + Shift + Delete
   或直接使用 Ctrl + F5 强制刷新页面
   ```

2. **确认数据库中有配方**
   - 至少创建 2-3 个配方用于测试自动切换
   - 配方应该有不同的 POS 和层号

3. **访问页面**
   ```
   http://localhost:8000/vision/rack-location/workbench/
   ```

---

## 测试用例

### ✅ 测试 1: 页面初始化

**操作步骤**:
1. 打开工作台页面
2. 观察配方选择区域

**预期结果**:
- ✓ 看到一个下拉选择器，显示格式：`POS1 · L1 - 配方名称`
- ✓ 第一个配方自动被选中
- ✓ 下方显示配方信息卡片（蓝色渐变背景）
- ✓ 卡片显示：标准坐标、料架侧、允许偏差、置信度阈值
- ✓ 状态栏显示：`已选配方：xxx（POS1 层1），可采集点云。`

---

### ✅ 测试 2: 手动切换配方

**操作步骤**:
1. 点击配方下拉框
2. 选择另一个配方

**预期结果**:
- ✓ 下拉框的选项正确切换
- ✓ 配方信息卡片内容自动更新
- ✓ 标题显示新配方名称
- ✓ 右上角徽章显示新的 POS 和层号
- ✓ 标准坐标、允许偏差等数据正确更新
- ✓ 状态栏更新为新配方信息

---

### ✅ 测试 3: 配方 ROI 自动加载

**操作步骤**:
1. 选择一个已保存 ROI 的配方
2. 观察点云画布区域

**预期结果**:
- ✓ 如果该配方保存过 ROI，ROI 参数自动填充到高级选项中
- ✓ 3D ROI 输入框（x_min/max, y_min/max, z_min/max）显示配方的值
- ✓ 状态栏提示：`已加载配方 ROI，采集点云后将自动显示框选区域。`

---

### ✅ 测试 4: 采集点云后 ROI 自动显示

**操作步骤**:
1. 选择配方
2. 点击「📡 采集点云」
3. 等待点云图像加载完成

**预期结果**:
- ✓ 点云伪彩图显示在画布上
- ✓ 如果配方有保存的 2D ROI，绿色框自动显示在画布上
- ✓ 左下角显示 ROI 坐标信息
- ✓ 状态栏提示：`点云已采集，配方 ROI 已自动显示。`

---

### ✅ 测试 5: 高级选项折叠

**操作步骤**:
1. 点击「▶ 高级选项」按钮

**预期结果**:
- ✓ 高级选项区域展开，显示：
  - 定位类型（整体定位/层定位）
  - Layer Index（0-3）
  - POS 输入框
  - 料架侧（左料架/右料架）
  - 6 个 3D ROI 输入框（x/y/z min/max）
- ✓ 按钮图标变为 `▼`
- ✓ 再次点击，区域收起，图标变回 `▶`

---

### ✅ 测试 6: 计算偏差

**操作步骤**:
1. 选择配方
2. 采集点云
3. 绘制 ROI 或使用配方 ROI
4. 点击「🎯 计算偏差」
5. 等待计算完成

**预期结果**:
- ✓ 右侧显示计算结果（OK/NG）
- ✓ X/Y/Z 轴偏差、Rz 旋转角度正确显示
- ✓ 置信度进度条正确显示
- ✓ 结果图显示（带 ROI 框的点云）

---

### ✅ 测试 7: 计算后自动切换配方 ⭐

**操作步骤**:
1. 选择第一个配方（例如 POS1 L1）
2. 采集点云 → 绘制 ROI → 计算偏差
3. 等待计算完成
4. 观察配方下拉框

**预期结果**:
- ✓ 下拉框自动切换到下一个配方（例如 POS1 L2）
- ✓ 配方信息卡片自动更新
- ✓ 新配方的 ROI 自动加载（如果有保存）
- ✓ 状态栏显示新配方信息

**边界情况**:
- 如果当前是最后一个配方，应该循环回到第一个配方

---

### ✅ 测试 8: 多次计算循环测试

**操作步骤**:
1. 从第一个配方开始
2. 连续执行 3 次：采集点云 → 计算偏差
3. 观察配方切换是否正确

**预期结果**:
- ✓ 第 1 次计算后：切换到配方 2
- ✓ 第 2 次计算后：切换到配方 3
- ✓ 第 3 次计算后：切换到配方 1（循环）

---

### ✅ 测试 9: 料架侧显示

**操作步骤**:
1. 创建配方时设置不同的 `rack_side`：
   - 配方 A: LEFT（左料架）
   - 配方 B: RIGHT（右料架）
   - 配方 C: BOTH（左右料架）
2. 依次选择这些配方

**预期结果**:
- ✓ 配方 A 显示：`料架侧: 左料架`
- ✓ 配方 B 显示：`料架侧: 右料架`
- ✓ 配方 C 显示：`料架侧: 左右料架`

---

### ✅ 测试 10: 手眼标定兼容性

**操作步骤**:
1. 选择配方（确保 `hand_eye_config` 包含 `skip_validation: true`）
2. 采集点云 → 绘制 ROI → 计算偏差

**预期结果**:
- ✓ 不再提示"缺少手眼标定参数"
- ✓ 计算正常完成
- ✓ 结果正确显示（即使 X/Y/Z/Rz 可能为 0）

---

## 常见问题排查

### 问题 1: 页面显示旧版卡片布局

**原因**: 浏览器缓存未清除

**解决**:
```
1. 按 Ctrl + F5 强制刷新
2. 或清空浏览器缓存后重新打开
```

---

### 问题 2: 配方信息卡片不更新

**排查步骤**:
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签页
3. 切换配方，查看是否有 JavaScript 错误
4. 检查配方选项的 `data-*` 属性是否完整

**可能原因**:
- 配方数据缺少某些字段
- JavaScript 函数执行出错

---

### 问题 3: 计算后不自动切换配方

**排查步骤**:
1. 检查浏览器控制台是否有错误
2. 确认 `selectNextRecipe()` 函数被正确调用
3. 检查下拉框是否有多个有效选项

**可能原因**:
- 只有 1 个配方（无法切换）
- JavaScript 函数执行失败

---

### 问题 4: ROI 不自动显示

**排查步骤**:
1. 检查配方是否保存过 ROI
2. 查看浏览器控制台的网络请求
3. 确认 `loadRecipeRoi()` 函数被调用

**可能原因**:
- 配方未保存 ROI 数据
- API 请求失败
- ROI 数据格式不正确

---

## 性能测试

### 测试场景: 大量配方

**操作**:
1. 创建 20+ 个配方
2. 打开工作台页面
3. 测试下拉框性能

**预期**:
- ✓ 下拉框流畅打开
- ✓ 选项滚动流畅
- ✓ 切换配方响应迅速（< 100ms）

---

## 回归测试

确保旧功能仍然正常工作：

- [ ] 重画 ROI
- [ ] 自动对齐
- [ ] 保存 ROI
- [ ] 保存结果到数据库
- [ ] 写入 PLC
- [ ] 自动拍照计算
- [ ] 历史记录查询

---

## 测试完成清单

- [ ] 测试 1: 页面初始化
- [ ] 测试 2: 手动切换配方
- [ ] 测试 3: 配方 ROI 自动加载
- [ ] 测试 4: 采集点云后 ROI 自动显示
- [ ] 测试 5: 高级选项折叠
- [ ] 测试 6: 计算偏差
- [ ] 测试 7: 计算后自动切换配方 ⭐
- [ ] 测试 8: 多次计算循环测试
- [ ] 测试 9: 料架侧显示
- [ ] 测试 10: 手眼标定兼容性

---

## 测试报告模板

```
测试日期: ___________
测试人员: ___________
浏览器: Chrome / Edge / Firefox (选择一个)
测试环境: 开发 / 测试 / 生产 (选择一个)

通过测试: _____ / 10
失败测试: _____

详细说明:
_______________________________
_______________________________
_______________________________
```

---

## 联系方式

如有问题，请联系开发团队或查看以下文档：
- `3D料架定位工作台-单配方显示改造完成.md`
- `料架定位工作台功能增强说明.md`
- `手眼标定问题修复总结.md`
