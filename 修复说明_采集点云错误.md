# 采集点云功能修复说明

## 📋 问题描述

**错误信息**：
```
数据源 —网络请求失败：Unexpected token '<', "<!DOCTYPE "... is not valid JSON
```

**发生场景**：
在「3D 料架定位工作台 - AutomaticOrder」中点击「采集点云」按钮时失败

## 🔍 问题分析

这个错误表明：
1. 前端期望收到JSON响应
2. 实际收到的是HTML页面（通常是错误页面）
3. 当尝试将HTML解析为JSON时失败

常见原因：
- URL路由404（请求地址错误）
- CSRF验证失败（返回403页面）
- 服务器内部错误（返回500页面）
- 中间件重定向（如未登录重定向到登录页）

## ✅ 已完成的修复

### 1. 增强前端错误处理 (`static/vision/js/rack_locator_workbench.js`)

**修改前**：
```javascript
async function postJson(url, body) {
    const res = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'X-CSRFToken': csrf() },
        body: JSON.stringify(body || {}),
    });
    return res.json();  // ❌ 如果响应不是JSON，这里会抛出难以理解的错误
}
```

**修改后**：
```javascript
async function postJson(url, body) {
    const res = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'X-CSRFToken': csrf() },
        body: JSON.stringify(body || {}),
    });
    
    // ✅ 检查响应的Content-Type
    const contentType = res.headers.get('content-type');
    if (!contentType || !contentType.includes('application/json')) {
        const text = await res.text();
        console.error('服务器返回非JSON响应:', {
            status: res.status,
            statusText: res.statusText,
            contentType: contentType,
            responseText: text.substring(0, 500)
        });
        
        if (!res.ok) {
            throw new Error(`服务器错误 (${res.status}): ${res.statusText}。可能是URL路径错误或权限问题。`);
        }
        
        throw new Error('服务器返回了HTML页面而不是JSON数据。请检查API URL配置是否正确。');
    }
    
    return res.json();
}
```

**优势**：
- ✅ 提供清晰的错误信息
- ✅ 在控制台记录响应详情，便于调试
- ✅ 区分不同类型的错误（4xx, 5xx, 格式错误）

### 2. 增强后端错误日志 (`apps/vision/views.py`)

**为两个API端点添加了详细的错误日志**：

#### `api_rack_location_workbench_capture`：
```python
@require_POST
def api_rack_location_workbench_capture(request):
    """工作台「采集点云」：真实 3D 相机优先，离线回退模拟，返回预览图与 token。"""
    try:
        data = _request_data(request)
        payload = RackLocationService().capture_workbench(recipe_id=data.get('recipe_id') or None)
        return JsonResponse({'success': True, **payload})
    except json.JSONDecodeError as exc:
        return JsonResponse({'success': False, 'error': f'JSON解析失败: {exc}'}, status=400)
    except Exception as exc:
        import traceback
        error_detail = traceback.format_exc()
        print(f"采集点云错误: {error_detail}")  # ✅ 输出到控制台便于调试
        return JsonResponse({'success': False, 'error': f'相机采集失败: {exc}'}, status=400)
```

#### `api_vision_3d_capture`：
```python
@require_POST
def api_vision_3d_capture(request):
    """3D视觉采集点云API"""
    try:
        data = _request_data(request)
        payload = Rack3DLocator().capture(...)
        return _api3d_success(payload)
    except json.JSONDecodeError as exc:
        import logging
        logger = logging.getLogger(__name__)
        logger.error(f"JSON解析失败 in api_vision_3d_capture: {exc}")
        return _api3d_error(f'请求数据格式错误: {exc}')
    except Exception as exc:
        import logging
        import traceback
        logger = logging.getLogger(__name__)
        logger.error(f"采集点云失败: {exc}\n{traceback.format_exc()}")  # ✅ 完整异常追踪
        return _api3d_error(exc)
```

### 3. 创建调试工具

#### 调试页面：`/vision/test-capture-debug/`
- ✅ 独立测试两个API端点
- ✅ 显示URL配置
- ✅ 显示详细的请求/响应日志
- ✅ 自动检测Content-Type
- ✅ 友好的错误展示

#### 测试脚本：`test_capture_api.py`
- ✅ 命令行快速测试API
- ✅ 不依赖浏览器
- ✅ 验证后端逻辑是否正常

#### URL检查脚本：`test_url_routing.py`
- ✅ 验证所有相关URL是否正确配置
- ✅ 显示实际的URL路径

## 🚀 使用方法

### 方法1: 使用调试页面（推荐）

1. 启动Django服务器：
```bash
cd d:\workspace2\AutomaticOrder
python manage.py runserver
```

2. 访问调试页面：
```
http://127.0.0.1:8000/vision/test-capture-debug/
```

3. 点击测试按钮，查看结果

### 方法2: 使用测试脚本

```bash
cd d:\workspace2\AutomaticOrder
python test_capture_api.py
```

### 方法3: 在实际页面调试

1. 访问工作台：
```
http://127.0.0.1:8000/vision/rack-locator/
```

2. 打开浏览器开发者工具（F12）

3. 切换到Console标签，查看详细错误日志

4. 切换到Network标签，查看实际的HTTP请求和响应

## 🔧 排查步骤

如果问题仍然存在，请按以下顺序排查：

### 步骤1: 验证后端API
```bash
python test_capture_api.py
```
- ✅ 如果显示 "JSON解析成功"，说明后端正常
- ❌ 如果失败，检查数据库连接、相机配置等

### 步骤2: 检查URL配置
```bash
python test_url_routing.py
```
- 确认所有URL都能正确解析
- 记下URL路径，与前端配置对比

### 步骤3: 检查前端配置
打开 `templates/vision/rack_locator_panel.html`，查找：
```javascript
window.rackLocatorConfig = {
  captureUrl: '{% url "vision:api_vision_3d_capture" %}',
  legacyCaptureUrl: '{% url "vision:api_rack_location_workbench_capture" %}',
  ...
};
```

确认URL模板标签能正确渲染。

### 步骤4: 检查CSRF Token
在页面源代码中确认存在：
```html
{% csrf_token %}
```

或者：
```html
<input type="hidden" name="csrfmiddlewaretoken" value="...">
```

### 步骤5: 查看服务器日志
在Django控制台（运行 runserver 的终端）中查看是否有：
- 异常追踪
- 404错误
- 权限错误

### 步骤6: 检查浏览器Network标签
1. 打开F12开发者工具
2. 切换到Network标签
3. 点击「采集点云」按钮
4. 查看请求：
   - Request URL: 是否正确？
   - Status Code: 是什么？（200, 400, 403, 404, 500?）
   - Response Headers: Content-Type是什么？
   - Response: 实际内容是什么？

## 📊 API测试结果示例

### ✅ 成功的响应：
```json
{
  "success": true,
  "pointcloud_token": "vision/rack_workbench/2026/07/02/rack_workbench_041350_586315.npy",
  "preview_image_url": "/media/vision/rack_workbench/2026/07/02/rack_workbench_preview_041350_603542.png",
  "image_width": 640,
  "image_height": 480,
  "source": "dm_camera"
}
```

### ❌ 失败的响应示例：

**场景1: 404错误（URL不存在）**
```
Status: 404 Not Found
Content-Type: text/html
Body: <!DOCTYPE html>...Page not found...
```

**场景2: 403错误（CSRF验证失败）**
```
Status: 403 Forbidden
Content-Type: text/html
Body: <!DOCTYPE html>...CSRF verification failed...
```

**场景3: 500错误（服务器内部错误）**
```
Status: 500 Internal Server Error
Content-Type: text/html
Body: <!DOCTYPE html>...Server Error...
```

## 💡 建议

1. **清除浏览器缓存**：有时旧的JS文件会导致问题
2. **检查登录状态**：确认已登录Django后台
3. **检查权限**：确认用户有访问视觉模块的权限
4. **查看中间件**：检查 `settings.py` 中的 `MIDDLEWARE` 配置
5. **调试模式**：确认 `DEBUG = True` 以查看详细错误

## 📝 文件修改清单

修改的文件：
- ✅ `apps/vision/views.py` - 增强错误处理
- ✅ `apps/vision/urls.py` - 添加调试页面路由
- ✅ `static/vision/js/rack_locator_workbench.js` - 改进前端错误处理

新增的文件：
- ✅ `templates/vision/test_capture_debug.html` - 调试页面
- ✅ `test_capture_api.py` - API测试脚本
- ✅ `test_url_routing.py` - URL检查脚本
- ✅ `启动调试工具.md` - 调试指南
- ✅ `修复说明_采集点云错误.md` - 本文档

## 🎯 预期结果

修复后，当点击「采集点云」按钮时：
1. 如果成功：显示点云预览图，可以绘制ROI
2. 如果失败：显示清晰的错误信息，而不是 "Unexpected token '<'"

## 📞 后续支持

如果问题仍未解决，请提供：
1. 浏览器Console的完整错误日志
2. Network标签中的请求详情
3. Django服务器控制台的输出
4. `test_capture_api.py` 的运行结果
