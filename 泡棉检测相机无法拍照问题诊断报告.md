# 泡棉检测相机无法拍照问题诊断报告

## 🔍 问题现象

点击"拍照检测"按钮后，相机无法拍照，系统报错。

## 📋 诊断结果

### 错误信息
```
RuntimeError: Hik camera capture failed for CAM-INSPECT-FOAM-01 (FOAM_INSPECTION): 
连接相机失败: 相机 0 打开设备失败，错误码: -2147483115
```

### 错误码含义
**错误码 -2147483115** (0x80070005) 对应海康威视 SDK 的 `MV_E_ACCESS_DENIED` 错误，表示：
- **相机被其他程序占用**
- **相机权限不足**
- **相机未正确初始化**

## 🎯 根本原因

通过分析代码和运行测试，问题出在：

### 1. 相机连接状态异常
相机可能被以下程序占用：
- 海康威视 MVS 客户端软件（MVS Client）
- 其他正在运行的视觉检测程序
- 上一次未正确关闭的相机会话

### 2. 网络配置问题
当前配置的相机参数（`settings.py`）：
```python
'CAMERA_IP': '169.254.160.253',  # 相机 IP
'PC_IP': '169.254.160.95',       # 本机 IP
```

可能存在的问题：
- 相机未通电或网络连接断开
- IP 地址配置错误
- 防火墙阻止连接
- 网卡未正确配置 169.254.x.x 网段

### 3. SDK 环境问题
- SDK DLL 加载失败
- 驱动未正确安装
- 权限不足

## 🛠️ 解决方案

### 方案 1：检查并释放相机占用 ⭐️ 推荐优先尝试

#### 步骤 1: 关闭可能占用相机的程序
```cmd
# 在任务管理器中结束以下进程：
- MVS Client
- MVS
- Hikrobot
- 其他视觉检测软件
```

#### 步骤 2: 重启 Django 服务器
```cmd
# 停止当前服务器（Ctrl+C）
# 重新启动
python manage.py runserver
```

#### 步骤 3: 测试相机
```cmd
python test_camera_capture.py
```

### 方案 2：检查网络连接

#### 步骤 1: 验证相机 IP 可达性
```cmd
# 测试相机网络连通性
ping 169.254.160.253

# 如果 ping 不通，说明网络连接有问题
```

#### 步骤 2: 检查本机 IP 配置
```cmd
# 查看网卡配置
ipconfig

# 应该有一个网卡配置为 169.254.160.95
# 如果没有，需要手动配置网卡：
# 控制面板 -> 网络和共享中心 -> 更改适配器设置
# 右键相机连接的网卡 -> 属性 -> IPv4 -> 手动配置
# IP: 169.254.160.95
# 子网掩码: 255.255.0.0
```

#### 步骤 3: 关闭防火墙（测试用）
```cmd
# 临时关闭 Windows 防火墙测试
# 控制面板 -> Windows Defender 防火墙 -> 关闭防火墙
```

### 方案 3：检查相机硬件

#### 步骤 1: 确认相机通电
- 检查相机电源指示灯是否亮起
- 检查网线是否插好

#### 步骤 2: 使用 MVS 客户端测试
1. 打开海康威视 MVS Client 软件
2. 扫描设备，查看是否能发现相机
3. 尝试连接相机并获取图像
4. **如果 MVS 能正常工作，关闭 MVS 后再测试 Django 程序**

### 方案 4：重新安装相机驱动

#### 步骤 1: 卸载现有驱动
```cmd
# 控制面板 -> 程序和功能
# 卸载 "MVS" 或 "Hikrobot Camera Driver"
```

#### 步骤 2: 重新安装
1. 从海康威视官网下载最新版 MVS SDK
2. 安装 SDK（确保勾选"驱动"选项）
3. 重启电脑

#### 步骤 3: 验证 SDK 路径
确认 `C:\Program Files (x86)\Common Files\MVS\Runtime\Win64_x64` 目录存在且包含 DLL 文件。

### 方案 5：修改配置使用 USB 连接（如果相机支持）

如果相机同时支持网络和 USB 连接，可以尝试 USB 连接：

编辑 `AutomaticOrder/settings.py`：
```python
'HIK_CAMERA': {
    'OUTPUT_DIR': BASE_DIR / 'media' / 'hik_captures',
    'SDK_LIB_DIR': 'C:/Program Files (x86)/Common Files/MVS/Runtime/Win64_x64',
    # 注释掉网络配置，使用 USB 模式
    # 'CAMERA_IP': '169.254.160.253',
    # 'PC_IP': '169.254.160.95',
    'CAMERA_IP': None,  # USB 模式不需要 IP
    'PC_IP': None,
    'FORMAT': 'BMP',
    'QUALITY': 5,
    'RUN_IN_SUBPROCESS': True,
},
```

### 方案 6：使用直接模式（跳过子进程）

有时子进程模式会导致权限问题，可以尝试直接模式：

编辑 `AutomaticOrder/settings.py`：
```python
'HIK_CAMERA': {
    # ... 其他配置保持不变 ...
    'RUN_IN_SUBPROCESS': False,  # 改为 False
},
```

**注意**：直接模式可能会导致 Django 进程在相机出错时崩溃，建议解决问题后改回 True。

## 🧪 验证修复

### 测试 1: 运行诊断脚本
```cmd
python test_camera_capture.py
```

**期望输出**：
```
============================================================
🔍 测试相机拍照功能
============================================================

⏳ 正在拍照...
✅ 拍照成功！
   - success: True
   - image_path: D:\workspace2\AutomaticOrder\media\hik_captures\xxx.bmp
   - camera_code: CAM-INSPECT-FOAM-01
   - task_type: FOAM_INSPECTION
   - 文件大小: 30228534 字节
```

### 测试 2: 在网页上测试
1. 访问泡棉检测工作台：http://127.0.0.1:8000/vision/foam-inspector-interactive/
2. 点击"刷新预览"按钮
3. 点击"拍照检测"按钮
4. 查看是否正常显示检测结果

## 📊 常见错误码参考

| 错误码 | 16进制 | 含义 | 解决方法 |
|--------|--------|------|----------|
| -2147483115 | 0x80070005 | 设备被占用/权限不足 | 关闭其他程序，检查权限 |
| -2147483644 | 0x80070002 | 设备未找到 | 检查连接，确认相机通电 |
| -2147483640 | 0x80070006 | 无效句柄 | 重启程序，重新初始化 |
| -2147483647 | 0x80000001 | 通用错误 | 检查 SDK 安装，查看日志 |

## 🔄 问题持续存在？

如果尝试以上所有方案后问题仍然存在，请：

### 1. 收集诊断信息
```cmd
# 运行完整诊断（注意：这是 3D 相机诊断脚本，需要创建 HIK 版本）
python diagnose_camera.py

# 查看最近的错误日志
dir media\hik_captures\.hik_capture_*.json* /o-d
```

### 2. 检查系统日志
- Windows 事件查看器 -> Windows 日志 -> 应用程序
- 查找 "MVS" 或 "Hikrobot" 相关的错误

### 3. 联系技术支持
准备以下信息：
- 相机型号：MV-CS050-10GC
- SDK 版本
- 错误信息完整截图
- 网络配置信息（ipconfig 输出）
- MVS Client 是否能正常工作

## 📝 预防措施

### 1. 规范操作流程
- 使用前确保相机未被其他程序占用
- 使用完毕后正常关闭程序（不要强制终止）
- 定期重启相机电源（每周一次）

### 2. 独占访问
建议在生产环境中：
- 专用一台电脑运行泡棉检测程序
- 不要同时打开 MVS Client 和检测程序
- 设置开机自动启动检测程序

### 3. 监控和告警
可以添加相机健康检查：
```python
# 定期（如每小时）运行
python test_camera_capture.py
# 如果失败，发送告警通知
```

## ✅ 总结

**最可能的原因**：相机被其他程序（如 MVS Client）占用

**最快的解决方法**：
1. 关闭所有可能占用相机的程序
2. 重启 Django 服务器
3. 运行 `python test_camera_capture.py` 验证

**建议操作顺序**：方案 1 → 方案 2 → 方案 3 → 其他方案
